/bin/
*.sqlite
*.sqlite-wal
*.sqlite-shm
coverage.out
.DS_Store
images/dist/
.private-journal/
.kata.local.toml

# node deps for web/; dist/ is committed on purpose (embedded in the binary).
web/node_modules/
# tsc's incremental build cache: machine-local, rewritten by every typecheck.
web/tsconfig.tsbuildinfo

# Local scratch that must never reach the public repo: a Repomix dump of the
# whole tree, and the kata tool's project file.
output.txt
.kata.toml
