# vmobs build plan

Working state for building the system in `docs/SPEC.md`. Update the session log at the end of every session. Spec milestones M0–M5 (SPEC §18) remain the acceptance structure; the phase order below front-loads everything buildable and honestly testable on a macOS dev host, because M0/M1 gates need a KVM Linux host we haven't picked yet.

## Strategy

Two tracks:

- **Portable core** (buildable now, real-component tests on any OS): protocol types, event store, API, CLI, situation/attention, run objects and reports, admission bookkeeping. The runtime sits behind an interface; unit tests use the SPEC §18-sanctioned fake runtime. No fake ever serves as a mode of the real daemon.
- **Linux track** (needs the KVM host): privd, runner + spool, jailer/Firecracker adapter, doctor, guest image + guestd, eBPF/fanotify sensors, network namespaces/nftables/mitmdump, terminal path, final-disk inspection, and every acceptance test that touches a real VM.

## Phases

| Phase | Content | Spec refs | Status |
|---|---|---|---|
| P0 | Scaffold: module, plan, conventions, `scripts/check` | §0, §18 | done |
| P1 | Evidence spine: `internal/events` (envelope, validation, kind registry), `internal/store` (WAL SQLite, dedup, integrity, keyset queries), `internal/api` (/meta, /meta/event-kinds, /events, structured errors), `cmd/vmobsd`, `cmd/vmobs` CLI | §12, §14, §14.1, R-04, R-12, R-17 partial | done |
| P2 | Situation + attention + annotations: attention queue (collapse, ack, overflow health record), /situation snapshot + `?since` delta, /annotations | §12.7, R-15, R-14 partial | done |
| P3 | VM registry, operations, admission reservations, runtime interface + fake runtime, lifecycle event emission | §5, §6, R-01, R-02, R-09 partial, R-10 partial | done |
| P4 | Declarative runs + reports: run state machine, criteria evaluation, report generation with reproduce_query rollups | §8.6, §8.7, R-16 | done |
| P5 | Auth (local operator, sessions, CSRF), owner scoping | §15.1, R-11 | done |
| L0 (M0) | Remote exec, root helper, runtime lock, preflight doctor, vsock proto, guestd, image pipeline, network baseline, boot fixture | §18 M0 | done — gate green, AT-004 TESTED_PASS, merged to main |
| L1a (M1) | privd, runner + spool, jailer adapter, doctor-gated admission, real lifecycle | §18 M1 | done — gate green twice on aibox03, merged to main at `be48165`; plan: docs/superpowers/plans/2026-09-01-m1a-real-runtime.md |
| L1b (M1) | Phase A: launch form, batch launch, multi-select actions, recent operations (§13.1, §13.7). Phase B: guest PTY broker, vsock 10002 relay, bounded session rules, authenticated WebSocket, xterm client (§8) | §18 M1 | done — Tasks 1-14 built on `m1-fleet-page`; `TestM1bGate` 12/12 twice on aibox03, AT-019..AT-030 recorded, §18's storage-and-stop clause closed by a real browser run; merged to main at `f7ddf3d`. Plan: docs/superpowers/plans/2026-09-04-l1b-web-and-terminal.md |
| L2a (M2a) | Guest telemetry transport: vsock 10001, guest bounded ring, runner-assigned stream identity with guest-owned seq, `guest.sensor_health`, `telemetry_health` + a real `SensorsDegraded`, Reconcile adopts a healthy runner | §18 M2, §110, §138–§139, §320, §507, §952 | done — `TestM2aGate` 6/6 on aibox03, AT-074 and AT-075 recorded TESTED_PASS (partial), merged to main at `0517f19`. Plan: docs/superpowers/plans/2026-09-05-m2a-guest-telemetry-transport.md |

Auth note: P5 landed. Loopback dev mode (`require_authentication: false`, `mode: loopback_only`) remains valid for local iteration. Any non-loopback bind now requires `mode: https` with TLS files and `require_authentication: true`; config validation and the bind-time loopback guard both enforce this.

## Open questions for Doctor Biz

None right now.

## Linux track host (answered 2026-08-31)

`aibox03` — tailnet `100.64.0.100`. Verified by probe: bare metal (`systemd-detect-virt: none`), x86_64 Intel VT-x (`kvm_intel` loaded), Ubuntu 24.04.4 LTS, kernel 6.8.0-134 (fanotify + eBPF capable), 32 CPUs, 62 GiB RAM, 60 GiB free on `/`. SSH works as `harper` (key auth). Setup needed at L0 start: add `harper` to the `kvm` group (has sudo), install firecracker. Kernel/image pipeline targets x86_64.

Host kernel drift: aibox03 runs host kernel 6.8 (observed 6.8.0-138 at L0 close). Firecracker v1.16.1's validated guest kernel is 6.1; its stated minimum host kernel is 5.10. The host 6.8 kernel sits between the project's validated guest 6.1 and Firecracker v1.16.1's upper tested bound of 6.18 — no documented incompatibility, but outside the exactly-validated pairing. Accept for M0; re-evaluate at M1 if jailer or KVM behavior differs from docs.

## Deviations log (SPEC §0: record deviations as architecture decisions)

- 2026-08-31 — Build order deviates from spec milestones: portable core first because the dev host is macOS (no KVM). All M0–M5 gates still require the real Linux host; nothing is claimed accepted without it.
- 2026-08-31 — Interchange schemas stay in `docs/schemas/` (the docs package validates them); implementation tests read them from there instead of a root `schemas/` dir. Revisit if/when generated protocol docs land.
- 2026-08-31 — CLI exit codes fixed as: 0 success, 1 structured API failure, 2 transport failure, 3 usage error (SPEC §14.1 names the classes but not the numbers).
- 2026-08-31 — Annotation `target_ref` format fixed as `<type>:<id>` with type ∈ {vm, boot, run, event, operation, template, artifact, host} and a printable non-space id (spec leaves the format open). Served through the target_ref_invalid remediation so agents learn it from the error.
- 2026-08-31 — `/situation` omits `capacity_free_mib` (spec example shows it) until P3 admission bookkeeping exists: serving a number with no accounting behind it would be invented evidence. `changed_vms` is `[]` for the same reason (no VM registry yet). `sensors_degraded` is 0 because zero sensors exist, which is the truth of the host today.
- 2026-08-31 — Attention/annotation events ride the store's own host-wide stream (`vm_id` null at the envelope level; VM linkage in event data and queue rows) so store-synthesized events never fight producer stream scope binding.
- 2026-08-31 — Of the eight configured trigger classes, P2 implements only `telemetry_degraded`. P3 adds `lifecycle_failed`, `reconciliation_surprise`, and `capacity_exhausted` (sourced from vm.state_changed and operation.state_changed). Four now active; `run_concluded`, `spool_threshold`, `disk_reserve_threshold`, `policy_denial_anomaly` remain unimplemented until their source events exist (P4+). `/meta` keeps serving enabled∩implemented.
- 2026-08-31 — Boot identity in P3 is a UUID carried in vm.state_changed events (`boot_id` on transitions that establish a new boot), not a boots table. The §5.1 Boot entity's other fields (guest boot id, runner instance, kernel/agent versions) only exist once a real runner boots a real guest (L0); a table of nulls would be invented structure.
- 2026-08-31 — Batch IDs use `batch-%06d` format, parallel to `op-%06d` for operations. SPEC §6.3 does not specify the wire format for batch IDs; this is consistent with the established ID scheme.
- 2026-08-31 — The batch-level operation uses kind `vm_batch.create`, stored in the operations table (not the events registry). It is not a registered event kind; it is an operation record. Batch member operations remain `vm.create` as for individual VMs.
- 2026-08-31 — `max_batch_size=50` is a protocol constant in the API layer. SPEC §6.3 does not specify a maximum; 50 is a request-shape bound in the spirit of P-01 (every request and response bounded). It is not a capacity control — admission governs capacity; the provisioning semaphore governs launch parallelism.
  - **Superseded 2026-09-04 (L1b Task 4).** The maximum is specified — SPEC §347's tunables table says "Batch size limit: 8 per request" — and `docs/examples/host-config.yaml` already shipped `max_batch_size: 8`, so the API enforced 50 while the config file, the example, and the spec all said 8. `config.Admission.MaxBatchSize` was referenced by nothing outside tests: dead config. The cap is now `Server.maxBatchSize()` — the host's configured value, falling back to `api.DefaultMaxBatchSize = 8` — and `GET /meta`, `GET /host/status` and the enforcement check all read it. The table is labelled a tunable starting point, which is why the number stays configurable rather than becoming a constant 8.
- 2026-08-31 — Batches without an `idempotency_key` are never treated as replays of each other. The UNIQUE partial index `WHERE idempotency_key IS NOT NULL` makes NULL keys outside idempotency scope, matching the same convention as single-VM create.
- 2026-08-31 — `reservation_mode` is required-explicit on POST /vm-batches (no default). SPEC §6.3 says to support both modes "explicitly" and names no default; making the caller choose keeps admission semantics visible in every request. `on_failure` does default (`keep_successful`) because the spec names that default outright.
- 2026-08-31 — Idempotent replays of POST /vms and POST /vm-batches return the same 201 as the original request, with `is_replay: true` in the body (absent otherwise). Retry-transparent status plus an honest explicit marker; SPEC fixes neither status code.
- 2026-08-31 — `on_failure=stop_successful` with any member failure finalizes the batch operation as `failed` (cause `member_failed`) even when some members launched before the stop wave. The contract was "all or stop"; per-member operations keep their own honest verdicts — a launch that succeeded before the wave stays `succeeded` while its VM is stopped.
- 2026-08-31 (P4-R1) — All three criteria types accepted at run creation; criteria that cannot be evaluated at conclusion conclude `inconclusive` with a typed reason — §8.6 sanctions exactly this.
- 2026-08-31 (P4-R2) — `on_completion=stop_and_finalize` refused with `missing_capability` (501) at all three run ingress points (POST /vms/{id}/runs, launch run block, batch member run block) — `validateRunBlock` is the single shared validator so the refusal is structurally impossible to bypass. The run-report schema's `final_diff.status` enum has no honest value for requested-but-impossible finalization.
- 2026-08-31 (P4-R3) — Guest result is a run-row record served with `"provenance":"guest_reported"`, plus a `run.result_recorded` event that is `host_observed` with a registry caveat that `data` content is guest-supplied.
- 2026-08-31 (P4-R4) — One non-terminal run per VM, enforced by a partial unique index. §12.7 serves a singular `active_run`.
- 2026-08-31 (P4-R5) — `run-report.schema.json` `boot_ids` relaxes `minItems` 1→0: a run whose VM failed before boot honestly has zero boots, and AT-094 requires a report in every terminal phase.
- 2026-08-31 (P4-R6) — Rejected guest submissions (oversized/malformed) recorded as `run.submission_rejected` events (host_observed, bounded reason).
- 2026-08-31 (P4-R7) — Report retry: Manager re-enqueues generation on reconcile for terminal runs without a stored report; GET of a failed report re-enqueues one attempt. Generation is deterministic and idempotent. GET on a non-terminal run returns pending WITHOUT enqueueing (prevents spurious failed ops).
- 2026-08-31 (P4-R8) — A launch-attached run whose VM fails before running concludes `inconclusive`, `evaluated_by:"system"` (`aborted` reserved for operator abort).
- 2026-08-31 (P4-R9) — `exec_exit_zero` evaluation branch exists but no exec can attach until L0; such runs conclude only via abort or VM-terminal (→ `inconclusive`, reason `no exec attached; exec capability not built`).
- 2026-08-31 (P4-R10) — Guest ingress surface is `Manager.SubmitRunProgress` / `Manager.SubmitRunResult` (Go methods), not HTTP routes.
- 2026-08-31 (P4-R11) — Progress submissions share the `guest_result_max_bytes` bound.
- 2026-08-31 (P4-R12) — Every terminal phase is reached through `concluding`. Abort transitions to `concluding` and records `aborted` in the same tx (two `run.state_changed` events, one tx).
- 2026-08-31 (P4 session) — `on_completion=stop_and_finalize` R2 refusal was extended to all three ingress points in Task 8 (the launch-run block and batch member block both call `validateRunBlock`); launch-request.schema.json permits the field on the wire so the API must refuse everywhere.
- 2026-08-31 (P4 session) — One vm_id matching semantic: `store.Query` applies column OR `json_extract(payload,'$.data.vm_id')` unconditionally. The per-family MatchDataVMID knob and the `family=run` special-case were deleted. Column-only matching was an accidental artifact of the host-wide-stream deviation, not a contract; it caused a live P-05 violation where `family=vm` reproduce_queries returned 0 while the stored count was >0 (Task 7 fix round 2).
- 2026-08-31 (P4 session) — `boot_ids` read from event data (`data.boot_id` on `vm.state_changed`), not the envelope field which is never set (Task 7 fix round 1).
- 2026-08-31 (P4 session) — Terminal-record evidence link uses the `family=run` form; kind= filtering cannot match host-stream events with NULL vm_id column, and the one-event window keeps identical precision (Task 7).
- 2026-08-31 (P4 session) — `CountEventsForReport` dropped its `data.run_id` arm for structural congruence with the API's matching — counts and reproduce_queries now take the same path (Task 7).
- 2026-08-31 (P4 session) — Placeholder kinds `net.flow` / `net.dns` / `net.policy` registered with explicit caveats (not emitted in the portable core) so schema-required `network_summary` counted fields carry reproduce_queries that honestly return 0 (Task 7).
- 2026-08-31 (P4 session) — `runs.result_received_at` column added; the result wire shape requires `received_at`, assigned at trusted ingress in `SubmitRunResult` (Task 8 fix).
- 2026-08-31 (P4 session) — `GET /runs?phase=<unknown>` → 400 `phase_unknown` for consistency with `family_unknown` on GET /events (Task 8 fix).
- 2026-08-31 (P4 session) — `situation.active_run`: the P3-era wire field was `any` always serializing null; now `*wireActiveRun` with omitempty — key absent when no active run, per §12.7 (Task 9).
- 2026-08-31 (P4 session) — CLI run-flag group rule: any of the four flags engages the group; `--run-goal/--run-criteria/--run-on-completion` must then be complete; `--run-progress-events` is optional within a complete group (Task 10 fix).
- 2026-08-31 (P4 session, PARKED) — Startup `Reconcile` generates missing reports synchronously before serving. Acceptable at current scale; revisit if report backlogs grow.
- 2026-09-01 (P5-A1) — `auth.mode: local_operator` is the only built auth mode. `server.trust_forwarded_identity: true` (reverse-proxy identity, §15.1's "or" branch) is refused at config validation as not built. YAGNI; the spec offers either.
- 2026-09-01 (P5-A2) — `require_authentication: false` is valid only with `server.mode: loopback_only`; the middleware then injects identity `local_operator` (host-ACL trust — the P1–P4 model, kept for dev). Non-loopback always authenticates; enforced at config validation and at bind.
- 2026-09-01 (P5-A3) — Sessions are in-memory with a fixed absolute TTL (`auth.session_ttl_minutes`, default 720). Daemon restart logs the browser out. No session material at rest; no sliding renewal.
- 2026-09-01 (P5-A4) — CSRF: synchronizer token per session, required via `X-CSRF-Token` on every cookie-authenticated mutation (POST/PUT/PATCH/DELETE). Bearer-token requests are exempt (a browser cannot set `Authorization` cross-origin). When an `Origin` header is present on a cookie-authenticated mutation it must exactly equal `server.public_origin`. `auth.csrf_protection`, `auth.session_cookie_http_only` cannot be false while authentication is enabled (config error): there is no built "cookies without CSRF" mode.
- 2026-09-01 (P5-A5) — CLI tokens: `vmobs_` + 43 base64url chars (256 random bits), stored SHA-256-hashed. Sent as `Authorization: Bearer`. Scope = the owner's full API (single-operator V1): "scoped" means bound to owner identity, revocable, optionally expiring — not per-route grants.
- 2026-09-01 (P5-A6) — The credential store is a directory (`auth.credential_store`): `operator.json` (argon2id: t=1, m=64MiB, p=4, 16B salt, 32B key, params stored alongside) and `tokens.json`. Dir 0700, files 0600, atomic tempfile+rename writes, re-read per operation (external mint/revoke visible immediately; V1 scale makes caching pointless). Deliberately not in the event-store SQLite: credentials and evidence are different trust domains.
- 2026-09-01 (P5-A7) — Bootstrap is offline: `vmobsd init-auth` creates the credential store and mints one initial token, printed once. No unauthenticated bootstrap endpoint. Password arrives via `-password-stdin` or `VMOBSD_OPERATOR_PASSWORD`, never argv (§15.3). Re-running against an initialized store is an error. Password rotation = delete + re-init (documented; a rotate command is future work).
- 2026-09-01 (P5-A8) — Denial taxonomy: missing/invalid credentials → 401 `unauthenticated` (remediation: login or bearer token); expired/unknown session → 401 cause `session_invalid_or_expired`; CSRF/origin failure → 403 `csrf_rejected` / `origin_rejected`; cross-owner resource access → the same 404 `not_found` body as an unknown ID (existence hidden; §14 groups "unauthorized/not found").
- 2026-09-01 (P5-A9) — Owner scoping applies to owned resources: vms, operations, runs (+ reports), vm-batches, and their mutations. Host-scoped surfaces (meta, events, situation, attention, annotations, host/status, templates) require authentication but not ownership — they are the host's shared evidence plane. Annotations record the authenticated identity as `author`.
- 2026-09-01 (P5-A10) — System-initiated operations (reconcile repairs, report generation, post-run stops) carry the owning resource's owner — the system acts on the owner's behalf; a distinct `system` owner would hide those operations from the operator's own authorized reads. `ManagerConfig.Owner` is deleted; creation paths take an explicit `owner` parameter from trusted ingress.
- 2026-09-01 (P5-A11) — Auth event kinds (all `host_observed`, host-wide stream): `auth.session_created`, `auth.session_ended`, `auth.login_failed`, `auth.token_created`, `auth.token_revoked`. Login attempts are globally serialized with a post-failure delay (default 500ms; test-tunable), so the unauthenticated path cannot flood the event stream. Event data carries bounded username (≤64 bytes, truncation marker) and connection remote address — never passwords, session IDs, or token secrets.
- 2026-09-01 (P5-A12) — `server.mode: https` with `tls_cert_file`/`tls_key_file` is the one non-loopback mode: it forces `require_authentication: true`, `session_cookie_secure: true`, and an `https://` `public_origin`. `loopback_only` mode refuses configured TLS files (a cert nothing serves is a config lie). Exact-origin WebSocket checks arrive with terminals (L0).
- 2026-09-01 (P5-A13) — Auth routes live at `/api/v1/auth/{login,logout,session,tokens}` — §14's table names no auth routes; §15.1 demands the capability. As built: `GET /auth/session` answers 200 with identity in auth-off mode and for authenticated callers; it returns 401 (typed cause + login remediation) for unauthenticated callers when auth is on — an honest "no credentials, here is where to get them" answer that satisfies A13's intent. Login/logout/token routes answer 409 `auth_disabled` when `require_authentication: false`.
- 2026-09-01 (P5-T7) — The auth login route is NOT in `/meta`'s Links — `TestMetaIsHonest` requires every link to GET 200 and login is POST-only; instead the login path rides every 401 remediation (`params.path`), which is where an agent needs it.
- 2026-09-01 (L0-T7-D1) — Default routes (/0) are skipped in overlap checks. A default route's Bits()==0 mathematically contains every address; including it would exclude all pools and make NewAllocator impossible to construct on any real host. The skip is documented in code; §10.1's intent is LAN/VPN collision detection, not global reachability.
- 2026-09-01 (L0-T7-D2) — The consumed input for ParseIPRoutes is `ip -json route show table all`, not plain `ip -json route`. On aibox03, tailscale uses policy routing table 52; the main table omits those entries entirely, making the §10.1 collision check false. The table-all output includes type=local/broadcast/multicast entries (interface addresses, not transit routes); ParseIPRoutes filters them by the "type" field (keeping "" and "unicast" only). Tailscale routes on aibox03 are individual /32 peer routes, not a /10 block.
- 2026-09-01 (L0-R1) — `scripts/linux` rsyncs the working tree to `aibox03:~/vmobs-build/` and runs a command over SSH. No git remote is created.
- 2026-09-01 (L0-R2) — Root boundary for M0: one-time setup is a script Doctor Biz runs by hand (password sudo). Ongoing root ops go through `/usr/local/sbin/vmobs-root-helper` — root-owned, typed verbs, strict argument validation — with a sudoers NOPASSWD entry for exactly that path. Replaced by `vmobs-privd` in M1.
- 2026-09-01 (L0-R3) — Kernel and rootfs builds run inside docker containers on aibox03 (pinned base digests). Docker is a build sandbox only; no container runtime appears in any served path.
- 2026-09-01 (L0-R4) — Guest userspace: Ubuntu 24.04 base (pinned digest), systemd init, `vmobs-guestd` as a systemd unit.
- 2026-09-01 (L0-R5) — Rootless imaging: rootfs and config disks built with `mkfs.ext4 -d <dir>`. Reproducibility = pinned inputs + recorded output digests; timestamp variance documented.
- 2026-09-01 (L0-R6) — vsock library: `github.com/mdlayher/vsock v1.3.0`, pinned in go.mod.
- 2026-09-01 (L0-R7) — Auth proof in the §7.4 handshake: the per-boot capability token delivered via the read-only config device (§7.3), compared constant-time. No HMAC; vsock has no third party.
- 2026-09-01 (L0-R8) — Doctor freshness: cheap checks re-run on demand; the expensive live-guest check is the fixture; checks whose subject is not built yet report `not_implemented` (honest rows beat missing rows).
- 2026-09-01 (L0-R9) — Lock absence vs mismatch: present-but-mismatched = fatal at startup (AT-002). Absent lock = degraded dev state: daemon runs, doctor `fc_binaries` fails, launches refused.
- 2026-09-01 (L0-R10) — M0 network scope: private networking = per-VM namespace + TAP + veth with anti-spoof and default-deny. No uplink routing, DNS, or policy profiles (M3). Deviation: implemented in M0 as default-deny only (nft forward-drop); anti-spoof deferred to M3 with the egress policy work, since M0 assigns the guest no IP/MAC contract to enforce against (SPEC §10.1 anti-spoofing lands with policy profiles).
- 2026-09-01 (L0-R10b) — images/rootfs/guestd.service uses After=local-fs.target, sufficient while all virtio drivers are built-in; if an M1 kernel re-pin goes modular, revisit unit ordering (device availability).
- 2026-09-01 (L0-R11) — Socket access: fixture VMs run with gid = `vmobs-fixture` (gid 36000); helper sets umask 0002 before jailer, so sockets are group-writable without chmod games.
- 2026-09-01 (L0-R12) — AT-001 linkage: `runtime.ForHost` UnavailableError reason appends preflight overall + first failing check ID when a report exists. Full doctor-gated admission is M1.
- 2026-09-01 (L0-T5) — Unknown JSON fields in context.json tolerated by design: M4 extends the same file additively, so strict rejection would break forward compatibility. `encoding/json` default behavior (silently ignore unknown fields) is the correct policy here.
- 2026-09-01 (L0-T6-frag) — Fragment adjustments from verification: CONFIG_MODULES and CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT added (required by BTF/KPROBES/JIT chains); CONFIG_DEVPTS_FS dropped (not a Kconfig symbol in mainline 6.1 — DEVPTS is built-in); CONFIG_TRACEPOINTS dropped (prompt-less bool, select-only — set automatically by BPF deps). Documented in images/README.md.
- 2026-09-01 (L0-T6-build) — Kernel build-deps installed via live apt inside the build container; rootfs packages snapshot-pinned at 20260820T000000Z. Two different pinning strategies in one pipeline, intentional: build host is ephemeral and disposable; guest userspace is the artifact we ship and must be reproducible.
- 2026-09-01 (L0-T6-repro) — vmlinux timestamp is not reproducible (kernel embeds build time in the binary). Output digest is recorded in runtime.lock.json and verified by VerifyArtifacts before every boot; the non-reproducibility is documented in images/README.md.
- 2026-09-01 (L0-T6-eol) — Guest kernel 6.1.186: Linux 6.1 reached EOL 2026-09-02 (the day after L0 closed). No security patches will follow. Acceptable for M0 (aibox03 is a controlled lab host, no internet traffic into the guest); revisit at M1 — migration path is to re-run images/kernel/build.sh with an updated kernel version and re-pin the lock.
- 2026-09-01 (L0-T6-filter) — `scripts/linux` gains `--filter='P images/dist/'`: macOS openrsync ignores `:- .gitignore` for `--delete` protection, so without the explicit protect rule a sync would delete build artifacts living under gitignored dirs on aibox03.
- 2026-09-01 (L0-T8-deferred) — M0 gate run deferred: `TestM0Boot` (tests/integration/boot_test.go) is fully implemented and reviewed; fixture performs an honest skip naming `scripts/aibox03/setup.sh` when the helper is absent. The two-VM boot has never executed and no evidence file exists. Gate run is a Doctor Biz handoff (password sudo + re-login for kvm group). Evidence will land at tests/integration/evidence/m0-boot-<hostname>.txt.
- 2026-09-01 (L0-R13, first live gate run) — Two boot-path defects found by the first real execution, fixed on `m0-gate-fixes`: (1) jailer chmods its chroot root to 0700 (uid-owned) during startup, locking the vmobs-fixture group out of the v.sock path — every host dial got EACCES; `jail-start` now waits for firecracker to bind v.sock (strictly after jailer's chroot prep) and reopens the dir to 0750. Requires a setup.sh re-run to install the updated helper. (2) `waitForVsock` let a final dial made with a nearly-expired context overwrite the informative last error with "i/o timeout", masking the persistent EACCES; now only errors from attempts that failed on their own merits are recorded (pinned by `TestWaitForVsockReportsLastRealDialError`). Guest-side health verified independently: a foreground unjailed boot of the same images reaches multi-user with guestd started.
- 2026-09-01 (L0-R14, gate green) — Two more defects below the L0-R13 pair, found by driving the guest interactively (`init=/bin/sh` over `screen`) and by rebuilding the rootfs. (3) guestd's config mountpoint `/run/vmobs/config` was never created — `/run` is a fresh tmpfs under systemd, so `LoadBootConfig` died with ENOENT on every boot, invisibly (unit stderr never reached the serial console). Fixed with `os.MkdirAll` before mount, pinned by `TestLoadBootConfigCreatesMountpoint`; rootfs rebuilt, lock re-pinned (54426c1c…). (4) be420fd's "log quoting" cleanup put a raw `"` inside the one big double-quoted docker script in images/rootfs/build.sh, truncating it: the container exited 0 having installed only ca-certificates, and a stale inventory file defeated the emptiness check — the rebuild produced a guestd-less, init-less image. Fixed the quote, added a quoting-discipline comment, removed the stale inventory pre-run, and made mkfs refuse a tree missing guestd/guestd.service. Also: the evidence writer re-stat'd jail paths after teardown (six ENOENTs where AT-004's ownership proof belonged); `statHostFiles` now returns its live observations and the writer prints them. Gate green twice (7.5s each); evidence committed at tests/integration/evidence/m0-boot-aibox03.txt; AT-004 → TESTED_PASS.
- 2026-09-02 (M1a-T9-exit-door) — Guest shutdown execs `systemctl reboot`, not the `systemctl poweroff` the M1a plan mandates (`docs/superpowers/plans/2026-09-01-m1a-real-runtime.md`, file map and Task 9 requirement prose). Firecracker v1.16.1 gives this guest ACPI tables advertising only S0, never S5, so `poweroff` completes a clean systemd shutdown in well under a second and the kernel then halts — the vCPU parks in HLT and the VMM never exits, at any grace period. Host-driven `SendCtrlAltDel` cannot substitute: the pinned kernel has `# CONFIG_SERIO is not set`, so the guest can never receive the injected key event. `reboot=k` is already in the boot args, so a guest-initiated restart writes the i8042 reset byte and Firecracker exits on it: measured at 164.9 ms from `shutdown_ack` to `exit_code=0` on the shipped path, with zero `[FAILED]` lines in 424 console lines. Shipped in `internal/guest/agent.go` `defaultPoweroff` (commit c21234b); `PoweroffFunc` keeps its name because it describes what happens to the VM, not the mechanism. Probe evidence, carried here in full because the probe reports lived in the SDD workspace, which is deleted at close-out: two throwaway diagnostics on aibox03 (2026-09-02) ran the pinned vmlinux and rootfs under an unjailed foreground Firecracker v1.16.1 with production boot args and drove the real guestd over the real vsock protocol (`hello_ack {"accepted":true}` on the real token); declared fidelity deltas: no tap device, no jailer. Probe 1: the boot console prints `ACPI: PM: (supports S0)` where a guest that can soft-off prints `(supports S0 S5)`; `shutdown_ack` came back in 1 ms and the channel closed 17 ms later; the console ran through `Reached target poweroff.target` to `reboot: System halted` about 80 ms after the request, with no `[FAILED]` line and no stop-job timeout, and the VMM was still alive 180 s later at near-zero CPU (vCPU parked in HLT). `PUT /actions {"action_type":"SendCtrlAltDel"}` returned 204 and the guest did nothing for 120 s: the pinned kernel's config (`images/dist/kernel.config` on the build host, an untracked build output) has `# CONFIG_SERIO is not set`. Probe 2, test B: a fresh copy of the same pinned rootfs with only the guestd binary swapped for a `systemctl reboot` build, production boot args, real protocol — `shutdown_ack` in 1 ms, `reboot: Restarting system`, then Firecracker's own stamped `Firecracker exiting successfully. exit_code=0` 164.9 ms after the ack, with zero `[FAILED]` lines in 424 console lines. Probe 1's ~30 ms figure was a sysrq-b `emergency_restart()`, a different trigger and not what ships.
- 2026-09-02 (M1a-T14, deferred to M1b; extended at M1a final-review) — **Daemon shutdown has no ceiling of its own when a create-launch wedges.** Two halves, recorded together because either one alone is survivable and the pair is not. (1) `runLaunch` (`internal/runtime/manager.go:551-552`, `ctx := m.ctx`) runs on the unbounded manager context, not a per-call bounded one — a deliberate inversion, since `start`'s own 240s `launchBudget` (`:283`, applied at `:718`) is what bounds the synchronous call from the HTTP side. The create path has no such bound: `CreateVM` dispatches `runLaunch` through `GoTracked` (`internal/runtime/manager.go:535-546`), so nothing but the jailer's internal timeouts limits it. (2) `Manager.Close` (`:165-171`) is ordered `m.closed = true` → `m.wg.Wait()` → `m.cancel()`, so the cancel that would release a goroutine blocked on `m.ctx` runs only after the wait it is blocked in. Consequence: a wedged create-launch blocks `Close` for as long as it takes to finish on its own, and `Close` runs after `srv.Shutdown`'s 10s (`cmd/vmobsd/main.go:446`) on the way out of `main`, so the process sits past systemd's default 90s `TimeoutStopSec` and is killed rather than stopped. Not fixed in the final-review wave: reordering `Close` changes graceful-shutdown semantics on a branch that spent six gate runs on shutdown timing, and it deserves its own task and its own test. M1b: bound `runLaunch`, then revisit the `Close` ordering with a test that pins the shutdown deadline.
- 2026-09-02 (M1a-T14, deferred to M1b) — The 202-plus-poll async action contract (SPEC's stated shape for long operations) is not built. `start` stays synchronous in M1a; the 240s `launchBudget` is what makes blocking the request thread for a full provisioning transaction safe. M1b builds the real async contract.
- 2026-09-02 (M1a-T14, deferred to M1b) — No periodic `stopping`-reconcile sweep exists. `Reconcile` runs once, from `NewManager` (`internal/runtime/manager.go:129`), at startup only. One sweep, run periodically rather than just at boot, fixes this and the accepted `NotifyVMMExit` consequence (recorded below) together — M1b.
- 2026-09-02 (M1a-T14, deferred to M1b) — The jailer adapter serializes all launches behind one adapter-wide `launchMu` rather than a per-VM lock. Correct and simple at M1a's single-operator scale; a per-VM lock is M1b work once concurrent launch volume matters.
- 2026-09-02 (M1a-T14, deferred to M1b) — M1a's force-stop fix (`7934423`) closes the specific release race it targeted but adds no general sweep for jail chroots leaked by other causes. A cleanup backlog for orphaned chroots is M1b.
- 2026-09-02 (M1a-T14, deferred to M1b) — The guest console is unrecoverable in production: privd builds the jailer argv with `--daemonize` and no `--log-path` (`internal/privd/vmops.go`, `JailerArgv`). That blindness cost this milestone directly — every shutdown-timing theory on this branch was built on pid polling, and one console capture from an out-of-band probe overturned all of them in a single run (see the graceful-stop-path entry below). A recoverable console is M1b.
- 2026-09-02 (M1a-T14, deviation — recorded, not fixed) — The pinned guest kernel is built without `CONFIG_SERIO`/i8042, so `SendCtrlAltDel` can never reach the guest: the API answers 204 and nothing happens. Recorded, not fixed, in M1a scope.
- 2026-09-02 (M1a-T14, deviation — recorded, not fixed) — The pinned rootfs still carries Docker build residue (`/run/systemd/container=docker`, `/.dockerenv` present), so systemd reports "Detected virtualization docker" and silently discards every `systemd.*` kernel argument; the same cause leaves top-level directories owned by uid 1000, not root. Two `rm` lines in the build would fix it, but removing the markers flips systemd's detected virtualization to `kvm`, which changes `ConditionVirtualization` gating throughout the unit set — and this image has never booted to full multi-user as a KVM guest with that gating active. Evidence (probe 2, 2026-09-02, read with `debugfs` from the then-pinned rootfs `4da32a1f…`, read-only): `/run/systemd/container` holds `docker`, `/.dockerenv` is present (0 bytes), and a leftover `/build` directory is there too; the boot console prints `systemd[1]: Detected virtualization docker.`, and a `systemd.unit=reboot.target` boot argument was ignored — the guest booted to a login prompt; removing only `/.dockerenv` and `/run/systemd/container` from a scratch copy flipped the console to `Detected virtualization kvm.` and `Queued start job for default target reboot.target.` (observed cause and effect, not inference). `debugfs` listed `etc`, `usr` and `sbin` owned by uid/gid 1000, read from a scratch copy that had been booted once. The residue comes from `images/rootfs/build.sh:128`, `docker export … | tar -xf -`, which keeps `/.dockerenv` and `/run`, and an unprivileged `tar -xf` cannot restore root ownership. The `0881e69` rebuild used that same export step with no marker removal, so the residue carries into `c6a92bba…` by construction; the record shows no re-read of the new image.
- 2026-09-02 (M1a-T14, deviation — recorded, not fixed) — The rootfs build is not reproducible: `images/rootfs/build.sh:88-90` runs live `apt-get update`/`apt-get install ca-certificates` before the pinned snapshot step, because the snapshot fetch itself needs working TLS first (chicken-and-egg). SPEC §4.2 (`docs/SPEC.md:251`) requires reproducible builds from pinned inputs; M1a discloses the gap in `images/README.md`'s "Reproducibility caveats" section rather than hiding it. Fix is M1b.
- 2026-09-02 (M1a-T14, deviation — recorded, not fixed) — An adopted VM whose VMM disappeared while the daemon was down reads `failed`, because `NewManager`'s startup `Reconcile` (`internal/runtime/manager.go:1099-1108`, case `"running", "paused"`) flips it there before the jailer adapter's own adoption findings are visible — the same ten-line block carries the "adoption is L0" scoping comment, so this is a known, bounded gap, not an oversight. Second half: `Delete` on a VM in this adopted-then-`failed` state leaks a live VMM, because `Release` never signals it. Both wait on real adoption support (M2+). Closed 2026-09-05 (kata `ynx9`, verified against the code): the findings no longer arrive late — `cmd/vmobsd/main.go` builds the `adopted` set from the adapter's findings *before* `runtime.NewManager`, and `ManagerConfig.AdoptedVMs` carries it into the `Reconcile` that runs inside it (M2a Task 6, kata `ffxv`), so a surviving VM keeps its row and only a VMM that really disappeared reads `failed`. The second half closed with it: `Delete` signals before releasing (`internal/runtime/manager.go`, the `if !signalled` guard ahead of the R1 release), and that guard's comment names this issue. A `ForceStop` returning nil is still not proof of death — the forced path drops `SignalVM` errors — so the window is narrowed, not shut, which the comment also says.
- 2026-09-02 (M1a-T14, deviation — recorded, not fixed) — A live VM's compute reservation is released on daemon restart even when the VM survives cleanly: `jailer.Adapter.Reconcile` (`internal/jailer/stop.go:388-396`) classifies a healthy running VM as `"adopted"`, and `cmd/vmobsd/main.go:334` correctly takes no action on that outcome — but `Manager.Reconcile`, called from `NewManager` (`internal/runtime/manager.go:129`) before the adapter's findings are ever consulted, has already released that VM's RAM back to the admission pool. Contradicts SPEC §5.5's "Adopt healthy owned runners" (`docs/SPEC.md:320`). Bounded by single-operator scale today; first thing to fix when real adoption lands. Closed 2026-09-05 (kata `ynx9`, verified against the code): real adoption landed in M2a Task 6. `Manager.Reconcile`'s `running`/`paused` branch returns early for a VM in `cfg.AdoptedVMs`, and adoption is the absence of a write, so no `ReleaseCompute` runs. Pinned by `TestAdoptionKeepsTheVMsComputeReserved` and `TestAdoptionLeavesTheRowUntouched` (`internal/runtime/adoption_test.go`).
- 2026-09-02 (M1a-T14, follow-up — not done in M1a) — `scripts/check`'s gofmt now covers `tests/` and lint runs cross-compiled for linux; four findings had accumulated unseen, one of them dead code the branch itself had added. Half the gap remains open: cross-compiled lint *typechecks* linux-tagged tests but nothing on a Mac can *run* them, so `internal/jailer`'s linux files and all of `tests/integration` execute only on aibox03 or in a Linux container — a contributor on darwin can see a fully green `scripts/check` on a change whose Linux tests fail.
- 2026-09-02 (M1a-T14, follow-up — not done in M1a) — `internal/runtime/batch.go`'s teardown wave (`internal/runtime/batch.go:447-477`) cannot distinguish a `stopping` row another actor legitimately owns from one whose own failed terminal write left it behind. Restructuring was considered and deferred: reproducing that failure needs a genuine store fault, and `Manager.st` is a concrete `*store.Store`, not something injectable at any seam this codebase owns today. An operator can already recover a stuck row via `stop`, `force_stop`, or `Delete(force=true)`, all of which accept `stopping` as a starting state.
- 2026-09-02 (M1a-T14, graceful-stop-path root cause) — Two real product bugs in the stop path were necessary fixes but not sufficient ones. Bug 1: `dialCtl` set a constant 30s socket deadline regardless of the caller's grace period (`internal/jailer/stop.go:57` before the fix), so at the documented default `stop_grace_seconds: 30` the client gave up 5s before the runner's own `grace+5s` reply ceiling could ever answer — the graceful path was structurally unreachable. Fixed by deriving the deadline from grace (commit `43d6f50`). Bug 2: `doStop` checked `reply.OK` but never read `reply.Error`, silently dropping the runner's own typed reason for a refusal. Fixed by surfacing it as a stderr warning (commit `9e793a4`). A third, related fix (commit `569f80d`) added a warning for a previously-silent case: a graceful reply accepted (`reply.OK == true`) but the VMM never observed reaching `vmm_exited`/`finalized` within the poll window.
  That third warning is what proved bugs 1 and 2 were not the true blocker. Run 6, at HEAD `569f80d` with both bugs already fixed, still saw all twelve stops in the run take the forced path — every one through this third, newly visible branch, not the two already-fixed ones (run 6: started 2026-09-02T18:58:20Z on aibox03, 566.69s, 7/8, the one failure `m1a_gate_test.go:1566: AT-011: no vm.state_changed event with reason=graceful_stop found for vmA after stop` — line number at `569f80d`; the daemon's rescued stderr held six startup lines and exactly twelve stop-path warnings, one per stopped VM, each of the shape `jailer: stop: warn: runner accepted graceful shutdown for <vm> but the VMM did not reach vmm_exited or finalized within 35s (last phase observed: "degraded"); escalating to a forced stop` — the message has since gained its allotted-window clause, `b751fe5`, `internal/jailer/stop.go:238`; jail, stage, netns, veth and privd-ledger counts were identical before and after the run, so nothing leaked; the run-6 post-mortem held five files and no runner log, because the rescue then fired only while the state dir existed — at HEAD `disposeVM` rescues before its DELETE on a failed run (`tests/integration/m1a_gate_test.go:965-967`), but at011 records its interruption check with `t.Errorf` (`:1674`) and then deletes A itself (`deleteVM`, `:1009`, at `:1680`), so a failure recorded non-fatally before that line, or after it, still leaves no runner log for A to rescue — a gate limitation, recorded here, not fixed). The runner accepted every shutdown request; the guest control channel then dropped to `"degraded"` (`supervisionLoop`, `internal/runner/runner.go:179`, handling `pingLoop`'s redial return at `:248-250`), the runner sat in redial-backoff for the full window, and the VMM was never observed exiting. The true final blocker was Firecracker v1.16.1 never advertising ACPI S5 to this guest — already recorded above (`M1a-T9-exit-door`, this log, and commit `c21234b`).
  This corrects an earlier misreading in this same investigation: a "guest takes 15.1-16.0s to gracefully power off" reading from an earlier run (recorded in commit `0310fe0`'s message, which raised `stop_grace_seconds` from 10 to 30 on the strength of it) was not a guest measurement at all — it was `doStop`'s own fixed poll-timeout window (old grace 10s + 5s) expiring on schedule, mistaken for the guest's own timing because the guest console is invisible in production (see the console-unrecoverable deferral above). That misreading cost real time and belongs in the record, not just the correction.
- 2026-09-02 (M1a close-out, limitation — recorded, not fixed) — The /30 prefix allocator never reclaims. `internal/network/alloc.go` has `Next()` (`:152`), which advances a cursor, and `Exclusions()` (`:146`); no release, free or return path exists in the package, and `internal/jailer/launch.go:80` is its only caller. The allocator is built once per daemon (`cmd/vmobsd/runtime_linux.go:80`) over two /16 pools (`:76-78`: `10.190.0.0/16` and `10.191.0.0/16`), so a daemon can perform 32,768 fresh launches in its lifetime (2 × 2^14 /30s); after that every fresh launch fails with `ErrPoolExhausted` (`alloc.go:75`). A rolled-back launch and a deleted VM never return their prefix (a stopped VM keeps its manifest and reuses its CIDR on restart, `launch.go:76-78`), and only a daemon restart resets the cursor. Generous at M1a's single-operator scale, so not fixed at milestone close; reclamation is an M1b candidate. `docs/ACCEPTANCE.md`'s AT-005 note claimed the opposite ("reservations are actually freed") until this close-out and now says what the recovery launch proves.
- 2026-09-02 (M1a close-out, deviation — recorded, not fixed) — The launch rollback can miss a stage. Each provisioning stage's side effect precedes its manifest record (`internal/jailer/launch.go:146` before `:150`, `:162` before `:176`, `:218` before `:227`), and `doRollback` (`:483-554`) releases only what the on-disk manifest names, so a failure between a side effect and its record leaks that stage past delete — the netns and privd ledger entry, the VMM, or the runner: `Release` calls `release_network` only for a manifest that names network (`stop.go:456`) and never signals a VMM or a runner, and the rollback has already deleted the manifest. Narrowed 2026-09-03: `Release` no longer needs the manifest — with it gone, `releaseWithoutManifest` calls `release_vm` and `release_network` unconditionally, so the netns, the TAP, the privd ledger entry and the jail chroot are reclaimed on delete after all. What still escapes these windows is the two processes: the runner is never signalled, and a VMM still alive makes `release_vm` answer `invalid_state`, which fails the delete and holds the row at `deleting` — visible now, but not reaped. The AT-005 suite injects at nine points, none in those windows; the runner case is the known escape `4db1081`. A fourth window is the restart path: the reserved write at `:111` replaces the stopped VM's manifest with one naming only `reserved`, so any restart failure before the write at `:150` (in `doStage`, at the `:140` write, at `allocate_network`) rolls back without `release_network` and deletes the record `Release` needed — the stopped VM's netns and ledger entry leak. Fix is M1b: record a stage before its side effect (or have `doRollback` probe rather than trust the manifest), carry a stopped VM's `network` record into the restart's manifest, then inject in each window. The stage dir left this list at commit `02ea171`: until then `doRollback` guarded its removal on the `staged` record, written only after `doStage` returns, so every `doStage` failure orphaned the stage dir for good — the rollback deleted the manifest, and `doRelease` at that commit returned nil without one (`stop.go:440-442`), so nothing ever reclaimed it — that early return is gone as of the 2026-09-03 entry below, which probes the host instead. The guard is gone (`:545-549`; `stage_copy_failure` covers it). The same commit reclaims the state dir when the reserved write at `:111` fails for a fresh VM — before it the launch returned without any rollback and left `vms/<id>/` for `Reconcile` to flag as ambiguous at every startup — and leaves the dir alone on a restart, where it still holds the previous manifest (`manifest_write_fresh_vm`, `manifest_write_restart`). Earlier text of this entry said the stage dir was "reclaimed by `Release` on delete, `stop.go:369-370`" — never true, for the `:440-442` reason — and that the comment at `launch.go:126` had been corrected; the corrected comment said the same false thing. At `:136` it now reads "doRollback removes the state dir and whatever doStage left in the stage dir". Narrowed 2026-09-03: this entry's own "with it gone, `releaseWithoutManifest` calls..." sentence named a function that no longer exists — the chroot-leak fix below folded `releaseWithoutManifest`'s body into `doRelease`, so the netns, TAP, ledger entry and jail chroot it describes are reclaimed by that one function now, manifest present or not.
- 2026-09-02 (M1a final-review, deviation — recorded, not fixed) — `Manager.Reconcile`'s `stopping` case (`internal/runtime/manager.go`, the `case "stopping"` block in `Reconcile`) settles the row at `stopped` with `ReleaseCompute` without observing the VMM, so a stop interrupted mid-flight is recorded as completed and a VMM that survived the controller keeps running behind a `stopped` row. The comment there claimed the VMM was gone "because we're not running"; firecracker is started `--daemonize`d and reparented to init (`internal/privd/vmops.go:129`), so it is not — the comment was corrected in this wave, the behaviour was not. Same root as the two adoption entries above: this portable core has no runtime to ask, and `NewManager` (`internal/runtime/manager.go:129`) runs it before the jailer adapter's own findings are consulted (`cmd/vmobsd/main.go:334`). Waits on real adoption (M2+). Narrowed 2026-09-05 (kata `ynx9`): real adoption landed, so this branch can now tell the two cases apart. A `stopping` row the runtime found alive is an interrupted stop, and `Reconcile` finishes it the way the `deleting` branch finishes a delete — `ForceStop` first, settle on the answer, leave the row at `stopping` when the answer is no, because a released reservation on a live microVM is what lets admission hand the same memory out twice. What remains is the case with no answer available: a `stopping` row with no finding (a portable core, an unobservable host) still settles at `stopped` with its compute released, because there is no host verb worth calling on a VM nobody can see. Three tests pin the split (`internal/runtime/adoption_test.go`) and three mutations die against them.
- 2026-09-02 (M1a close-out, scope gap — recorded, not fixed) — **M1a is done; M1 is not, and the split that made that true is ours, not the spec's.** `docs/SPEC.md:55` puts a web interface in V1, §13 (`:787`) specifies it — 13.1 fleet view, 13.2 VM detail — as a client of the public API, R-02 and R-03 require batch launch and a real-PTY web terminal from it, and `docs/SPEC.md:981` places "the minimal fleet page and one xterm terminal backed by a guest PTY" inside **M1** with the gate "no host shell proxy masquerading as a guest terminal; reconnect does not spawn a duplicate shell." The M1a/M1b split above (Phases table, `L1b (M1)`) moved that work behind the runtime slice for build-order reasons. The consequence, stated plainly so no later reader mistakes M1a's green gate for M1: there is no HTML, CSS or JS in this repository; `/events/stream`, the four terminal routes (`/vms/{id}/terminals` plus `/terminals/{id}`, `/terminals/{id}/stream`, `/terminals/{id}/lease`), the four exec routes, `/vms/{id}/coverage`, `/vms/{id}/filesystem/diff`, `/vms/{id}/exports` and `/artifacts/{id}` are registered stubs (`internal/api/api.go:60-100`, `nil` handler → `stub(r.feature)`); and §981's gate is **unrun**, because there is no terminal to run it against. Nothing about the API surface M1a built forecloses the UI — §13's "client of the public API" holds — but M1 closes only when L1b lands. Narrowed 2026-09-04 (L1b Tasks 1-13, branch `m1-fleet-page`): the "no HTML, CSS or JS" clause is no longer true — `web/` holds a React fleet page and VM detail workspace with an xterm terminal, built to `web/dist/` and served by `serveShell` at `/ui/`. The four terminal routes are built and serve (`internal/api/terminals.go`). Still stubs at this writing: `/events/stream`, the four exec routes, `/vms/{id}/coverage`, `/vms/{id}/filesystem/diff`, `/vms/{id}/exports` and `/artifacts/{id}`. §981's gate stays **unrun** until L1b Task 14 executes on aibox03, so M1 is still open.

- 2026-09-03 (M1a fix wave 2, deviation — recorded, not fixed) — `Manager.Reconcile` can stall daemon startup, silently, once per `deleting` row. Its `case "deleting"` branch calls `m.rt.Release` on the manager's root context, which carries no deadline, and `doRelease` gives itself 30s (`internal/jailer/stop.go`, both the manifest path and the new `releaseWithoutManifest`); `releaseVMWhenDead` can spend that whole budget retrying while privd answers `invalid_state`. Reconcile runs serialized inside `NewManager` (`internal/runtime/manager.go`), before the daemon's listener exists (`cmd/vmobsd/main.go`), so N rows left `deleting` by an interrupted delete cost up to N × 30s of startup with nothing on the API able to say why — the daemon is not answering yet. It cannot fail startup: `releaseOK` swallows the error and the row simply stays `deleting` for the next restart to retry. This wave added host I/O to that path rather than removing it — `releaseWithoutManifest` now asks privd for both halves instead of returning nil on a missing manifest. Accepted, because the alternative is the defect that change fixes: a delete that lies is worse than a slow start. Not bounded here on purpose — a budget is its own task with its own failure modes, since a truncated release has to record what it did not reclaim or it re-creates the same lie somewhere new. Narrowed 2026-09-03: `releaseWithoutManifest` and the manifest-present `doRelease` this entry treats as two paths are one function now, still called `doRelease`, still uncapped at 30s regardless of which case reaches it.

- 2026-09-04 (L1b-D2, deviation — decided, not deferred) — **No terminal tickets.** SPEC §15.1 says "Bind terminal tickets, *if used*, to owner/VM/session and a short lifetime; do not put persistent tokens into query strings or access logs." L1b declines to use them. The browser sends its `HttpOnly` session cookie on a same-origin WebSocket upgrade automatically, and a non-browser client can set an `Authorization` header on the upgrade, so both callers already authenticate without a ticket. Adding one would create a credential class whose only job is to travel in a URL — the exact thing §8.4 and §15.1 warn against. The P5 deferral "WebSocket origin checks + terminal tickets: L0" is therefore answered as: origin checks built (L1b Task 12, `internal/api/terminals.go`), tickets declined with reasons. Cost if wrong: a future non-same-origin client (a separate UI origin, or a CLI that cannot set headers) would need auth on the upgrade and would have to add tickets then, with the short-lifetime binding §15.1 names.
- 2026-09-04 (L1b-T13, deviation — CSP split across two files) — The task's file list named only `web/`, but the content security policy §8.4 asks for cannot live in one place. The body of the policy ships in `web/index.html`'s `<meta http-equiv>`, so a `web/dist/` served by anything other than `vmobsd` keeps it; `frame-ancestors 'none'` ships as a response header from `serveShell` (`internal/api/ui.go`), because a browser ignores `frame-ancestors` in a meta tag and an unframeable shell is what stops a guest terminal being clicked through an invisible iframe. Each half lives in exactly one place — no duplicated policy string. Pinned by `TestUIShellRefusesToBeFramed` (`internal/api/ui_test.go`). Cost if wrong: a reader looking for the whole policy in one file will not find it; the comment in each half names the other.
- 2026-09-04 (L1b-T13, deviation — no lifecycle action row on the VM detail page) — §13.2 lists lifecycle actions on the detail workspace. L1b ships the workspace without them. The delete confirmation dialog is welded inside `web/src/components/BulkActions.tsx`, and a Delete button without its confirm is a button that either does nothing or destroys a VM with no prompt. Extracting the dialog is a real refactor of a component the fleet page depends on, outside this task's footprint. The fleet page keeps every lifecycle action, one click away through the workspace's own back control. Cost if wrong: an operator deep in one VM has to return to the fleet to stop it. Fix is the dialog extraction, then the action row. Closed 2026-09-05 (kata `bn1t`): the confirm dialog is now `DeleteConfirm`, a component `web/src/components/BulkActions.tsx` exports and renders, and `web/src/components/VMActions.tsx` draws §13.2's row from it — start, pause, resume, stop, force stop, delete, each gated by the same `actionLegality` the fleet rows use. The safety property survives the extraction because the prompt and the request stayed one unit: `confirmDelete` is still the only caller of DELETE, so a surface that draws a Delete button without rendering `DeleteConfirm` asks and never sends, which is what two of the mutation proofs pin. Force stop is on the detail row and not the fleet bar (§13.2 names it; §13.1's multi-select text does not), and Export and Clone template from §13.2's sketch are absent because their routes are registered stubs — a control that cannot work is worse than its absence.
- 2026-09-04 (L1b-T14, deviation — input dedupe is per connection, not per session) — SPEC §8.2 describes the input high-water mark as belonging to the session. It does not. `streamRelay` is built once per WebSocket upgrade (`internal/api/terminal_stream.go:97`) and its `lastClientSeq` dies with the connection, so the host's replay guard resets every time a browser reattaches. Two counters are easy to confuse here and only one of them is per session: the sequence stamped on the wire toward the guest is `a.state.inputSeq` (`internal/terminal/transport.go:187`), which lives on the shared session state precisely so a writer steal cannot make the guest swallow the new writer's keystrokes. The host-side dedupe is the per-connection one. Nothing the product ships can exploit the gap: `web/src/terminalSocket.ts` starts its sequence at 0 on every open and never resends across a reconnect, and AT-026 asserts both halves directly — one frame sent twice under one sequence runs its command once, and a reconnect re-runs nothing. A third-party client that did resend its pre-reconnect frames would not be caught by the host. Fixing it means moving the high-water mark onto the session state beside `inputSeq`, which is small; it is recorded rather than done because no reachable client needs it and the change belongs with whatever else touches attach.
- 2026-09-04 (L1b-T14, deviation — AT-028 pause/resume recorded `inconclusive`) — AT-028 asks that a VM's terminal sessions survive nothing and that a new boot gets a new PTY. Pause and resume cannot be part of that evidence in M1a: `Adapter.Pause` and `Adapter.Resume` return a typed `UnavailableError` ("pause not supported in M1a"), a scope decision recorded at M1a Task 10. There is also no `reboot` action — the API's verbs are `start`, `pause`, `resume`, `stop`, `force_stop` — so the gate proves the new-boot half by stopping and starting, which is the same state machine and the same fresh `boot_id`. `inconclusive` is the honest label for the pause half, and the ACCEPTANCE entry carries it rather than a pass inferred from the stop path.
- 2026-09-04 (L1b-T14, defect found and not fixed — start never re-reserves compute) — Task 14 step 7 asked for storage and stop actions through the web UI's own controls, and driving them in a real browser found something the API-driven gate did not. Stopping a VM sets `compute_released = 1` on its reservation row, handing its RAM and vCPU back to admission; starting it again never clears the flag. `internal/store/vms.go` writes that column in exactly two places (lines 579 and 585) and both set it to 1, the only two `INSERT INTO reservations` sites are on the create path (`internal/store/vms.go:402`, `internal/store/batches.go:373`), and the `start` case at `internal/runtime/manager.go:753` transitions the VM and calls `rt.Launch` without touching the row. Host-observed on aibox03 with three VMs running, one of them stopped and started through the UI: `active_vms: 3`, `reserved_memory_mib: 2560` and `reserved_vcpu: 2` (two VMs' worth), `reserved_disk_mib: 9216` (three VMs' worth, because disk is never released). Admission sums those totals, so every stop/start round trip permanently hides one VM's compute from the check that keeps the host from overcommitting. Filed as kata `r799` (P1) rather than fixed here: the fix needs an admission check and a store write inside the `stopped → starting` transaction, with its own tests, and landing it inside the gate task would have changed the binaries the two recorded PASS runs were measured against. No AT asserts capacity accounting across a stop/start, so it is not an M1 gate blocker; `docs/ACCEPTANCE.md` records it beside the browser evidence that found it.

## Session log

- 2026-08-31 (session 1, compactions: 1) — Docs package agent-interface revision landed on `agent-ergonomics` (spec + acceptance + schemas + check.py, 46/46). Then P0+P1 built on `build-foundation` (branched off agent-ergonomics). Next: P2 situation/attention.
- 2026-08-31 (session 2, compactions: 2) — Correction: session 1 logged P1 "done" before its commits existed. Now true: five commits on `build-foundation` land events/store/api/config/daemon/CLI; `scripts/check` fully green (gofmt, vet, golangci-lint via new `.golangci.yml`, go test, docs check); real-usage smoke passed (daemon on loopback, CLI exit codes 0/1/2/3 verified). Branch not pushed — merge is Doctor Biz's call. Next: P2 situation/attention.
- 2026-08-31 (session 2 cont., compactions: 3) — P2 done on `build-foundation` (5 commits): registry kinds attention.raised/queue_overflow + annotation.created; `internal/redact` (policy rp1, applied to annotations and attention summaries before persistence); store attention queue (collapse by class+vm, durable idempotent ack, overflow refuses non-critical with a queue_overflow event, never refuses critical) + annotations (immutable, event+row in one tx) + engine cursors (advance in the raise's tx — crash cannot re-raise); `internal/situation` engine (lazy fold on read, telemetry_degraded only, active set = enabled∩implemented); API /situation (+since, byte-bound sheds head), /attention (+ack with rendered-at-serve ack action), /annotations (author assigned at boundary, client author refused); CLI situation/attention/attention-ack with --json parity. `scripts/check` fully green. Branch not pushed. Next: P3 (VM registry, operations, admission, runtime interface + fake runtime, lifecycle events).
- 2026-08-31 (session 2 cont., compactions: 4) — P3 built via forked subagents, reviewed and history-cleaned twice (two forks committed premature "P3 done" PLAN entries; both unwound — a false done claim in history is a truthfulness violation). Landed: store migrations v3+v4 (vms/operations/reservations, last_event_id change cursor); `internal/store/vms.go` (§5.2 state machine, AT-006 idempotent replay incl. refused-create replay, AT-012 in-tx admission); `internal/runtime` (Runtime iface, honest ForHost unavailable, admission Policy, template registry with load-time digests, Manager with reconcile-on-start, §18 fake for unit tests + scripts/check tripwire); triggers lifecycle_failed/reconciliation_surprise/capacity_exhausted; API /vms CRUD + actions (expected_revision required) + /operations/{id} + /host/status + /templates, situation enriched (real vm counts, capacity_free_mib, changed_vms with real per-VM open-attention counts — P2's three honesty deviations closed); CLI vm/operation/template/host subcommands; `scripts/smoke` (live daemon + CLI, AT-001 honest refusal, exit codes 0/1/2/3) green. `scripts/check` fully green. Branch not pushed. Next: vm-batches (§6.3, AT-014/AT-015), then P3 closes.
- 2026-08-31 (session 2 cont., compactions: 5) — vm-batches (§6.3) built by a forked subagent: store migration v5 (vm_batches + vm_batch_members, UNIQUE partial index for idempotency); `internal/store/batches.go` (CreateVMBatch: atomic_reservation and best_effort modes, AT-012 in-tx cumulative admission, AT-015 idempotent replay, ErrIdempotencyConflict, GetVMBatch); `internal/runtime/batch.go` (Manager.CreateBatch, batchCoord for AT-014); `internal/api/batches.go` (POST /vm-batches, GET /vm-batches/{id}, max_batch_size=50). The fork's transactional core verified sound in review; its "P3 done" claim was premature — defects below.
- 2026-08-31 (session 2 cont., compactions: 6) — Review of the batches fork found 7 defects; all fixed TDD, P3 now honestly done. (1) Batch error mapping split-brained from single-VM (503/422 vs the contract's 501/400): deleted `writeBatchError`, batch handlers use `writeVMError`. (2) `on_failure` did not default to `keep_successful` (SPEC §6.3 names that default). (3) stop_successful wave raced queued/launching members — members queued behind the semaphore launched after the wave, and a wave-stopped member could be revived via the legal stopped→starting edge: added `TransitionInput.From` pins (store-level guard), a stop-wave flag check after semaphore acquisition, and a read-act retry loop in stopSiblings that parks still-provisioning members without a runtime Stop. (4) The wave flipped already-succeeded member ops to failed: added `OperationUpdate.IfState` (guarded update, `ErrOperationStale`, no event on mismatch); all finalizers pin `IfState: "running"`. (5) Batch finalization claimed `succeeded` under stop_successful teardown whenever any member had succeeded: now `failed`/`member_failed` when any member failed under stop_successful. (6) Replay status diverged (batches 200, VMs 201): both return 201 + `is_replay: true`; threading the replay flag through `Manager.CreateVM` also exposed and fixed a real single-VM bug — a replayed create re-enqueued the launch job, which could revive a since-stopped VM. (7) AT-014 had zero tests: added 5 manager batch tests (fake gained `FailCall(method, n, err)` for server-generated VM IDs) + store guard tests + member-owner parametrization (was hardcoded `local_operator` in the members INSERT). `scripts/check` and `scripts/smoke` fully green. Branch not pushed — merge is Doctor Biz's call. Next: P4 (declarative runs + reports) in a fresh session.
- 2026-08-31 (session 3, compactions: 0) — P4 Task 6 (run engine) built TDD on `build-foundation`. Commit 4de3d9d. `scripts/check` fully green. 15 new tests (all from RED→GREEN), 42 P3 tests unchanged. Next: Task 7 (report generation).
- 2026-08-31 (session 3 cont.) — P4 complete. Tasks 7–11 landed: Task 7 required two fix rounds (boot_ids from event data; one vm_id matching semantic closing a P-05 violation), Task 8 required one fix round (result_received_at column, phase validation, terminal guard on report GET), Task 9 zero fix rounds, Task 10 required one fix round (run-flag group completeness rule), Task 11 extended scripts/smoke with run route checks and this close-out. Task 7 mid-flight agent was lost and recovered. All 11 P4 tasks reviewed and clear. P4 commit range on `build-foundation`: 298751c (docs(schemas): allow zero boot_ids) through e3b4eb7 (fix(cli): reject partial run-flag group) — 19 commits total. State: P4 done, `scripts/check` and `scripts/smoke` fully green. Next: P5 (auth, local operator, sessions, CSRF) pending final whole-branch review.
- 2026-08-31 (session 3 cont., compactions: 2) — Final whole-branch review (opus): ready to merge, one blocker. One fix wave landed it: 049dee7 (docs/examples/run-report.json reproduce_queries rewritten to the family=/vm_id=/after=/until= forms generate.go actually emits — the old kind_prefix=/run_id= params were silently ignored, so every reproduce_query returned zero events) and ade3b7f (ConcludeRun with no verdict and no abort now returns ErrConcludeArgsMissing instead of silently concluding with a false "vm reached terminal" reason; wg race + stopVMAfterRun latency documented in gotchas.md; request_hash non-key path commented; ErrRunNotAcceptingSubmissions reworded). Scoped re-review clean. Full P4 branch: aba1b11..ade3b7f, 22 commits. Follow-ups (small, non-blocking): manager wg.Add/Close closing gate, async stopVMAfterRun, HTTP mapping for ErrConcludeArgsMissing. Next: P5 (auth, local operator, sessions, CSRF) in a fresh session.
- 2026-09-01 (P5 session, compactions: 0) — P5 Task 7 (auth boundary) built TDD on `p5-auth`. Commit 2535205. AuthConfig added to api.New; withAuth middleware (session cookie + bearer token + CSRF + origin); POST /auth/login, POST /auth/logout, GET /auth/session; daemon wiring; all existing api.New call sites swept to AuthConfig{Enabled:false}. Config test adapted for require_authentication:false in example config. 7 auth tests all green (7/7 new, 0 pre-existing broken). scripts/check fully green including -race on ./internal/api/. One deviation: auth_login not in /meta Links (POST endpoint can't satisfy the links-answer-200 invariant). Next: P5 Tasks 8–12 (remaining auth endpoints, token management, CSRF, owner scoping).
- 2026-09-01 (P5 Task 13, compactions: 0) — P5 done. scripts/smoke extended with the full auth-on sequence: config patched to require_authentication:true; vmobsd init-auth via -password-stdin; unauthenticated CLI → exit 1; --token whoami → exit 0; all existing VM/run checks run with bearer token; curl cookie-jar login; mutation without CSRF → 403; GET /auth/session → csrf_token; mutation with cookie+CSRF → 201; token revoke; revoked token → exit 1. Both gates green: scripts/check (gofmt, vet, golangci-lint, go test, docs check) and scripts/smoke. Commit range on p5-auth: 26e37d4..HEAD. PLAN.md: P5 marked done, A1–A13 + T7 deviations recorded, auth note updated. gotchas.md: five P5 entries appended. Comment truth fixed in cmd/vmobsd/main.go and docs/examples/host-config.yaml. docs/VALIDATION.md revision 6 appended. Next phase: L0 (Linux track — aibox03, KVM host).

## P5 final-review follow-ups

- 2026-09-01 (P5 final review) — A future batch list endpoint must owner-scope at the SQL query layer (`Query.Owner` pattern like VMs/Runs), never post-fetch filtering — noted by the P5 final review for P6.

## L0 session log

- 2026-09-01 (L0 Task 2, compactions: 0) — Guest channel wire protocol: `internal/guest/proto` package built TDD. 23 tests (all RED→GREEN): frame round-trips, oversize/unknown/truncated rejection, Envelope/Hello/HelloAck/CapabilityManifest encode-decode, WriteControl/ReadControl, and 6 DialHostVsock tests (success, ERR, garbage, closed-conn, ctx-cancelled, no-buffered-bytes-after-OK). All names and constants match task-2-brief.md exactly. Docs: `docs/guest-protocol.md`, `docs/schemas/guest-hello.schema.json`, `docs/schemas/guest-capability.schema.json`. All gates green: scripts/check (gofmt/vet/lint/test/docs) and uv run docs/validation/check.py (46/46). VALIDATION.md revision 8 appended. Next: L0 Task 3.
- 2026-09-01 (L0 Task 1, compactions: 0) — Remote exec bridge, aibox03 setup, root helper, and Firecracker pin. Resolved v1.16.1 from GitHub API; tarball + binaries verified on aibox03 against published SHASUMS. Binary SHA-256s recorded in runtime.lock.json (firecracker: 2fd017..., jailer: 1f3a0c...). min_kernel=5.10 per kernel-policy.md in the v1.16.1 tagged source. jail-stop corrected: pgrep pattern won't match (jailer passes --id=<id> with =, not space); now reads $JAIL_BASE/firecracker/<id>/root/firecracker.pid per jailer docs recommendation. All flags in jail-start verified against jailer --help — no other corrections. All gates green: sh -n, scripts/check (gofmt/vet/lint/test/docs), uv run docs/validation/check.py (46/46), scripts/linux 'go version && go build ./...' (go1.27.0 linux/amd64). Concerns: cgroup-version 2 needed for Ubuntu 24.04 (Task 8 scope); aibox03 kernel 6.8.0 is between Firecracker-validated v6.1 and v6.18 (deviation note at acceptance). Corrected 2026-09-03: the parenthetical above is false — jailer v1.16.1 passes `--id` and the id as two separate argv elements (`.args(["--id", &self.id])` in `src/jailer/src/env.rs`), confirmed against a live `/proc/<pid>/cmdline` sample on aibox03. The change it justified stands on a reason that holds: `pgrep -f <id>` over-matches, because every path under the jail carries the VM id. Left in place and corrected here rather than rewritten, because privd's rollback now identifies its kill target by exactly this argv spelling (`internal/privd/vmops.go`) and a reader who finds only the old claim would undo it. Next: L0 Task 2.
- 2026-09-01 (L0 Task 4, compactions: 0) — Preflight doctor built TDD. 33 new tests (all RED→GREEN). New packages: `internal/preflight` (types, runner, portable checks, linux KVM check, non-linux stub). New files: `cmd/vmobs/doctor.go` (vmobs doctor command). Modified: `internal/api/api.go` (PreflightFunc hook on New), `internal/api/vms.go` (preflight block in /host/status), `internal/runtime/runtime.go` (ForHost PreflightSummary variadic hook, L0-R12), `cmd/vmobsd/main.go` (preflight runner wired at startup), `cmd/vmobs/main.go` (doctor dispatch). Linux verification (aibox03): TestArchKVMHonest PASS — detected EACCES (harper not in kvm group yet), test accepted the honest fail with re-login remediation. cgroup_v2 and net_prereqs PASS. All portable tests PASS. scripts/check fully green. Next: L0 Task 5 (guestd core).
- 2026-09-01 (L0 Task 5, compactions: 0) — vmobs-guestd core built TDD. 18 new tests (all RED→GREEN): handshake accept/reject (wrong token, version mismatch), listener survives rejection, oversized frame closes conn, capabilities round-trip carries manifest, ping→pong, bootcfg valid/missing-field/version-mismatch, probe honesty on darwin and linux. New packages: `internal/guest` (agent.go, bootcfg.go, bootcfg_linux.go, probe.go, probe_linux.go, probe_other.go). New binary: `cmd/vmobs-guestd` (main.go linux, main_other.go stub exits 2). Added: `github.com/mdlayher/vsock v1.3.0`. Local gate: scripts/check fully green. Cross-compile: `env GOOS=linux GOARCH=amd64 go build ./...` clean. Remote (aibox03): `go build ./cmd/vmobs-guestd && go vet ./... && go test ./...` all green; TestProbeCapabilitiesLinuxHonest PASS (btf/cgroup_v2/vsock Present values match independent stat). Next: L0 Task 6 (guest image bake).
- 2026-09-01 (L0 Task 6, compactions: 0) — Guest kernel + Ubuntu rootfs pipeline landed. Linux 6.1.186 vmlinux (549M) built on aibox03 inside pinned ubuntu@sha256:33ceb71... container; all 27 fragment symbols verified =y post-olddefconfig. Three symbols excluded from fragment with documented reasons: CONFIG_DEVPTS_FS (not a Kconfig symbol in mainline 6.1), CONFIG_TRACEPOINTS (prompt-less bool, select-only), CONFIG_MODULES + CONFIG_DEBUG_INFO_DWARF_TOOLCHAIN_DEFAULT ADDED as deps for CONFIG_BPF_JIT/CONFIG_KPROBES/CONFIG_DEBUG_INFO_BTF. Ubuntu 24.04 rootfs (268M/1G) with vmobs-guestd installed via apt snapshot 20260820T000000Z. All hashes in runtime.lock.json (vmlinux: b606768..., rootfs: d8f8e20...). VerifyArtifacts proof: both hashes match. go test ./internal/lock/ 12/12 green. scripts/check all gates green. scripts/linux gains --filter='P images/dist/' protect rule (openrsync's :- .gitignore doesn't prevent --delete on destination). Commit: bf7a4e1. Next: L0 Task 7 (private-network baseline).
- 2026-09-01 (L0 Task 7, compactions: 0) — Private-network baseline. New packages: `internal/network` (alloc.go, names.go) with 10 tests (all RED→GREEN). Route fixture: real captured output from aibox03 `ip -json route` (main table: default, 172.17.0.0/16 docker, 192.168.10.0/24 LAN, 192.168.10.1 host route). ParseIPRoutes handles bare host addresses (no /prefix suffix) as /32. Default-route (0.0.0.0/0) skipped during overlap checks (see deviation). Integration test: `tests/integration/netns_test.go` (linux build tag, VMOBS_FIXTURE=1 env gate, skips with clear message when helper absent). Remote verified: internal/network 10/10 green; integration test skips honestly. All gates green: scripts/check (gofmt/vet/lint/test/docs), GOOS=linux cross-compile clean. Next: L0 Task 8.
- 2026-09-01 (L0 Task 8, compactions: 0) — Jailed two-VM boot fixture (M0 gate). New: `tests/integration/fixture/fixture.go` (//go:build linux; BuildVMConfig core, PrepareVM, Start, Stop, DialControl), `tests/integration/fixture/fixture_core_test.go` (8 unit tests: UUID, token, FCConfig JSON shape, BootConfig schema, CID/UID derivation, BuildVMConfig end-to-end — all RED→GREEN on aibox03, TestBuildVMConfig runs mkfs.ext4 with real images), `tests/integration/boot_test.go` (TestM0Boot: four-assertion gate — vsock handshake, identity isolation, capability report, disk ownership, teardown, evidence capture), `tests/integration/README.md` (how to run, env vars, evidence location). fc-config.json field names validated field-by-field against firecracker_spec-v1.16.1.yaml — all match. Skip gate: VMOBS_FIXTURE unset → skip; set but helper/srv/FC absent → skip naming scripts/aibox03/setup.sh. VMOBS_FIXTURE=1 remote verified: honest skip with setup.sh-naming reason. Gate run is DEFERRED pending scripts/aibox03/setup.sh (one-time root setup Doctor Biz owns). All existing tests unchanged. scripts/check fully green. Next: deferred gate run after setup.sh.
- 2026-09-01 (L0 Task 9 — close-out, compactions: 4) — L0 Tasks 1–9 executed via SDD this session; session ran through 4 context compactions under the standing continue ruling. Acceptance ledger updated (AT-001 TESTED_PASS for M0 scope, AT-002 TESTED_PASS, AT-003/AT-004/AT-009/AT-011/AT-018 SPECIFIED with honest notes). Deviations log: L0-R1..R12 recorded + T5/T6/T8 session distillations. PLAN.md phase table, host section, and session log updated. VALIDATION.md revision 9 appended. gotchas.md: six new entries (scripts/linux rsync+P-filter, sudo boundary on aibox03, mkfs.ext4 -d rootless, fixture gate env, ip route table-all, jailer pid file). All gates green: scripts/check, scripts/linux 'go test ./...', uv run docs/validation/check.py. Next steps: (1) Doctor Biz runs setup.sh + re-login on aibox03; (2) gate run + evidence commit + AT-004 flip; (3) final whole-branch review is in flight now.
- 2026-09-01 (M0 gate green, compactions: 1) — The deferred gate ran and passed: `TestM0Boot` PASS 7.5s (twice), `TestNetNSSetupTeardown` PASS. Six runs total to green; four defects found and fixed on `m0-gate-fixes` (L0-R13: helper chmod race + error masking; L0-R14: guestd mountpoint ENOENT + rootfs build quoting regression), plus the evidence writer's post-teardown stat replaced with live-capture. Rootfs rebuilt twice (husk detected by debugfs inspection, then good build 54426c1c…), runtime.lock.json re-pinned. Evidence committed: tests/integration/evidence/m0-boot-aibox03.txt (handshake + cross-auth rejection, capability manifests both VMs, live ownership stats uid 20000/20001 distinct inodes, teardown proof). AT-004 → TESTED_PASS; AT-009/AT-011/AT-018 notes flipped to "seed evidence executed". gotchas.md: four entries (tmpfs mountpoint, docker-script quoting, evidence capture timing, debugfs journal clobber). Debug technique of record: `screen`-driven `init=/bin/sh` console + debugfs journal/state inspection. Next: scoped review of the m0-gate-fixes diff, then merge decision (Doctor Biz).

## M1 session log

- 2026-09-01 (M1a planning, compactions: 2) — M0 close-out finished earlier this session: scoped review APPROVED, minors fixed (1fde7aa), Doctor Biz approved the merge, main fast-forwarded, m0-gate-fixes deleted. M1 then scoped and split: L1a (real runtime slice) + L1b (terminal + fleet page, planned after L1a lands). L1a plan written from a two-agent recon (SPEC digest + code map): docs/superpowers/plans/2026-09-01-m1a-real-runtime.md — 14 TDD tasks (privd wire proto → server/ledger → net verbs → VM verbs w/ fd-pinned digest staging → daemon+setup.sh HANDOFF → spool segment format → importer+kinds → runner → guestd shutdown+rootfs re-pin → launch transaction → stop/doctor/reconcile → vmobsd wiring → AT-005 injection → aibox03 gate). Plan fixes decisions D1–D9 (privd topology, runner-supervises-by-identity, spool scope, no CreateCgroup verb, fixture untouched, shared jail gid kept, workspace-disk-only seeding, tmpfs ledger, kernel 6.1.186 EOL accepted through M1a). Known handoff inside execution: Task 5 needs Doctor Biz to run setup.sh (installs vmobs-privd unit). Session at 2 compactions + a landed planning phase → retiring per lifecycle rule; L1a execution starts in a fresh session via subagent-driven-development.
- 2026-09-01 (M1a Task 4, compactions: 0) — Task 4 (privd VM verbs) built TDD on m1a-real-runtime. Commit 2087b16. New: backenderr.go (BackendError typed error, portable), vmops.go (VerifyStagedFile with O_NOFOLLOW+SHA-256; JailerArgv pure builder; CheckSignalIdentity host probe; StartVM copy+exec+pid; SignalVM identity-gated; ReleaseVM RemoveAll-only per R4), vmops_linux_test.go (TestStagedFileVerification, TestJailerArgv, TestSignalIdentityGate, TestWireBackendErrorCause). Modified: netops.go (log field, stubs removed), server.go (backendErrResp, handler updates), server_linux_test.go (signalErr field). All 22 linux tests PASS, 1 SKIP (root-only), scripts/check and GOOS=linux vet clean. Next: Task 5 (daemon wiring + setup.sh) — needs Doctor Biz to run setup.sh on aibox03.
- 2026-09-01 (M1a Task 7, compactions: 0) — Task 7 (spool importer + kind registrations) built TDD on m1a-real-runtime. Commit 96d8b07. New: internal/spool/importer.go (NewImporter, Run, ImportOnce, cursor, writeCursor, gapSourceInstanceID, segmentHasEndMarker, countSegmentRecords); internal/spool/importer_test.go (9 tests, all RED→GREEN). Modified: internal/events/registry.go (4 new kinds: guest.channel_established, guest.channel_lost, vm.vmm_exited, spool.recovery_gap). One deviation: gap envelope's SourceInstanceID patched to a stable UUID (SHA-256 of vmDir) because buildGapEnvelope uses "spool.recovery" which fails Validate(); patched at trusted ingress, dedup preserved. Run(ctx) drops per-cycle errors, returns ctx.Err(). All gates green: scripts/check. Next: Tasks 5–6 (already landed; Task 8 runner is next unbuilt task).
- 2026-09-01 (M1a Task 8, compactions: 0) — Task 8 (vmobs-runner) built TDD on m1a-real-runtime. Commits c9399f5 + e4ec697. New: internal/runner/{runner.go,state.go,ctl.go,flags.go,vmm_watch_linux.go,vmm_watch_other.go,umask_unix.go}; cmd/vmobs-runner/main.go; 12 portable tests + 1 linux integration test (real guest.Agent, real sleep child, VMOBS_RUNNER_TEST=1 gate). Modified: internal/guest/proto/messages.go (KindShutdown, KindShutdownAck, Shutdown type). Linux test PASS in 1.11s on aibox03. All gates green: scripts/check, GOOS=linux vet. Known limitation: spool append failure while supervising logs to stderr and continues — no overflow-health-record path this milestone. Next: Task 9 (guestd shutdown handler + rootfs re-pin).
- 2026-09-01 (M1a Task 9, compactions: 0) — Task 9 (guestd shutdown + rootfs rebuild) built TDD on m1a-real-runtime. Commits 7aefb9f + a6c554d. Modified: internal/guest/agent.go (PoweroffFunc exported field, default exec("systemctl poweroff"); KindShutdown case in serveRequests — ack first, poweroff second); internal/guest/agent_test.go (3 new tests: TestShutdownAfterHello, TestShutdownBeforeHello, TestReconnectAfterClose — all RED→GREEN). All 11 guest tests + 13 runner tests PASS on aibox03. Rootfs rebuilt on aibox03 (guestd 4941539 bytes verified via debugfs); new rootfs sha256 5381506c… pinned in runtime.lock.json. M0 boot fixture regression TestM0Boot PASS 7.52s. D9 deviation: guest kernel 6.1.186 stays pinned through M1a (EOL 2026-09-02); migration is pre-M2 task. Next: Task 10 (launch transaction).
- 2026-09-01 (M1a Task 10, compactions: 0) — Task 10 (launch transaction) built TDD on m1a-real-runtime. Commit 6a3092f. New: internal/jailer/{adapter.go,launch.go,launch_other.go,manifest.go} + tests. privdClient interface wraps *privd.Client (AT-005 seam). Adapter satisfies runtime.Runtime; Pause/Resume return typed M1a errors; Stop/ForceStop/Reconcile stub as Task 11 errors. Launch: 7-step §5.3 transaction with manifest-tracked staging (lock-verified artifacts, per-boot token 0600, config.ext4 via mkfs.ext4 -d, workspace.ext4, fc-config.json), AllocateNetwork, StartVM, runner spawn (Setsid), 60s attach-wait polling runner-state.json, ordered rollback. 5 portable tests + 2 linux integration tests (real privd server + recording backend, real runner binary built by TestMain, real guestd agent on vsock UDS). All 7 PASS on aibox03 (1.03s). scripts/check all gates pass. Interface drift from brief: none. Next: Task 11 (Stop/ForceStop/Reconcile/doctor).
- 2026-09-01 (M1a Task 11, compactions: 1) — Task 11 (stop/release/reconcile/doctor) built TDD on m1a-real-runtime. Two commits: 0617b3c (R1 controller ruling: runtime.Release interface + Manager.Delete integration + fake + 4 manager tests) and 8362fbd (Task 11 proper: stop.go + stop_other.go + stop_linux_test.go + preflight real guest_channel check + api test fix). Stop/ForceStop: shutdown_guest ctl → SIGTERM → SIGKILL escalation, launchMu guards concurrent Launch. Release: ReleaseNetwork + remove stage dir + vms state dir (spool stays). Reconcile: scans vms dirs, classifies each VM as adopted/vmm_gone/ambiguous per §5.5. Availability (AT-001): first failing preflight check surfaces as UnavailableError. Preflight guest_channel: real Linux check dials privd socket (1s timeout) + stats stage root via unix.Access; non-Linux returns not_implemented. Linux fix: API test TestHostStatusPreflightGuestChannelNotImplemented renamed to TestHostStatusPreflightGuestChannelPresent — accepts either not_implemented or fail (both honest). 11 new jailer linux tests + 3 new preflight linux tests + 4 new manager tests — all PASS on aibox03 and macOS. scripts/check all gates pass. Deviation: Release leaves spool dir (brief says so; importer prunes independently). Next: Task 12 (vmobsd wiring).
- 2026-09-01 (M1a Task 12, compactions: 2) — Task 12 (vmobsd wiring) built TDD on m1a-real-runtime. Commit bc33d1d. New: cmd/vmobsd/runtime_linux.go (buildFirecrackerRuntime: resolves runner beside daemon, ip-json routes → network.NewAllocator, jailer.New; maxSlotsWiring=64), cmd/vmobsd/runtime_stub.go (!linux build error). Modified: internal/config/config.go (Runtime.Mode, JailUIDBase, JailGID, CIDBase fields + defaults + validation); internal/runtime/manager.go (NotifyVMMExit: §5.2-compliant stopping→stopped/failed, no-op on terminal); internal/spool/importer.go (onImported func(*events.Envelope) callback, nil-safe); cmd/vmobsd/main.go (runtime mode switch, adapter.Reconcile→findings, NotifyVMMExit mapping, importer goroutine). 4 config tests + 3 manager tests (all RED→GREEN); 13 importer call sites updated (nil callback). Both gates green: scripts/check + scripts/linux 'go test ./...'. Concern (medium): NewManager.Reconcile moves running VMs to failed before adapter adoption is visible — store-level state recovery is M2 scope. Path derivations (no new config fields): StageRoot=Runtime+"/stage", JailBase=Runtime+"/jail", SpoolRoot=State+"/spool", RepoImagesDir=State+"/images/dist".
- 2026-09-01 (M1a Fix round 1, compactions: 0) — Review fixes on m1a-real-runtime. Commit 95714c1. C-1: renamed jailer.Config.RepoImagesDir→RepoRoot; doStage copy sources now derived from lock fields (lk.GuestKernel.VmlinuxPath, lk.RootImage.Path) joined to RepoRoot — lock is single source of path truth. Runtime wiring: RepoRoot=cfg.Paths.State (lock "images/dist/..." resolves correctly). Deleted dead double-filepath.Dir assignment. Test fixture updated: files at images/dist/, lock paths "images/dist/...", Config uses RepoRoot=dir. I-1: NotifyVMMExit provisioning/starting routes to failed regardless of graceful (§5.2 matrix; VMM never ran). New test TestNotifyVMMExitProvisioningGracefulGoesToFailed RED→GREEN. I-3: ip error hints at PATH. All 7 files changed. Both gates green: scripts/check + scripts/linux 'go test ./...'.
- 2026-09-01 (M1a Task 13, compactions: 0) — Task 13 (AT-005 injection suite) built TDD on m1a-real-runtime. Commit 6747f7c. New: internal/jailer/inject_linux_test.go (TestInject: 6 subtests). Modified: adapter.go (Config.AttachTimeout + time import), launch.go (apply AttachTimeout at attach-wait, zero=60s default), export_linux_test.go (PrivdClient alias). No product bugs found — Tasks 10–11 rollback transaction was correct first time. All 6 subtests PASS on aibox03 (14s); scripts/check and full scripts/linux 'go test ./...' all-green. AT-005 evidence complete. Hardened in `11864b9` (exact verb sequences, runner-gone check, root guard): at that commit the suite passed 6/6 on aibox03 in 14.5s. After the wave-A review fixes (`e6172f8`) it was re-run on aibox03 with `-race -run TestInject -count=5`: `ok internal/jailer 88.593s`. Both runs were recorded in the SDD ledger, which is deleted at close-out; this entry is their record now.
- 2026-09-02 (M1a gate green, close-out, compactions: 0) — L1a executed via SDD across the M1a task sequence (Tasks 1–13: privd wire protocol through the AT-005 injection suite) on `m1a-real-runtime`; Task 14 is this gate run and close-out. The live gate (`TestM1aGate`, `tests/integration/m1a_gate_test.go`) ran nine times on aibox03 before it had the two consecutive PASS runs it needed — six blocked runs (the first at `6b4971d`, which launched no VM; the sixth at `569f80d`, 7/8), then a diagnostic full pass at `0881e69` (93.62s, 8/8, zero skips) that was excluded from the count by ruling because product code changed after it (`e6172f8`, `702905e`, `54d571b`), then run A started 2026-09-02T23:25:14Z (92.40s) and run B started 2026-09-02T23:27:03Z (98.44s), both at commit `aa12929`, eight subtests each, eight passes, zero skips. Evidence committed at `tests/integration/evidence/m1a-gate-aibox03.txt` (run B's output; the two runs' evidence is byte-identical after normalizing UUIDs/timestamps/pid/temp-dir).
  The gate found real product bugs the unit suite had missed; the ones told here each survived for a different reason. This is a narrative, not a count — several fixes on the branch came from smoke runs, from reading, and from review rather than from a gate run, and the commit log between `11864b9` (the last commit before the live work) and `2f14d05` (the evidence commit) is the full record. Lock artifact paths resolved against the wrong root, so every launch died with a false "hash mismatch" (`81c083e`) — unit tests always ran with matching fixture paths, so the mismatch never existed in them. Five precondition checks returned untyped errors that fell through to a 500 instead of 409 (`cff63e3`) — unit tests asserted on status codes the fake runtime always got right, never on the real precondition-violation path. A revision pin was spent twice across two transitions inside one action, so every real stop, force-stop, or force-delete over HTTP got a false 409 (`0f2bb62`, `28577d4`) — unit tests exercised the manager methods with a nil pin, never a real caller-supplied one. The spool importer's `NotifyVMMExit` raced a stop's own terminal write on the `stopping` state, turning a stop that genuinely worked into a recorded failure (`426955b`) — needs two real concurrent processes racing against real files to happen at all. `release_vm` never retried while privd still saw the VMM alive, leaking a 1.7 GB jail chroot and an immortal network-ledger entry (`7934423`) — needs the real async gap between a real SIGKILL and privd's own liveness check. The daemon's preflight wiring omitted the privd socket and stage root, so every real launch was refused with 501 (`7af0a59`) — unit tests constructed `preflight.Config` directly, never through the daemon's own wiring path. Not told above, because the gate did not find them: the smoke interlude before any gate run turned up three launch blockers — the first live smoke over the real privd socket found veth names over IFNAMSIZ for UUID VM IDs (`29ad9b8`) and a failed `allocate_network` leaving a netns behind with no ledger entry (`ea4c51e`), and reading privd's exec path during that interlude found its umask leaving the jailer's v.sock group-unwritable (`359bb0d`); the AT-001 fix round found an unset runtime root deriving relative stage and jail roots, which let the doctor skip its not_configured branch (`0175975`); run 5 traced three of its four failures to lifecycle mutations running on the HTTP request context and dying with the client that asked for them (`18c6d03`); and a start that ran on the stop path's budget could strand a VM in `starting` forever (`6151213`).
  The seventh, and the milestone's headline finding, is not softened here: **Firecracker v1.16.1 never advertises ACPI S5 to this guest.** `systemctl poweroff` ran a complete, clean shutdown and then halted forever with the vCPU parked in HLT — the VMM never exited, at any grace period. No unit test could have found this; it needs a real Firecracker VMM and a real guest kernel. Two smaller stop-path bugs (a constant 30s ctl deadline making the graceful path unreachable at the documented default grace, `43d6f50`; the runner's typed refusal reason silently dropped, `9e793a4`) were real and necessary fixes, but a sixth run proved them insufficient — with both already fixed, all twelve stops in that run still went forced, through a third mechanism (the guest control channel dropped to `"degraded"`, the runner stuck in redial-backoff). The actual fix was guest-initiated: `reboot=k` was already in the boot args, so a guest exec of `systemctl reboot`, not `poweroff`, writes the i8042 reset byte directly and Firecracker exits on it — shipped as `defaultPoweroff` (commit `c21234b`, recorded as `M1a-T9-exit-door` in the deviations log above), which required a rootfs rebuild and a `runtime.lock.json` repin (`0881e69`).
  An earlier analysis in this same investigation misread a fixed timer as a guest measurement: a "guest takes 15.1-16.0s to gracefully power off" reading (which raised `stop_grace_seconds` from 10 to 30 in the gate, commit `0310fe0`) was actually `doStop`'s own poll-timeout window expiring on schedule, not the guest's own timing — indistinguishable from the real thing because the guest console is unrecoverable in production. That misreading cost real time and belongs in the record, not just the correction.
- 2026-09-02 (M1a close-out review, shutdown budget) — Separately from the gate: a close-out review, not a gate run, found the daemon's graceful-shutdown budget was 5s against a `net/http` StateNew reclaim that can land ~6.5s after a connection parks, so an ordinary SIGTERM with one idle pooled connection returned `context deadline exceeded`. Fixed to 10s (`54d571b`, `cmd/vmobsd/main.go:446`). Its only evidence is the unit test `TestShutdownOutlastsParkedConnection` (`cmd/vmobsd/tls_test.go:262`): the live gate tears its daemon down with SIGKILL (`exec.CommandContext` with the default `Cancel`, `tests/integration/m1a_gate_test.go:375`, `:473-477`), so neither run A nor run B exercised the shutdown path, and nothing above should be read as if they had.
- 2026-09-02 (M1a close-out, rollback leak fix, compactions: 1) — Two launch-rollback leaks fixed TDD on m1a-real-runtime, commit `02ea171`; the docs travel in the commit that carries this entry. N-1: `doRollback` guarded the stage-dir removal on the `staged` record, which `launch` writes only after `doStage` returns, so every `doStage` failure orphaned the stage dir and then deleted the manifest `Release` would have needed to find it (`doRelease` returns nil without one, `stop.go:346-347`). The guard is gone (`launch.go:513-517`), matching `doRelease` (`stop.go:368`); nothing reads the stage dir after privd copies it into the jail at `start_vm`, and `launchMu` serializes launch, stop and release, so no path needs a stage dir to outlive a rollback. N-2: a failed reserved manifest write (`launch.go:111`) returned without any rollback and left `vms/<id>/` behind; the launch now removes it — for a fresh VM only. On a restart the dir holds the stopped VM's manifest (`writeManifest` is temp-and-rename, so a failed write leaves it intact) and `Release` still needs it to free the network; removing it unconditionally, as first proposed, would trade one leak for a worse one, and `manifest_write_restart` fails under that variant. The `manifest_write_failure` subtest never injected where its name said: it chmodded `vms/` 0555 and hit the `MkdirAll` at `:107`; it is now `state_dir_mkdir_failure`, pinned to that error. Injecting at `:111` re-executes the test binary as a child with `RLIMIT_FSIZE=0`, so the first `Write` in `writeManifest` fails with EFBIG and nothing else is touched (an in-process rlimit would also trip the testing package's own log writes; a chmod would also block the unlink the fix needs). Nine subtests. Mutation proof — each new assertion failing under a deliberate regression, quoted from the container runs: `manifest_write_fresh_vm` against BASE `6c9556e`, `inject_linux_test.go:649: state dir still present after rollback (err: <nil>)`; `stage_copy_failure` against BASE, `:795: stage dir still present after rollback (err: <nil>)`; `manifest_write_restart` under the unconditional removal, `:702: previous manifest gone after the failed restart: open .../state/vms/vm-inject-8/manifest.json: no such file or directory` and `:720: recovery did not reuse the stopped VM's identity: slot 0 cidr "10.117.0.0/30"; want slot 0 cidr "192.0.2.0/30"`; the `state_dir_mkdir_failure` pin, with the mkdir error return dropped, `:625: error = "launch vm-inject-1 failed at stage reserved: write manifest: jailer: mkdir manifest dir: ...: permission denied"; want it to contain "mkdir state dir"`; the two harness-side drifts — `manifest_write_fresh_vm` back on the chmod injection, `:656` in the mutated file (`:652` as shipped), `error = "launch vm-inject-7 failed at stage reserved: mkdir state dir: ...: permission denied"; want it to contain "write manifest"`, and `stage_copy_failure` planting at vmlinux, `:798: error = "launch vm-inject-9 failed at stage staged: copy vmlinux: ...: is a directory"; want it to contain "copy rootfs"`; and a privd verb ahead of the first write, `:658: expected zero backend calls; got 1 (allocate: [vm-inject-7])`, `:709: expected zero backend calls; got 1 (allocate: [vm-inject-8])`, `:803: expected zero backend calls; got: [allocate_network]`. Runs: the injection suite is linux-only and aibox03 was off limits this round, so it ran in a linux/arm64 Docker container on the darwin workstation (go1.26.8, uid 1000, no KVM): BASE 6/6; the new tests against BASE failing as quoted; 9/9 in 19.95s at `02ea171`. The whole module, `go test -count=1 ./...` in that container, passed except one `internal/jailer` run in which `TestReconcileAdoptsHealthyVM` failed at TempDir cleanup, `unlinkat .../state/vms/vm-stop-test-b1c6362e: directory not empty`: the stop-test harness kills the fake VMM in cleanup but never the runner, which rewrites `runner-state.json` in the state dir while `t.TempDir` removes it. It passed 3/3 in isolation and on a whole-package rerun, its launch succeeds and never enters the changed paths, and it is a pre-existing race — flagged here, not fixed. On darwin, `scripts/check` passed (the jailer's linux-only files do not build there) and `GOOS=linux GOARCH=amd64 go vet ./...` and golangci-lint with a fresh cache were clean. Not run: anything on real KVM or amd64. Found and recorded, not fixed: the restart escape in the deviation entry above.
- 2026-09-02 (M1a merged to `main`, close-out) — `main` fast-forwarded to `be48165` in two steps on Doctor Biz's instruction: `0a5e994..6c9556e` (132 commits) and then `6c9556e..be48165`. Both were strict fast-forwards done with `git push . m1a-real-runtime:main` rather than a checkout-and-merge, because a subagent held the working tree dirty at the time and `main` was an ancestor in both cases. The first of the two knowingly shipped the launch-rollback stage-dir leak — it was already understood and recorded when that merge ran; `be48165` carries its fix (`02ea171`) and the AT-005 rewrite that describes the shipped behaviour. Gate state at merge: `scripts/check` exit 0, docs validation 46/46, and the live `TestM1aGate` passing twice consecutively on aibox03 with committed evidence. Host confirmed clean afterwards: privd active, zero firecracker processes, no residual netns, jail or stage entries. The whole-branch review had not run at merge time and is a follow-up, not a gate that was skipped quietly — its outcome gets its own entry below. One commit sits past `main` on the branch: `cdc8832`, the web-interface scope-gap deviation recorded above.

- 2026-09-04 (L1b planning + the fleet page, compactions: 2) — This session started from a fair question: after M1a, the only way to see the product was `curl`. §18 puts "the minimal fleet page and one xterm terminal backed by a guest PTY" inside the M1 gate, so the gap was real and recorded as a scope deviation (`cdc8832`), not argued away. Six commits on `m1-fleet-page` answer the first half: a read-only React fleet page embedded in the daemon and served at `/ui/` (`8e74d23`), a script that puts a real daemon and three live VMs on aibox03 so a human can look at it (`0d2b9a1`), and two bugs that only showed up because someone finally looked — `desired_state` was never written on a lifecycle action, so the page could not distinguish "stopped because you asked" from "stopped because it died" (`5ba1807`), and a failed resource release reported itself as a storage error (`8dfe30c`). A third, in the demo script itself: a no-match `grep` inside a command substitution under `set -euo pipefail` killed the teardown before it stopped the daemon, silently (`30ae18d`). All three are in `gotchas.md` (`d94ba4d`).
  The L1b plan (`34a56a5`, `docs/superpowers/plans/2026-09-04-l1b-web-and-terminal.md`) covers the rest of M1 in 14 TDD tasks across two phases: Phase A finishes §13.1 on APIs that already work (launch form, batch launch, multi-select lifecycle actions, recent operations, URL-persisted state), Phase B builds §8.1's chain end to end. Ten decisions are fixed in the plan with their reasoning; the four that change something a reader might otherwise assume: **no terminal tickets** — §15.1 says "if used", a same-origin upgrade already carries the `HttpOnly` session cookie, and a ticket would exist only to ride in a query string, which §8.4 and §15.1 both forbid, so `PLAN.md`'s P5 deferral of "terminal tickets" is answered by declining them rather than by building them; **the upgrade needs an exact `Origin`, and an absent one is a denial** — today's check (`internal/api/auth.go:129`) is `origin != "" && origin != PublicOrigin` and runs only for mutating methods, so a WebSocket upgrade, being a `GET`, currently gets no origin check at all; **PTY bytes never enter the event store**, only `terminal.*` lifecycle and byte counts; and **batch UI is M4 text pulled forward on Doctor Biz's direction**, named as the one cuttable piece if the M1 gate is at risk.
  Two planning facts were measured rather than assumed. The guest image was read directly (`images/dist/rootfs.inventory.txt` plus the pinned base in docker): `bash`, `procps` (so `/usr/bin/top`) and `ncurses-base` (so `xterm-256color` terminfo) are already installed; `vim`, `vim-tiny`, `nano`, `busybox` and `less` are all absent. §8.2 and AT-020 require `vi` **installed in the image**, so exactly one package and one rootfs rebuild stand between the plan and that assertion — a bounded task, not a vague risk. And the `golang.org/x/sys/unix` PTY symbols were verified with `go doc` per symbol, with a deliberately fake symbol name queried alongside to prove the check could fail.
  Two recon subagents reported after the plan's first draft and contradicted two of its assumptions; both were fixed before the commit. The guest control loop is strict request/response — `ReadControl` rejects any non-`FrameControl` frame and nothing is ever pushed — so the draft's unsolicited `terminal.exited` had nowhere to go; exit now travels on the attached session's own port-10002 connection, and a detached session's exit surfaces on the next `terminal.list`. And `runner.sock` is JSON one-shot request/reply, so `terminal-attach` cannot be another verb of that shape: it replies once and then the same connection becomes a frame stream. Both reports also confirmed something that de-risks Phase B — Firecracker's vsock device multiplexes ports over one CID via `CONNECT`, so port 10002 needs no change to `internal/jailer/launch.go` or the machine config, and the guest kernel already carries `CONFIG_UNIX98_PTYS=y`.
  Left open for Doctor Biz: the merge decision on `m1-fleet-page` (7 commits, `scripts/check` green). Left running on aibox03: `scripts/aibox03/demo` with three VMs and an SSH tunnel from the workstation — `scripts/aibox03/demo stop` takes it down, and it should come down before the next live gate, which needs about 21 GB free.

- 2026-09-04 (M1b gate green, close-out, compactions: 29 on 2026-09-04, 53 across the whole session file) — L1b Tasks 7–14 landed on `m1-fleet-page`: the guest PTY broker with a controlling terminal and a bounded reconnect ring (`7a74f93`), `vim-tiny` in the rootfs and the runtime lock re-pinned to it (`a5b790c`, `e70224f`), the runner's vsock-10002 relay (`872fb63`), guestd serving session byte streams on that port (`8868cb2`), the session registry with its writer lease and in-flight window (`8f322b4`), the create/list/close/lease routes (`e5cbd52`), the authenticated origin-checked WebSocket relay (`d2f29f8`), and the xterm terminal with the VM detail workspace (`42ea4e9`). Deviations D2, the CSP split and the missing detail action row were recorded at `f3251f7`; the two above join them.
  The M1 gate (`tests/integration/m1b_gate_test.go`, `50f816f`) took five runs on aibox03 to go green, and what it found is the point of having built it. Two of the six fixes are product defects the unit suite could not have caught. The terminal event stream was created once for the whole API, but `internal/store/append.go` binds a source stream to exactly one VM scope, so every VM after the first had its `session_opened` and `session_closed` refused at ingest and surviving only as a log line — evidence loss under §8, invisible because the existing test opened one VM (`114a03e`). And a `vm.vmm_exited` crossed the spool boot-blind: the importer polls every 5s, `Manager.NotifyVMMExit` could not tell which boot a notice described, and a stop followed by a start inside one poll interval let boot 1's exit put boot 2 into `failed` while its firecracker was still running — a live VM that then refused both `start` (409 `invalid_transition`) and dispose (500, "vm process is still alive"). Fixed by carrying `boot_id` through the importer and adding `vms.current_boot_id` (store migration v7) so a notice naming a boot that is not the VM's current one is a no-op (`320cfe1`). A third fix predates the gate runs: preflight called a loopback binding unsafe even with authentication required, which it is not (`d1832d6`).
  The other four were the gate's own harness lying about the guest. A per-connection sentinel counter collided with the scrollback a reattach replays, so a wait returned on replayed text and never saw the command it was waiting for; one process-wide counter fixed it. Ctrl-D was sent mid-line, where canonical mode only flushes, so `cat > file` never closed and swallowed the next command; a `\r` before the `0x04` fixed it. PTY echo counted a marker twice, fixed with the empty-quote-pair trick the M1a gate already used (all three at `a3455f1`). The last one cost two runs to see: `vi` and `top` both painted, quit, restored the terminal and left the prompt back on screen, and the probe typed behind the quit key was never echoed — a terminal restore flushes the pty's pending input queue and takes the probe with it. The probe is now retyped until answered (`37d2e1b`).
  Two consecutive PASS runs back the evidence: `TestM1bGate` 12/12 subtests, zero skips, 53.05s at 2026-09-04T19:52:56Z and 53.21s at 19:54:30Z, both `scripts/linux 'env -u GOROOT VMOBS_FIXTURE=1 go test ./tests/integration/ -run TestM1bGate -v -count=1 -timeout 25m'`. Normalized for UUIDs, timestamps and pids, the two transcripts differ in one field. Run A is committed at `tests/integration/evidence/m1b-gate-aibox03.txt`; a §15.3 scan of its 90 lines returns two matches, both verified benign. AT-019..AT-030 are recorded in `docs/ACCEPTANCE.md` with four honest partials and one `inconclusive` (`c82dbdd`, VALIDATION revision 15). `scripts/check` green.
  Left open for Doctor Biz: the merge decision on `m1-fleet-page` (14 commits since the last entry). aibox03 is clean — only privd running, ~36 GB free.
- 2026-09-04 (M1b cont., Task 14 step 7 — the browser run, compactions: 30 on 2026-09-04) — The one clause of §18's M1 gate line no automated run covered got covered. A real Chrome (agent-browser, 1440x900) loaded `/ui/` from a real vmobsd on aibox03 serving three VMs launched by `scripts/aibox03/demo`, and demo-3 was stopped and started again by clicking its own buttons in the fleet table: `stopped/stopped rev 5` with `op-000005 succeeded`, then `running/running rev 7` with `op-000006 succeeded`. Storage read back from the page matched `/api/v1/host/status` exactly — `Disk 9216 / 35966 MiB, 26750 MiB free`, `2048 MiB root / 1024 MiB workspace` per row.
  It found a P1 the API-driven gate did not: start never re-reserves the compute that stop released (deviation above, kata `r799`). Recorded, not fixed — fixing it inside the gate task would have changed the binaries the two PASS runs were measured against. `docs/ACCEPTANCE.md` replaces its "no browser run" sentences with what the run did and what it found; VALIDATION revision 16 carries it with a dated correction clause on revision 15 (`bbdaa80`). `scripts/check` green, `uv run docs/validation/check.py` 46/46.
  Teardown clean: `scripts/aibox03/demo stop` exit 0, no firecracker/vmobsd/runner processes, jail dir empty, 35951 MiB free, no tap devices. Two stale privd ledger entries from earlier today survive in `/run/vmobs/privd` (04:14 and 19:38, root-owned, ~150 bytes each); they name no live process, jail or tap, they die at reboot, and clearing them means running the root helper against VM ids this task did not create, so they were left alone.
  Left open for Doctor Biz, unchanged: the merge decision on `m1-fleet-page`. Open katas: `r799` (P1, this one), `ffxv` and `52pj` and `vmvp` (P2), `0yh5` and `bn1t` and `q4b2` (P3).
- 2026-09-04 (M1 merged) — `m1-fleet-page` fast-forwarded onto `main` at `f7ddf3d`, 39 commits, no merge commit (same practice as the M1a waves). `env -u GOROOT mise exec -- ./scripts/check` green on `main` after the merge. The branch is kept, identical to main. M1 ships with one known P1 recorded against it, kata `r799` — start never re-reserves the compute that stop released — plus `ffxv`, `52pj`, `vmvp` (P2) and `0yh5`, `bn1t`, `q4b2` (P3). Next: M2, and `r799` first.
- 2026-09-04 (M2 begins — kata `r799` fixed, branch `m2-start-reserves-compute`) — The P1 M1 shipped with is closed at `816ae18`. `TransitionInput` gained `AcquireCompute` and an `Admit func(ReservationTotals) error`; `TransitionVM` evaluates the callback inside the writer transaction, after the from→to check and before any write, then clears `compute_released` on the reservation row. The `start` case in `internal/runtime/manager.go` sets both, reading the reservation row for the amounts to re-acquire — that row is what `ReservationTotals` sums, so recomputing from the VM row plus current config could admit against one number and hold another — and passing `0` for disk, because the row never released the disk and asking again would refuse a restart that fits. `failAction` now carries a typed `*store.AdmissionRefusal`'s own cause and message onto the failed operation instead of flattening both to `action_failed`, so the actions route answers 409 `insufficient_capacity` through the mapping `internal/api/vms.go` already had.
  Only one transition in the codebase comes from `stopped`: the other two `To: "starting"` sites (`manager.go:612`, `batch.go:317`) are pinned `From: "provisioning"`, where the create just inserted the row with `compute_released = 0`. Six tests, five mutation proofs. Store: totals restored after a round trip; a refusal leaves the VM stopped with totals unchanged and the callback sees this VM's own compute excluded. Manager: capacity restored; a start refused when the freed memory went to another VM, with the operation `failed` and `error_cause: insufficient_capacity`; and a stop/start on a host with room for exactly one VM's disk, which fails with "disk: need 18432 MiB, only 0 MiB free" the moment the disk is passed again. API: the 409 on the actions route, and every field of `/host/status` back to its pre-stop value. Disabling the acquire reproduces the original numbers exactly — `reserved_memory_mib` 0 where 768 was wanted, `reserved_disk_mib` still correct.
  Live on aibox03, two real Firecracker VMs at 1 vCPU / 512 MiB: `reserved_memory_mib` 2560 → 1280 → 2560 across the stop and the start, `reserved_vcpu` 2 → 1 → 2, `reserved_disk_mib` 6144 throughout, `free_memory_mib` back to 48589 exactly, `op-000009` and `op-000010` both succeeded, VM `running` at revision 7. Teardown clean: 36 GB free, no firecracker processes, no tap devices; the same two stale privd ledger entries from earlier survive and were again left alone. The refusal half has no live evidence on purpose — the transaction refuses before it writes anything and before `rt.Launch`, so a real host shows nothing the fake does not.
  `env -u GOROOT mise exec -- ./scripts/check` green; `uv run docs/validation/check.py` 46/46. `docs/ACCEPTANCE.md` keeps its gate paragraph and appends a dated parenthesis naming the fix; VALIDATION revision 17 records it and puts a correction clause on revision 16's "unfixed at this commit". `gotchas.md`'s entry is rewritten from the defect to the invariant. No AT covers capacity across a stop/start — that gap is what let this ship — and adding one belongs to whichever milestone next revises the ID set, not to a retroactive edit.
  Open katas after this: `ffxv`, `52pj`, `vmvp` (P2), `0yh5`, `bn1t`, `q4b2` (P3).
- 2026-09-04 (r799 merged) — `m2-start-reserves-compute` fast-forwarded onto `main` at `730f5f4`, three commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after the merge. The branch is kept, identical to main. Main no longer carries the capacity leak M1 shipped with. Open katas: `ffxv`, `52pj`, `vmvp` (P2), `0yh5`, `bn1t`, `q4b2` (P3). Next: M2 proper (§18 Milestone 2), which needs a plan before code — no `process.*` kind is registered and no guest sensor emits one.
- 2026-09-04 (M2 cont. — kata `vmvp`, branch `m2-honest-unmapped-errors`) — `writeVMError`'s catch-all stopped blaming storage, at `73c4025`. The kata's argument was that two instances of this exact shape had already been fixed one at a time, each found by an operator rather than by review, so the third should be found by enumerating the set. Enumerated, three shapes reached the fallthrough: every `m.rt.*` host verb failure across six sites, a bare `context.DeadlineExceeded` from a store read on an expired operation budget, and genuine SQLite errors — and only the last is storage, so the cause was wrong for two of the three.
  `runtime.ErrRuntimeOpFailed{VMID, Op, Err}` now types all six verb sites and maps to 500 `runtime_operation_failed`, carrying the runtime's own words and `details.runtime_operation`. Its `Unwrap()` is not decoration: without it an `UnavailableError` raised by a verb matches the new branch before the 501 branch and a runtime that cannot run at all reads as a host that tried and failed. Retryability comes from `validTransitions` rather than from an assertion — a failed `launch` leaves the VM at `failed`, whose only exit is `deleting`, so that one is `retryable: false` with delete-and-recreate as the remediation, while every other verb leaves the VM where the operator can ask again. `context.DeadlineExceeded` maps to `operation_timeout`, `retryable: false`, because a deadline says when we stopped waiting and never whether the work landed. What is left answers `cause: "unclassified"`, `retryable: false`, no echo of the error's text, and remediation naming the VM row and `/host/status`.
  Four rulings, each recorded in the kata with its cost if wrong. `context.Canceled` gets no branch — mutations detach via `Manager.OperationContext`, so a cancel can only arrive from a handler's pre-fetch read whose client is already gone, and there is no access log; a branch nobody can observe is decoration. No logging in the catch-all — `Server` has no logger and `writeVMError` is a free function at 13 sites. No echo of unclassified error text. And the scope stops at `writeVMError`: `failAction`'s durable `error_cause` still writes `action_failed`, vague but naming no innocent subsystem — worth its own kata, not this one.
  Seven mutation proofs, each observed rather than reasoned: `Launch` back to `fmt.Errorf` (`error is *fmt.wrapError …, want *runtime.ErrRuntimeOpFailed`); `Stop` bare (same, plus `cause = "unclassified", want runtime_operation_failed` over HTTP); `Unwrap()` deleted (`status 500 cause "runtime_operation_failed", want 501 runtime_unavailable`); each of the two new mapper branches removed (`want "runtime_operation_failed"`, `want "operation_timeout"`); the fallthrough back to `storage_failure` (`nothing this layer knows answered storage_failure; the map does not know that`); and the fallthrough echoing the error with `retryable: true` (`an unclassified fault gives no reason to believe a retry would differ`). The deadline mutation taught its own lesson first time round — removing the branch left `"context"` imported and unused, the package failed to build, and a `FAIL:`-only grep reported a silent pass. A mutation proof that prints nothing is not a proof; the runner now requires a non-zero exit and greps build errors too.
  `internal/api/vm_error_map_test.go` is the repo's first internal `package api` test file — `writeVMError` is unexported and two of the fifteen shapes (an idempotency conflict, a bare context error) have no HTTP injection point. It drives all fifteen through `httptest`, asserts as a property that nothing the map knows answers `storage_failure`, and proves with a real store that `GetVM` returns context errors raw.
  No live evidence and none needed: the branch is exercised by whatever the runtime returns, and the check that mattered was whether it is dead code on a real host — `internal/jailer/launch.go` and `stop.go` return plain `fmt.Errorf` for real faults (`start_vm: %w`, `spawn runner: %w`, `dial runner ctl %s: %w`), not `UnavailableError`, so a real failed launch or stop takes it. No `docs/VALIDATION.md` revision either: that ledger records runs of `docs/validation/check.py`, which reads `SPEC.md`, `ACCEPTANCE.md`, `README.md` and `VALIDATION.md` only, and this change touches none of them. No consumer breaks — there is no cause registry, and the web branches on `retryable` only for 409s.
  `env -u GOROOT mise exec -- ./scripts/check` green. Filed, not fixed: `Error.OperationID` is never populated on any error path, so an error that says "read the operation" never names which one — `manager.Action` returns the operation alongside the error and `handleAction` discards it. Open katas after this: `ffxv`, `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3). Left open for Doctor Biz: the merge decision on `m2-honest-unmapped-errors`.
- 2026-09-04 (vmvp merged) — `m2-honest-unmapped-errors` fast-forwarded onto `main` at `deeac5d`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after the merge. The branch is kept, identical to main. Main's API no longer answers an unmapped error by naming storage. Open katas: `mtxc` (P2, filed during this work — `Error.OperationID` is never populated, so the two mappings that tell an operator to read the operation never name which one, including on the action path where `handleAction` holds it and discards it), `ffxv`, `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3). Next: M2 proper (§18 Milestone 2), still unplanned — no `process.*` kind is registered and no guest sensor emits one.
- 2026-09-04 (M2 cont. — kata `mtxc`, branch `m2-errors-name-the-operation`) — The sentence `vmvp` started is finished at `091ad0e`. `Error.OperationID` had existed since the error contract was written and nothing ever set it, so the two mappings `vmvp` added — both of which remediate with "read the operation" — handed over a `GET /operations/{id}` with no id, while `handleAction` and `handleCreateVM` each held the operation and discarded it one line above the answer.
  `writeVMError` is split, not extended: `vmErrorFor(err) (int, Error)` returns the pair its fifteen branches used to write directly, `writeVMError` keeps the eleven call sites with no operation to name, and `writeVMErrorForOperation` serves the two that hold one. The mapping is byte-identical for every input, which `TestNamingTheOperationChangesNothingElse` asserts rather than assumes. The larger half of the value is not on the 500 paths at all: `failAction` records a failed operation for a refused start and the create path records one for a refused create, both answered 409 `insufficient_capacity`, and those are the cases where an operator most wants the shortfall back after the response is gone.
  Two guards, both proven load-bearing rather than defensive. Dropping the nil check panics three real tests — `Action` returns a nil operation for an unknown action, a failed `GetVM` and a failed operation insert, and `CreateVM` returns nil for an unavailable runtime. Dropping the zero check renders `"op-000000"`, which `parseOperationID` accepts and which resolves to nothing: an answer-shaped miss, worse than an absent field. `Delete` is outside the change by construction — it writes `OperationID: 0` on all four of its transitions and creates no operation record — and `CreateBatch` returns no operation either; both were checked rather than assumed.
  The tests follow the remediation instead of asserting a field is populated. `TestActionRuntimeFailureTeaches` now fetches the operation by the id the error gave it and reads the runtime's own words off that record, replacing the store scan it used to do — the scan existed only because the id was unavailable, which was the defect. `TestCreateVMAdmissionRefusal` asserts the AT-006 replay names the *same* operation as the original, because an idempotent retry answers from one record and a second id would invent a history the store does not have. Five mutation proofs, each observed.
  One process failure worth keeping: the first mutation run destroyed the fix. `git checkout -- internal/api/vms.go` at the top of the loop reverted an implementation that was written but not committed, and the run reported five missing anchors instead of five dead mutations — a clean-looking result for a file that no longer contained the code under test. Commit the implementation before mutating it.
  `env -u GOROOT mise exec -- ./scripts/check` green. Open katas: `ffxv`, `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3). Next: M2 proper (§18 Milestone 2), still unplanned.
- 2026-09-05 (mtxc merged) — `m2-errors-name-the-operation` fast-forwarded onto `main` at `2e95e8b`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after the merge. The branch is kept, identical to main. Main's error contract now finishes its own sentence: the mappings that remediate with "read the operation" name which one, on the 409 refusal paths as well as the 500s. Open katas: `ffxv`, `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3). Next: M2 proper (§18 Milestone 2), which still needs a plan before code — no `process.*` kind is registered in `internal/events` and nothing emits `fs.modify`.
- 2026-09-05 (ffxv, second half) — Branch `m2-delete-signals-before-release`. `Delete` and `Reconcile` now signal a VM's process before releasing its jail resources. Two commits: `54455bb` the fix, `1373e57` the two tests that say an unsignalled VM is never released.
  The kata offered two fix directions and asked that neither be assumed. They are not alternatives, and the evidence says so. Adoption — Reconcile asking the runtime before writing `failed` — would not have fixed this, because a live VMM behind a `failed` row is reachable with no reconcile in the picture: `failLaunch`'s three launch-rollback sites (`manager.go` at the `starting`/`launch`/`running` stages, and the `start` action's own launch) each write `failed` and then run `_ = m.rt.ForceStop(...)`, discarding the error. The row is a record; the process is a fact; `Delete` was reading the first as evidence of the second. Same class as every M1a wave. **Ruling: ship the delete-side fix now, leave adoption open for M2. Cost if wrong: one extra force-stop per delete on VMs whose VMM is genuinely gone — idempotent, and `stopWithoutManifest` is built for exactly that case.**
  **Ruling: `failed` does not join `liveStates`, despite the kata's wording.** `failed → deleting` is its only edge in the §5.2 matrix (`internal/store/vms.go`), so routing it through the live-state walk would ride two invalid transitions that the code silently tolerates — the fix would work by accident. The signal belongs next to `Release`, which is the call that needs it, not inside a walk it cannot legally take. Cost if wrong: none found; the walk's `stopping`/`stopped` transitions carry `ReleaseCompute`, which `failLaunch` has already done.
  **Ruling: the guard covers `stopped` as well as `failed`, and runs after the transition to `deleting` rather than before it.** `stopped` carries the identical lie — `doStop`'s forced path drops its `SignalVM` errors and writes `stopped` regardless, and Reconcile settles a `stopping` row without observing anything — and excluding it would have taken extra code to preserve a known hole. Placing the signal after the transition is what makes that safe: the transition is the atomic guard, so a `stopped` VM someone restarted under us fails it and keeps its process, and the caller's revision pin is still checked before anything is destroyed. Cost if wrong: a `ForceStop` failure parks the row at `deleting` instead of leaving it at `failed`, and only a restart retries it. That retry hole is not new — a `Release` failure has always parked there — and it is now filed as `vexd`.
  Reconcile's `deleting` case got the same treatment and needed it more: it is the only retry a stalled delete gets, and it runs at startup only. Releasing without signalling meant a row parked by a live VMM reproduced privd's refusal on every restart, which is what aibox03 did on 2026-09-04 until the root helper killed the process by hand. A signal that fails for any reason but an absent runtime now skips the release entirely; the retained row is the record.
  Six tests, five mutation proofs, all dead: deleting the pre-release guard (three tests fail), dropping `signalled = true` so the force path signals twice, swallowing the guard's error, removing Reconcile's signal, and calling Reconcile's signal but ignoring its verdict. `TestStrandedDeleteRecoversOnRestart` replays the incident end to end — privd refuses the release, the row parks at `deleting`, and the next startup signals and finishes it without a human running `jail-stop`. The first attempt at the first mutation died on a compile error rather than a test, which proves nothing; redone as `_ = signalled` so it compiled.
  Honest limits. `ForceStop` returning nil is not proof of death — the forced path drops `SignalVM`'s errors and SIGKILL cannot reap a task in uninterruptible sleep — so this narrows the window rather than closing it. What it removes is the case where nobody asked at all. No live-host coverage: the unit suite proves the verb order against the fake, and the aibox03 evidence for this path is the incident report in the kata, not a gate run.
  `env -u GOROOT mise exec -- ./scripts/check` green. `ffxv` stays open for the adoption half. New kata `vexd` (P2): DELETE on a row already parked at `deleting` answers success and does nothing. Open katas: `ffxv`, `vexd`, `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3).
- 2026-09-05 (ffxv second half merged) — `m2-delete-signals-before-release` fast-forwarded onto `main` at `aea782e`, three commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. Main no longer strands a VM whose process outlived its row: `Delete` and Reconcile's `deleting` retry both signal before releasing. `ffxv` stays open for the adoption half — Reconcile still writes `failed` over live `running`/`paused` rows at startup without asking the runtime. Open katas: `ffxv`, `vexd`, `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3).
- 2026-09-05 (vexd) — Branch `m2-delete-resumes-a-parked-row`. `Delete` now resumes a row parked at `deleting` instead of answering it from the row. One commit, `5b45de9`.
  The retry hole `ffxv` filed. `Delete` returned early for both `deleted` and `deleting`, and the two states are not alike: `deleted` is finished — the row and its resources went together, so answering from the row is correct idempotency — while `deleting` means the netns, the jail chroot and privd's ledger entry are still on the host. Every retry got `200 OK` and a `deleted`-shaped answer with nothing done, and `ErrReleaseFailed` was meanwhile publishing `retryable: true` with a remediation naming that exact DELETE (`internal/api/vms.go`). The contract was already right; the code underneath it was not. Reconcile's `deleting` case runs at startup only, so restarting the daemon was the whole retry story.
  **Ruling: keep `deleted`'s early return, replace `deleting`'s with a resume.** The resume skips the transition — the row is already there — and runs the signal and the release. Cost if wrong: a redundant `ForceStop`-plus-`Release` pair against a VM whose resources really are gone; both are idempotent, and `stopWithoutManifest` exists for the no-manifest case.
  **Ruling: a resume consumes no revision pin, and this is correctness rather than laziness.** `deleting → deleted` is the only edge out of that state and only `TransitionVM` bumps a row's revision (`internal/store/vms.go`) — every other `UPDATE vms` touches `last_event_id`. So a caller's stale pin on a `deleting` row can only mean the row already reached `deleted`, which the first early return answers before the pin is ever read. Cost if wrong: a concurrent writer that starts bumping revisions on `deleting` rows would make a resume miss a conflict; the invariant is asserted nowhere but the transition matrix, so it is written into the comment at the site.
  **Ruling: "in progress" becomes an in-flight set, not a state.** The row cannot tell a running delete from a stalled one — both read `deleting` — and the two want opposite answers, so `Manager` keeps `deleting map[string]bool` behind a mutex with `beginDelete`/`endDelete`. Without it, a second DELETE arriving inside the first one's `ForceStop`-plus-`Release` window (up to ~55s on the real jailer) duplicates the host work and then loses the transition race, answering the operator with a confusing invalid-transition error. Cost if wrong: the set is per-process, so two controllers against one store would still race — which they already do everywhere else, and the store's transition is the real arbiter.
  Four tests. `TestDeleteResumesARowParkedAtDeleting` is the headline and was red before the fix (`retry ran []; a parked row must be signalled and released, not answered from the row`). `TestDeleteOfADeletedVMTouchesNothing` keeps the idempotency that is genuinely correct. `TestConcurrentDeleteDoesNotDuplicateWork` blocks the first delete inside `Release` and runs a second on the main goroutine. And `TestDeleteVMReleaseFailureRetryActuallyRetries` walks the published remediation end to end over HTTP — it reads the `delete` action out of the error's own remediation list, issues that path, and asserts the VM reaches `deleted`. `go test -race ./internal/runtime/` clean.
  Three mutation proofs, all dead: restoring the `deleting` early return (the headline test and the API remediation test both fail), dropping the `beginDelete` guard (`TestConcurrentDeleteDoesNotDuplicateWork` fails), and dropping `defer m.endDelete(vmID)` (three tests fail — the claim outlives the call and the retry is answered as "in progress"). `golangci-lint` caught a real defect the tests could not: an ineffectual assignment where the setup's call-count offset was shadowed by the one taken after the refused delete.
  Honest limits. No live-host coverage — the verb order and the resume are proven against the fake; the incident that motivated it is the aibox03 report in `ffxv`. The in-flight set is per-process. And a resumed delete inherits `ffxv`'s limit: a `ForceStop` returning nil is not proof of death.
  `env -u GOROOT mise exec -- ./scripts/check` green. Open katas: `ffxv` (adoption half), `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3).
- 2026-09-05 (vexd merged) — `m2-delete-resumes-a-parked-row` fast-forwarded onto `main` at `630dc64`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. Main's delete path now finishes what `ffxv` started: a VM whose process outlived its row is signalled before its resources are released, and a row parked at `deleting` is resumed by the next DELETE instead of answering it from the row — so `ErrReleaseFailed`'s published `retryable: true` is true. Open katas: `ffxv` (adoption half), `52pj` (P2), `0yh5`, `bn1t`, `q4b2` (P3).
- 2026-09-05 (52pj) — Branch `m2-templates-drop-image-paths`. `kernel_image` and `root_image` leave the template manifest, the `Template` struct and the `GET /templates` wire. One commit, `9f7cda9`.
  The fields were read by nothing. `doStage` stages the kernel from `lk.GuestKernel.VmlinuxPath` and the rootfs from `lk.RootImage.Path`, resolved against `RepoRoot` and verified against the SHA-256s pinned in `runtime.lock.json` (`internal/jailer/launch.go`). The manifest's paths were a second declaration of the same fact, and both writers in the tree proved it: the demo template named `/srv/vmobs/images`, which is not a directory on aibox03, and `scripts/smoke` wrote `/nonexistent/vmlinuz` with a comment saying so. The API served them verbatim to an operator and to the launch form.
  **Ruling: drop the fields rather than validate them.** The kata offered both. Validation keeps two declarations of what a VM boots while the jailer obeys only one, so it would make the lie well-formed instead of absent — a direct one-source-of-truth violation. SPEC §18 defines `GET /templates` as templates, profiles, sensor support and toolchain inventory, and §7 puts image identity in the lock, so dropping matches the spec and validating would invent a requirement. Cost if wrong: if per-template images ever exist, the field comes back with a meaning — and the lock is still where the pinned artifact lives, so it would be a lock reference, not a path.
  **Ruling: an existing manifest carrying either key is refused at load, and the refusal teaches.** `DisallowUnknownFields` already rejects them, but "unknown field" reads as a typo when the field is one we shipped in our own demo template last week. `retiredFields` names both keys with the lock field that replaces each, checked *before* the strict decode so the specific message wins. Cost if wrong: a host with a hand-written template fails to boot until the two lines are deleted — `cmd/vmobsd` treats a template parse error as fatal, which is the existing "malformed = hard error, not skip" policy. The demo self-heals: `write_config` rewrites the manifest on every `start`.
  Three tests. `TestLoadTemplatesRefusesImagePaths` (subtest per field) asserts both that the error names the field and that it names `runtime.lock.json`. `TestLoadTemplatesAcceptsAManifestWithoutImagePaths` keeps the replacement shape loading with a digest. `TestTemplatesPublishNoImagePaths` asserts the two keys are absent from the response object rather than empty.
  Three mutation proofs, all dead: dropping the retired-key check, moving it after the strict decode, and publishing `kernel_image` on the wire again. The first two produce the same failure — `error does not say where images come from: … json: unknown field "kernel_image"` — because the ordering is exactly what the check depends on; recorded as one property proven twice, not two independent proofs.
  Fourteen files: the two source files, six test fixtures that carried the fields (`cmd/vmobs`, `internal/report`, `internal/runtime`, `internal/api`), the three manifest writers (`scripts/aibox03/demo`, `scripts/smoke`, `tests/integration/m1a_gate_test.go` at both sites), and three web files (`types.ts`, `fixtures.ts`, `App.test.tsx` — the fields were typed and fixtured but never rendered). No `docs/` change: no schema, example or spec text mentions them, so no `docs/VALIDATION.md` revision.
  `env -u GOROOT mise exec -- ./scripts/check` green. New kata `n6wh` (P3): nothing serves the images a VM actually boots — the lock has verified provenance and no endpoint publishes it. Open katas: `ffxv` (adoption half, P2), `0yh5`, `bn1t`, `n6wh`, `q4b2` (P3).
- 2026-09-05 (52pj merged) — `m2-templates-drop-image-paths` fast-forwarded onto `main` at `c80d9b7`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. `GET /templates` no longer tells an operator where the kernel came from, because it never knew; `runtime.lock.json` is the single declaration and a manifest that disagrees is refused at load. Open katas: `ffxv` (adoption half, P2), `0yh5`, `bn1t`, `n6wh`, `q4b2` (P3).
- 2026-09-05 (0yh5) — Branch `m2-one-runner-ctl-wire`. `internal/jailer`'s private `adapterCtlRequest`/`adapterCtlReply` are deleted; `dialCtl`, `ctlRefusalReason` and `dialCtlFn` speak `runner.CtlRequest`/`runner.CtlReply`.
  The duplication's stated reason was false on its face. The comment said the shape was replicated "so the jailer does not depend on the runner package"; `stop.go` imports `internal/runner` three lines above that sentence, and `internal/runner` imports nothing from `internal/jailer`, so the import direction the kata asked me to check has no cycle in it. The copy was already behind the original — `runner.CtlRequest` grew `Terminal *TerminalCtlRequest` in L1b and `CtlReply` grew `Terminal *TerminalCtlReply`, and the jailer's pair had neither.
  **Ruling: the deliverable's test is behavioural interop, not a compile check.** Unifying two types is enforced by the compiler, so the honest red state is a build failure and there is no failing test to write for the change itself. What was genuinely untested is the claim the change rests on: that the jailer's client and the runner's server are one protocol. Every existing test in `stop_ctl_linux_test.go` hands `dialCtl` a socket the test file decodes itself, so it proves the client agrees with the test — a copy checked against a copy. `TestDialCtlSpeaksTheRunnersWire` drives `dialCtl` against a real `runner.ListenCtl` server: a `shutdown_guest` leg the handler must see the grace period from, a `terminal-create` leg whose `Terminal` block the handler must receive, and a `terminal-list` leg whose `Terminal` block must survive the decode home. Cost if wrong: the test asserts the wire, not the teardown, so a regression in *when* the jailer sends these commands still falls to the existing suite.
  Two mutation proofs, both dead, and they die at different assertions — the two halves are independent. Narrowing the encode to `cmd`/`grace_s` (the old request shape) gets `terminal-create reply not OK: "terminal-create needs a terminal block"` from the server. Narrowing the decode to `ok`/`error` (the old reply shape) gets `terminal-list reply lost its terminal block on the way back`.
  Four files: `internal/jailer/stop.go` (the struct pair and its comment go, three signatures change), `internal/jailer/stop_ctl_linux_test.go` (rename plus the new test), `internal/jailer/stop_warn_linux_test.go` (rename across the `dialCtlFn` stubs), and `internal/runner/ctl.go`, whose comment enumerating the wire's encoders now names both of them.
  `env -u GOROOT mise exec -- ./scripts/check` green. `internal/jailer` is Linux-only, so its tests ran on aibox03: `scripts/linux 'go test ./internal/jailer/ ./internal/runner/ -count=1'` → both ok, 41.5s and 3.0s. Open katas: `ffxv` (adoption half, P2), `bn1t`, `n6wh`, `q4b2` (P3).
- 2026-09-05 (0yh5 merged) — `m2-one-runner-ctl-wire` fast-forwarded onto `main` at `59e7c9c`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. The runner ctl wire now has exactly one struct pair, and an interop test standing behind it.
- 2026-09-05 (n6wh) — Branch `m2-host-status-serves-images`. `GET /host/status` gains an `images` block carrying `runtime.lock.json`'s `guest_kernel` and `root_image` entries.
  **Ruling: host scope, not per-VM.** The kata offered both and asked for a decision. Host-level is what a launch would stage now; per-VM is what a given VM did boot, and the two differ only when the lock changes under a running VM. Host-level is also the half the launch form needs, so it is required either way — and `/host/status` is host scope by its own route, so there is no way to misread the block as per-VM. The per-VM half is genuinely more work than a column: `Runtime.Launch` returns only an `error`, so recording what was staged means changing the interface and its six implementations and callers. Filed as its own kata rather than smuggled in here. Cost if wrong: an operator on a host whose lock changed mid-flight reads the block as that VM's answer. Mitigated by the route's scope and by the code comment at the render site.
  **Ruling: serve the lock entries whole, not a chosen subset.** `base_image_ref`, `apt_snapshot`, `source_url` and the config hash are the provenance half of SPEC §7, and a digest with none of them beside it says what booted without saying where it came from. Rendering `lk.GuestKernel` and `lk.RootImage` directly also means one definition — the wire names are the lock's own JSON tags, so the block cannot drift from the file.
  **Ruling: the lock reaches the API through the manager.** `api.New` is a six-argument positional constructor; a seventh nilable parameter would have touched twenty call sites, nineteen of them tests passing `nil`. The handler already asks the manager for `Capacity`, `AdmissionParams` and `DefaultParams`, so `ManagerConfig.Lock` plus `Manager.RuntimeLock()` is the existing idiom. A nil lock omits the block, the rule preflight already follows.
  Four tests. Two at the API seam (the block's contents; its absence with no lock) and two on the wiring — `managerConfig()` is extracted in `cmd/vmobsd/main.go` beside `preflightConfig()`, which the repo had already extracted for the same reason, because dropping `Lock: pfLock` there makes the block vanish on a real host with every unit test still green.
  Three mutation proofs, all dead: serving an empty block when the lock is nil (`host/status serves an images block with no lock behind it: map[]`), narrowing `root_image` to digest and path (`base_image_ref = ""`, `apt_snapshot = ""`), and dropping the lock in the wiring (`ManagerConfig.Lock = <nil>, want the lock preflight was handed`).
  Live evidence on aibox03: a real `vmobsd` built from this tree, started on a scratch port against a copy of the demo config in `runtime.mode: unavailable`, served the block from the host's own `runtime.lock.json` — kernel 6.1.186 with `vmlinux_sha256` `b6067686…` and the Ubuntu-based rootfs `8adb2535…` with its apt snapshot, matching the file byte for byte. Scratch dir and process cleaned up; nothing else on the host was touched, and no demo daemon was running.
  `env -u GOROOT mise exec -- ./scripts/check` green. Not done: the web UI does not display the block, and SPEC §14's one-line summary of `/host/status` was left alone — the addition is within R-17/P-07 and that line is a summary, not a field list.
- 2026-09-05 (n6wh merged) — `m2-host-status-serves-images` fast-forwarded onto `main` at `b8c6b07`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. `GET /host/status` now answers what a launch would stage. New kata `qxth` (P3): nothing records what a given VM did boot, and `Runtime.Launch` returns only an error, so it has nowhere to report it. Open katas: `ffxv` (adoption half, P2), `bn1t`, `q4b2`, `qxth` (P3).
- 2026-09-05 (bn1t) — Branch `m2-vm-detail-action-row`. The confirm dialog leaves `BulkActions`, and §13.2's action row exists: `web/src/components/VMActions.tsx` draws start, pause, resume, stop, force stop and delete for the one VM on screen, each gated by the same `actionLegality` the fleet rows use, and `web/src/VMDetail.tsx` drives it with its own `useFleetControls`, re-reading the VM when an action settles.
  **Ruling: no file split — `DeleteConfirm` stays in `BulkActions.tsx`.** The obvious extraction, a `DeleteConfirm.tsx` that `BulkActions.tsx` also imports, is an import cycle: the dialog needs `FleetControls` and `LIVE_STATES`, which live beside the hook. Breaking it properly means moving the hook and the whole action vocabulary (~250 unchanged lines) into a new module and updating `App.tsx`, `VMTable.tsx` and `BulkActions.test.tsx` — churn in three files that no test would be any better for. `BulkActions.tsx` exports `DeleteConfirm`; `VMActions.tsx` imports it one way; no cycle, no moved lines. Cost if wrong: the detail page imports its dialog from a file named for the bulk bar, a rename away from fixed.
  **Ruling: `force_stop` on the detail row only.** §13.2's sketch names Force stop; §13.1's multi-select text enumerates "stop/pause/resume/delete" and does not. Forcing a whole selection at once is a bigger gesture than §13.1 asks for; on the single VM an operator is looking at, it is the action the spec names. `ROW_ACTIONS` is unchanged and `DETAIL_ACTIONS` is the detail page's own list. Cost if wrong: a bulk force-stop is still per-VM.
  **Ruling: no Export or Clone template buttons**, though §13.2's sketch shows them. `/vms/{id}/exports` and the clone path are registered stubs in this build (`internal/api/api.go`), so either button would be a control that cannot work.
  Ten tests: seven in `web/src/components/VMActions.test.tsx` (every action offered and gated on `observed_state`; the action sent with the VM's own revision; `force_stop` travelling as itself on the wire; the re-read after settling; the confirm naming the force before `DELETE ...&force=true`; cancel sending nothing; a delete nobody confirmed never reaching the wire) and three in `web/src/VMDetail.test.tsx` for the workspace's own wiring. `npm test` 22 files / 212 tests green, `npm run typecheck` clean.
  Five mutation proofs, all dead: `VMActions` not rendering `DeleteConfirm` (4 fail across both suites); `force_stop` sent as `stop` (`expected { action: 'stop' } to deeply equal { action: 'force_stop' }`); `force_stop` legality narrowed to `stopped` (3 fail); `BulkActions` not rendering `DeleteConfirm` (2 fail — the extraction kept the fleet page whole); `useFleetControls(() => {})` in `VMDetail` (the workspace never re-reads after an action settles).
  Not done: the visual check. A `vmobsd` built from this tree runs on darwin but refuses `POST /vms` with `missing_capability` (no KVM), so there is no VM row for a detail page to draw, and hand-writing one into SQLite would be inventing the evidence. The row reuses `.row-actions`, already verified in a real browser during L1b; only the new `.vm-actions` margin is unseen. The L1b-T13 deviation above is closed.
- 2026-09-05 (bn1t merged) — `m2-vm-detail-action-row` fast-forwarded onto `main` at `d2b08f7`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. §13.2's detail workspace now offers every lifecycle action, and the L1b-T13 deviation above is closed. Open katas: `ffxv` (adoption half, P2), `q4b2`, `qxth` (P3). Next: M2 proper (§18 Milestone 2) — still unplanned, and it needs an architecture conversation before code.
- 2026-09-05 (qxth) — Branch `m2-vm-records-its-images`. A VM row records the kernel and root image its current boot staged, and `GET /vms/{id}` publishes them. `Runtime.Launch` returns `(*lock.Images, error)`; store migration v8 adds `boot_images`.
  **Ruling: the launch reports what it staged — the kata's cheap alternative is unsound as written.** The kata offered "record the lock's content digest on the VM at launch", which reads as something the manager could stamp from the lock it already holds (`ManagerConfig.Lock`, n6wh). It cannot: `doStage` calls `lock.Load(a.cfg.LockPath)` on every launch while the daemon loads the lock once at startup (`cmd/vmobsd/main.go`) and serves that copy. If the file changes under a running daemon the two disagree, and a manager-stamped digest would name a lock the VM did not boot — an invented answer with a provenance label on it. So `Runtime.Launch(ctx, spec) (*lock.Images, error)`: four implementations (`jailer.Adapter` ×2 build tags, `runtimetest.Fake`, `unavailableRuntime`), three call sites (`manager.go` ×2, `batch.go`). Cost if wrong: a wider interface than a column would have been.
  **Ruling: the VM row, not a boots table.** The kata is right that the honest unit is a boot — and `current_boot_id` already sits on the row with exactly that property, holding one boot and overwriting it on the next. Recording the images beside it is as honest as the field they sit next to and costs one column. A boots table is a history feature; nothing asks to read a VM's previous boots today. Cost if wrong: the schema for boot history gets designed later, by whoever needs it.
  **Ruling: serve the lock's image entries whole, and share one type with `/host/status`.** `lock.Images{GuestKernel, RootImage}` plus `(*Lock).Images()`, rendered by both the host block and the VM row, so the two readers cannot describe the same fact in different words. Stored as one JSON TEXT column, the way `labels` already is, with the lock file's own JSON tags — storage and the wire cannot rename a field `runtime.lock.json` owns.
  **Ruling: a new boot clears the previous boot's images.** `provisioning → starting` establishes the boot id before `Launch` runs, so the images can only ride the `→ running` transition after it. Between those two writes the row says nothing rather than the last boot's answer, and a launch that fails leaves it empty — nothing was verified to have booted.
  Ten tests: one in `internal/lock` (both entries survive a round trip whole), two in `internal/store` (the transition records what it was handed; a new boot id clears the column), five in `internal/runtime` (`boot_images_test.go` — a launch's report reaches the row; a launch that reports nothing records nothing; a restart across a repinned lock records the new boot; a failed launch leaves the row empty; a batch member records its own, since `batch.go` is a third launch path), and three at the API seam (`vm_images_test.go` — the block's contents; a running VM keeping its answer while `/host/status` moves; absence when nothing was staged).
  Nine mutation proofs, all dead: the store ignoring `in.Images`; a new boot keeping the old images; each of the three call sites dropping what `Launch` returned (`runLaunch`, the `start` action, the batch member); `renderVM` leaving the field off the wire; the wire tag losing `omitempty`; `/host/status` no longer reading `lk.Images()`; and — on aibox03, because `internal/jailer` is Linux-only — `doStage` returning `lock.Images{}` instead of `lk.Images()`, which `TestLaunchTransactionAgainstFakePrivd` catches by comparing the report against a fresh read of the lock file the stage verified against.
  Linux evidence: `scripts/linux 'go vet ./... ; go test ./... -count=1'` on aibox03 — vet clean, 25 packages ok, no failures. That is where the jailer half of this change compiles and runs at all.
  `env -u GOROOT mise exec -- ./scripts/check` green. Not done: the web UI does not display the block (the API is the surface this kata asked for, and n6wh's host block landed the same way), and no boot history is kept. New kata filed on the way in: `/host/status`'s images block can be stale — it serves the lock verified at daemon startup while the jailer stages from whatever the file says now, though its own comment claims "what a launch would stage now". Which half is wrong is a real decision, so it gets its own kata; per-VM recording makes it much less load-bearing either way.
- 2026-09-05 (qxth merged) — `m2-vm-records-its-images` fast-forwarded onto `main` at `c29da35`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. `GET /vms/{id}` now answers what a VM's current boot staged, alongside `/host/status`'s answer about what a launch would stage next. New kata `qq3d` (P3): `/host/status` serves the lock verified at daemon startup while the jailer stages from whatever `runtime.lock.json` says now. Open katas: `ffxv` (adoption half, P2), `q4b2`, `qq3d` (P3). Next: M2 proper (§18 Milestone 2) — still unplanned, and it needs an architecture conversation before code.
- 2026-09-05 (qq3d) — Branch `m2-host-images-follow-the-lock`. `/host/status`'s images block reads `runtime.lock.json` per request instead of serving a copy parsed at daemon startup, so its standing claim — "what a launch would stage now" — is true.
  **Ruling: the block is wrong, not its comment and not the jailer.** The kata offered three resolutions and the code picks one. The lock's two halves are not symmetric: the **binary** half is verified once by `verifyRuntimeLock` at startup and never re-read at launch (privd owns the firecracker and jailer paths; `doStage` calls `VerifyArtifacts`, not `VerifyBinaries`), so for binaries the startup parse is the operative pin. The **image** half is re-loaded and re-verified on every launch. The one half where a snapshot is not the answer was the half being served from a snapshot. Cost if wrong: a small JSON read per `/host/status`, on a handler that already re-runs the whole doctor — including hashing both binaries — on every call.
  **Ruling: `ManagerConfig.LockPath string`, not `Lock *lock.Lock`.** The field had exactly one reader (`RuntimeLock()`) with exactly one caller (the host handler), so the contract was free to change. Its old comment said it existed so the API could publish the pins "without re-reading the file behind the runtime's back" — backwards: re-reading is the same read the runtime does, and holding a startup copy is what goes behind its back. `managerConfig` in `cmd/vmobsd/main.go` no longer takes `pfLock`; it takes the path the jailer adapter already stages from (`runtime_linux.go`).
  **Ruling: an unreadable lock is a third fact with its own shape.** Reading per request introduces a failure a startup parse could not have — the file can go bad after the daemon starts, where before `verifyRuntimeLock` made that fatal at boot. Omitting the block there would serve exactly what a host with no lock at all serves, which is the conflation this block's own comment exists to refuse. `wireHostImages` carries either both entries or an `error`, never both and never neither. Cost if wrong: one optional key on a block nothing consumes yet.
  Not verified: whether the pins the block names match the bytes on disk. That is `VerifyArtifacts` hashing a multi-gigabyte root image on a read path, and preflight owns the question.
  Four tests: two new in `internal/api/host_images_test.go` (a repin under a running daemon moves the block; an unreadable lock reports an error and names neither image) and two rewritten in `cmd/vmobsd/preflight_wiring_test.go` (the manager reads the jailer's lock file; no lock configured means no path, not a default one). The two pre-existing host-images tests kept their assertions and now run through file-based wiring — `newTemplateServerLock` writes the fixture to a temp file, because a path is what the daemon is given.
  Seven mutation proofs, all dead: the manager holding its first parse (kills both halves of the repin test); an unreadable lock omitting the block; the error block also naming zeroed entries; the manager pointed at a different file from the jailer; an unset lock defaulting to `runtime.lock.json`; an unconfigured lock serving empty pins; and the success path serving zeroed entries, which proves the pre-existing test still bites through the new wiring.
  `env -u GOROOT mise exec -- ./scripts/check` green, including `golangci-lint (GOOS=linux)`.
  New kata filed on the way out: `f8f2` (P3) — `preflight` has the same staleness and it is now visible. Its runner re-runs on every `/host/status` call and re-hashes both binaries live, but against digests from the startup parse, so the two blocks in one response now report on two versions of the same file. Less alarming than it sounds (nothing re-reads the binary half at launch either), and better than both being quietly wrong together, but it is a new way to be confused.
- 2026-09-05 (qq3d merged) — `m2-host-images-follow-the-lock` fast-forwarded onto `main` at `dcbf7c8`, two commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. `/host/status` now answers what a launch would stage from the file the launch reads, and `GET /vms/{id}` answers what each VM's boot did stage — the pair `qxth` set up. Open katas: `ffxv` (adoption half, P2), `q4b2`, `f8f2` (P3). Next: M2 proper (§18 Milestone 2) — still unplanned, and it needs an architecture conversation before code.
- 2026-09-05 (M2 architecture) — The conversation M2 was blocked on, held before any M2 code. Survey first: most of M2's spine already exists from P1–P4 (event schemas, spool with dedup and recovery-gap detection, SQLite index and replay, annotation store, situation v0 with a delta cursor, the store-level health model, ingress rejection of unregistered kinds). What is genuinely absent is the entire guest half — no telemetry transport of any kind, no `process.*` kind registered, `fs.modify` registered with zero emitters, `/events/stream` a registered route with a nil handler, no timeline or process view in `web/src`. The riskiest-looking dependency turned out already satisfied: M0 built the guest kernel with `CONFIG_BPF`, `CONFIG_DEBUG_INFO_BTF`, `CONFIG_KPROBES`, `CONFIG_PERF_EVENTS` and `CONFIG_FANOTIFY` (`images/kernel/vmobs.fragment`).
  **Decision: M2 ships as four slices, transport first.** M2a transport + one kind end to end + `ffxv` adoption; M2b process sensor (eBPF); M2c filesystem sensor (fanotify); M2d `/events/stream`, live timeline, process view. Each gets its own branch, gate and merge. Reason: a transport defect and a sensor defect are indistinguishable from the store, so they must not land together.
  **Decision: telemetry gets vsock port 10001, dedicated.** It was reserved and unused. The argument is already written down for 10002 at `internal/runner/terminal.go:20` — "Control verbs stay on vsockPort: that channel is strictly request/response, and a terminal's bytes would starve the ping cycle sharing it." A sensor firehose starves it harder than a terminal does. guestd listens on all three ports; the runner dials all three.
  **Decision: the host names the stream, the guest counts within it.** The runner derives the telemetry `source_instance_id` from its own trusted `InstanceID` at handshake; the guest owns a monotonic `seq` inside that identity, and the runner validates it. `streams.source_instance_id` is a host table's primary key and an untrusted party does not write it. The reason the guest owns the seq at all is idempotence across a reconnect: the store already dedups `(source_instance_id, source_seq)` when the payload matches, so a guest re-sending its unacknowledged tail costs nothing, where a runner re-stamping with its own counter would turn every re-send into a fresh duplicate event. A seq that does not increase is dropped and reported as `telemetry.integrity_failure`. Cost if wrong: an untrusted party influences a key the store indexes on, bounded by that validation and by `stream_scope_rebind` catching a cross-VM collision.
  Correction to my own framing during the conversation, recorded because it changed the argument: **the store does not detect seq gaps.** It detects conflicts (`seq_payload_conflict`) and scope rebinds (`stream_scope_rebind`); `spool.Recover` detects gaps in spool *segments*, which is a different layer. "Free gap detection" was not on offer from any option.
  **Decision: the first kind is `guest.sensor_health`, new, in the `guest` family.** `fs.modify` is registered and would have worked, but it pulls the fanotify slice into M2a. `telemetry.loss` is registered and host-observed, but with no guest events in M2a its count is always zero. The heartbeat is not a kind invented to exercise the pipe: §138 mandates a `telemetry_health` state and §139 a per-sensor block carrying coverage, last heartbeat time, observed drops, unknown loss intervals, exclusions and capture mode — none of which exists, and `situation.SensorsDegraded` is a struct field nothing writes. Family `guest` and not `telemetry` because `telemetry.*` is host-side facts about the pipeline (all `host_observed`) while a guest agent's self-report is `guest_reported`.
  **Deferred: the eBPF toolchain.** clang/LLVM plus `bpf2go` entering `scripts/check`, and §247's eBPF object digests in `runtime.lock.json`, are M2b decisions. Nothing in M2a compiles BPF, so the question waits until there is an object to digest rather than being answered ahead of the code.
  Plan written: `docs/superpowers/plans/2026-09-05-m2a-guest-telemetry-transport.md`, seven tasks in three phases. Kata `21aa` claims it; `ffxv` folds in as Task 6.
- 2026-09-05 (M2a Task 7 / kata `21aa` closed) — Branch `m2a-guest-telemetry`, thirteen commits, all seven plan tasks done. `TestM2aGate` on aibox03: six subtests, six passes, 223.62s, real Firecracker. Evidence at `tests/integration/evidence/m2a-gate-aibox03.txt`; acceptance in the "L2 M2a status notes (2026-09-05)" block of `docs/ACCEPTANCE.md`; `docs/VALIDATION.md` revision 18. What the run proves end to end: guestd pushing `guest.sensor_health` over vsock 10001, the runner stamping it `guest_reported`/`sensor=guestd` with a stream identity the guest never names, the spool carrying it across a dead controller, and `telemetry_health` answering as a §138 dimension separate from lifecycle state — one VM boot, three stream identities, each new one restarting `source_seq` at 1.
  **AT-074: TESTED_PASS (partial).** guestd stopped inside a live guest → `telemetry_health` healthy → `degraded` after 20s while `observed_state` stayed `running`, with the host's own `guest.channel_lost` recording the outage at 10s. Partial in three named ways: one privilege profile (the host has one), no egress enforcement and no strict mode in M2a.
  **AT-075: TESTED_PASS (partial).** The controller owning a running VM was SIGKILLed and a successor started over the same state dir: `running → running`, revision 3 → 3 (adoption writes no transition), VMM pid *and* `/proc` start time unchanged, runner pid *and* start time unchanged — the attached-runner branch, not a respawn — and the telemetry stream identities byte-identical across the restart. Partial: the runner-kill half is unit-covered in `internal/jailer`, and the cgroup half of §320's "identity/cgroup evidence" is not implemented — identity here is argv + pid + `/proc/<pid>/stat` field 22.
  **Ruling: the ring-overflow criterion is recorded `inconclusive`, not attempted.** 1024 items drained on host acknowledgement, with the heartbeat as the ring's only producer (`health.go`'s `Beat` is the sole `Push` call site until M2b), is ~2.8 hours to fill. A guestd built to push faster would be a test-only agent shipped inside the product. The wire path rides every heartbeat and the accounting is unit-covered; only the live non-zero `dropped` is unproven, and no row claims it. Cost if wrong: a drop-accounting bug that only appears under real pressure survives to M2b, where a sensor firehose will find it in minutes.
  **Ruling: "degraded then unavailable" was the plan overstating.** `unavailable` is not reachable by killing guestd on a running VM — `deriveTelemetryHealth` returns `degraded` for a stale heartbeat and reserves `unavailable` for a non-running VM, an empty current boot, or no heartbeat at all. §952 asks for either, so the gate proves `degraded` from a dead agent and `unavailable` from a stopped VM. Cost if wrong: nothing in the product; the plan sentence was wrong, not the code.
  **Ruling: AT-075 restarts the one controller rather than racing two.** The first draft booted a third VM under throwaway daemons while the primary was still up; it failed at stage `attached` and the fatal step could not be recovered, because `doRollback` removes the VM state dir with `runner.log` inside it. Filed as `b2t2` (rollback destroys the evidence) and `4fap` (`allocateSlot` scans only its own state dir, so two daemons both take slot 0 — and slot drives the jail uid and the guest CID). The causal chain from that collision to that specific exit is *not* proven and neither the acceptance block nor the kata claims it. Production runs one controller, which is what AT-075 names. Cost if wrong: the two-controller failure mode stays unreproduced until someone runs one deliberately.
- 2026-09-05 (M2a merged) — `m2a-guest-telemetry` fast-forwarded onto `main` at `0517f19`, fourteen commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept, identical to main. The guest half of M2 now has a spine: a bounded ring in guestd, a dedicated vsock port, a host-named stream the guest counts within, one registered kind flowing end to end, and `telemetry_health` as a §138 dimension a stopped VM and a dead agent answer differently. Kata `ffxv` closed with it — its adoption half is Task 6, proven live by AT-075. New katas from the run: `b2t2` (P3, a failed launch's rollback destroys `runner.log`) and `4fap` (P3, two daemons on one host both take slot 0). Open katas: `zd43`, `ynx9` (blocked), `tn5q`, `b33f` (P1), `q4b2`, `sd56`, `2jt1` (P2), `3tn6`, `f8f2`, `b2t2`, `4fap` (P3). Next: M2b — the eBPF process sensor, and with it the deferred toolchain question (clang/LLVM + `bpf2go` in `scripts/check`, §247 object digests in `runtime.lock.json`). It needs a plan before code.
- 2026-09-05 (kata `tn5q`) — Branch `m2-durable-ownership-records`, two commits. Both records that name resources a crash must reclaim — the privd ledger entry and the jailer manifest — now publish through a new `internal/durable`: temp file in the target directory, write, chmod, `Sync`, close, `Rename`, then `SyncDir`. `Remove` and `MkdirAll` take the same directory barrier, and the removals in `launch.go`'s rollback and `stop.go`'s release move with them, because a state-directory removal a crash could undo puts a slot, uid and cid back in use under a VM already told they were free.
  **Ruling: take the barriers on the ledger anyway, and write the tmpfs limit into the code.** The kata's stated problem — `ledger.put` closes and renames without syncing — is real as written and nearly inert as deployed: `/run/vmobs/privd` is a tmpfs by M1a decision D8 (VMs never survive a host reboot, so a record that clears at reboot is the correct one, and §5.5 cold reconcile reads the resulting empty ledger), and fsync on tmpfs is a no-op that returns success. Skipping the fix would leave the ledger correct only by accident of where systemd puts `/run`; taking it silently would let the next reader assume crash-durability it does not have. So `ledger.go`'s own doc comment now records the limit verbatim. The record where this actually bites is the manifest, under `paths.state` (`/var/lib/vmobs`), real disk — `doRollback` reads it for its target list. Cost if wrong: a few microseconds per ledger write on a path that already holds a mutex.
  **Ruling: the file sync inside `WriteFile` ships without a failing test, and the suite says so out loud.** Six of the seven barriers are mutation-proven and the killers are named: the post-rename `SyncDir`, the temp-file cleanup, `Remove`'s `SyncDir`, `MkdirAll`'s parent syncs, the sync-before-close ordering (seven tests turn red, because `Sync` on a closed descriptor answers `ErrClosed`), and the requested mode — that last one only after adding a 0644 case, since `os.CreateTemp` already answers 0600 and both real callers want 0600, so the `Chmod` mutation had survived. The seventh, `tmp.Sync()` on a regular file, cannot be induced without a full device or a fault-injecting block layer. The test file's header states that rather than implying coverage. Cost if wrong: a dropped file sync would be caught by nothing here — only by power loss.
  **Ruling: no crash harness for the manifest; its own window gets a unit test instead.** `TestLedgerSurvivesAProcessCrash` kills a child mid-write five times and reads back a complete entry each round, and it proves atomic publication and nothing else — the page cache outlives the process, so it passes identically with every fsync removed, which its comment says. Duplicating that harness for the manifest would prove the same thing about the same `durable.WriteFile`. What is different about the manifest path is `MkdirAll` running first, so a crash between the two leaves a VM directory with no record in it; `TestSlotAllocationIgnoresAVMDirectoryWithNoManifest` covers that, and a mutation making an unreadable manifest fatal in `allocateSlot` kills it. Cost if wrong: a manifest-specific publication defect would have to be caught by the jailer suite rather than by a dedicated crash test.
  The first crash-test draft was vacuous and the timing gave it away: five child spawns in 0.02s, because `waitForRecord` returned on the *previous* round's record and the kill landed before that round's child wrote anything. Removing the record at the top of each round fixed it. Its own mutation proof: truncate-then-write fails 8/8 with `unexpected end of JSON input`; the real implementation passed 20 consecutive runs.
  Linux evidence: `scripts/linux 'go test -count=1 ./internal/durable/ ./internal/privd/ ./internal/jailer/'` on aibox03 — all three ok, jailer 42.7s (real runtime). `internal/durable` verbose there: 12 tests, 12 passes, zero skips, as uid 1000 — the 0300-directory failure inductions really run on ext4, where root would have bypassed them.
  `env -u GOROOT mise exec -- ./scripts/check` green; `GOOS=linux GOARCH=amd64 go vet ./...` clean. Cost measured on darwin, the pessimistic case (`F_FULLFSYNC`, ~4.7 ms per file sync): jailer 0.419s → 0.703s, privd 0.181s → 0.430s.
- 2026-09-05 (tn5q merged) — `m2-durable-ownership-records` fast-forwarded onto `main` at `b72c307`, three commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept. Both privileged ownership records now publish atomically and durably through `internal/durable`, with the tmpfs limit on the ledger written into the code rather than left for a reader to assume. Open katas: `zd43`, `ynx9`, `b33f` (P1), `q4b2`, `sd56`, `2jt1` (P2), `3tn6`, `f8f2`, `b2t2`, `4fap` (P3). Next: `ynx9` — cleanup progress and identity reclamation, which is the half `tn5q` made possible (a cleanup ledger is worth nothing if a crash can lose it).
- 2026-09-05 (kata `ynx9`) — Branch `m2-cleanup-resumes-from-durable-facts`, four commits. Three defects and one gap, each found by asking the kata's own question — what does the code reclaim before it has proof? — and each red-then-green with mutation proofs.
  **The slot lease.** `doRelease` removed `<StateDir>/vms/<id>/` between the stage-dir removal and its verdict. The manifest is not only a record of what a launch provisioned: `allocateSlot` reads a slot as taken for exactly as long as some manifest names it, and `uid = JailUIDBase + slot`, `cid = CIDBase + slot`. So removing it *is* the act of reclaiming the uid and the CID, and doing it before the verdict surrendered them on the failure path — the chroot survives under uid N while the next launch is handed slot N and starts a different VM's firecracker on the same CID as one still alive. The row parking at `deleting` and Reconcile's retry were both correct and both too late. The removal now runs last, after the verdict, and stays durable because nil is what lets the caller reclaim those identities. Three tests (`release_orphan_linux_test.go`): a failed `release_vm`, a failed `release_network`, and a retry that must leave the successor legitimately holding slot 0 alone — nothing `doRelease` destroys is keyed by slot.
  **A stopped row over a live VMM.** `Manager.Reconcile`'s `stopping` branch settled every row at `stopped` with `ReleaseCompute: true` without observing anything, and its own comment ledgered that as waiting on real adoption (M2+). M2a delivered the findings. The red test showed the harm directly: `reservations = {MemoryMiB:0 VCPU:0 DiskMiB:18432 ActiveVMs:1}` while the microVM lives, so admission hands the same memory out twice on a VM whose stop never happened. An adopted row now finishes the stop the way the `deleting` branch finishes a delete — force-stop, settle on the answer, tolerate only `*UnavailableError`. A row with no finding still settles as before: absent evidence is not evidence of a live VMM either, and there is no host verb worth calling on a VM nobody can see. Three mutations, three different killers.
  **Ruling: an unreadable manifest refuses; an absent one is still free.** `allocateSlot` treated "missing or corrupt" as one case and both as free. They are opposites. Missing is provably safe — `writeManifest` creates the directory then publishes into it, so a bare directory is the crash window before any side effect (`tn5q` pinned that). Corrupt means a launch *did* run and the only record of the identities it took is the file that will not parse; skipping it hands a live VM's uid and CID to a new one, and answering that VM's own restart with a fresh slot strands its chroot and vsock. `Reconcile` has always called this case `ambiguous` and touched nothing (§5.5); `allocateSlot` now agrees, fails the allocation and names the VM. Fail-closed costs availability: one corrupt file stops all launches. That is bounded and has an in-API remedy — `Release` is keyed by `vm_id` alone and reads no manifest, so deleting the named VM returns the slot. Cost if wrong: an operator hits a refusal where a silent reallocation would have "worked", which is exactly the working we do not want.
  **Ruling: record cleanup failures, because §5.3 asks for recorded *and* retried and only the retry existed.** A retained row is a bounded result with nothing actionable in it. The synchronous `DELETE` answers its caller with a typed cause and two remediations, but that is a response body — there is no operation row for a delete — and `Reconcile`'s retry at the next start answered nobody. New kind `vm.cleanup_failed` (vm_id, the state the row is held in, the runtime's reason) written by `store.RecordCleanupFailure` from all three sites, redacted on the same terms as an attention summary because a host error string is where a stray credential surfaces. It is a statement, not a transition: no revision is spent, because nothing about the VM changed — the controller merely failed again to change it. Each attempt records, so an operator sees two refusals rather than one collapsed "still stuck".
  **Ruling: `lifecycle_failed`, not a new trigger class.** The situation engine raises the record as an attention item carrying the reason and a force-delete suggestion. `reconciliation_surprise` was the tempting fit and is wrong: the record is written on both paths and only one of them is a restart. A new class would need config plumbing, §12.7 wording and a change to the docs check that pins exactly eight classes, for no operator benefit. Cost if wrong: someone filters on the other class name and misses it; both default to enabled.
  Deviations log: entries at lines 128 and 129 closed after checking the code rather than assumed closed by M2a landing (findings reach `ManagerConfig` before `NewManager` runs Reconcile; `Delete` signals before releasing). Line 137's `stopping` deviation narrowed to the unobserved case, which is still a deviation and still says so.
  Fourteen mutation proofs across the four pieces, thirteen dead. The survivor is recorded, not hidden: dropping the VM name from `allocateSlot`'s own refusal message survives because `readManifest`'s wrap already names it, so the test pins the operator-visible outcome and not the format string.
  `env -u GOROOT mise exec -- ./scripts/check` green, exit 0. Linux: `scripts/linux 'go test -count=1 ./internal/jailer/ ./internal/runtime/ ./internal/store/ ./internal/situation/'` all ok on aibox03 (jailer 43.8s, real runtime); the seven new and adjacent jailer tests run there with zero skips as uid 1000.
  Not done: the kata's "crash at each cleanup boundary" is covered by unit tests at the boundaries, not by a crash harness — `tn5q` established that a process-crash test proves atomic publication and never durability, so a harness here would prove the same thing about the same `durable.WriteFile`. `vm.cleanup_failed` has no web UI surface; the API and the attention queue are the surfaces this kata asked for.
- 2026-09-05 (ynx9 merged) — `m2-cleanup-resumes-from-durable-facts` fast-forwarded onto `main` at `2e9a356`, five commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The branch is kept. Identity reclamation now happens only after the release that earned it, an adopted `stopping` row no longer settles on nothing, an unreadable manifest refuses instead of reallocating a live VM's uid and CID, and every stalled cleanup writes `vm.cleanup_failed` so the retained row states its own reason and reaches the attention queue. Open katas: `zd43`, `b33f` (P1), `q4b2`, `sd56`, `2jt1` (P2), `3tn6`, `f8f2`, `b2t2`, `4fap` (P3). Next: `b33f` — prove VM death before reporting stopped or releasing compute, which is the finding this kata's `stopping` fix opened the door to: adoption now tells the controller which rows have a live VMM, and `b33f` is where "prove it" replaces "assume it" everywhere else.
- 2026-09-05 (kata `b33f`) — Branch `m2-prove-vm-death`, five commits. The kata's question was whether "stopped" is ever written from a signal rather than an observation. It was, on three paths, and each one hands memory back to admission while the microVM keeps running on it.
  **The proof gate.** `doStop` reported success on a SIGKILL privd accepted, not on a process it watched end; and the runner's `finalized` phase is written blind by both `onFinalize` and `cleanExit`, so a guest that breaks the vsock channel after acking a shutdown reaches a terminal phase with its VMM untouched. `doStop` now ends at a gate that polls `/proc` for the manifest's VMM identity — pid *and* start time, because a recycled pid answers for a dead one — and returns `*runtime.ErrStopNotProven` when the process is still there, skipping the finalize and the release that privd would refuse anyway. A terminal runner phase is re-checked against `/proc` and falls through to the forced path when the two disagree. Signals privd refused travel into the reason: they are the whole explanation of why nothing died. A VMM observed gone from the SIGTERM gets no SIGKILL. `vmmProvenGone` reads a pid with no recorded start time as unknown rather than dead — `privd.PIDAlive(pid, "")` matches nothing and answers false for a perfectly healthy VMM.
  **The wiring.** The adapter's startup scan returns three outcomes and `cmd/vmobsd` read two: an `ambiguous` finding — an unreadable manifest, a runner whose recorded VMM identity disagrees with the manifest's — was dropped at the wiring, which made it indistinguishable from a VM proven gone. Reconcile then failed the row with `vmm_disappeared_on_restart` and released its compute on an observation nobody made. §5.5 quarantines those VMs at the runtime layer; a quarantine that ends at the package boundary is not one, so Reconcile now holds the row — state, reservations and revision exactly as the last controller wrote them — and records `vm.reconcile_ambiguous` naming the held state and what the runtime could not tell. Absent from the map still means nothing looked, which keeps the documented deviation for a runtime reporting no findings at all.
  **Ruling: compute and chroot are two different kinds of answer, and only one of them is observed.** Compute is freed by the process ending, which the gate watches. The jail chroot and the privd ledger entry are freed by a call that can fail on its own, and failing it does not resurrect the VM. That split is `*runtime.ErrCleanupPending`: the stop path settles the VM, releases its compute and records the debt as `vm.cleanup_failed` against the state the row reached; the delete path still treats it as failure, because a delete that leaves a chroot has not deleted anything. It used to go to stderr and nowhere else. Cost if wrong: a VM reads `stopped` with host debris behind it — visible in the attention queue, where the old behaviour was visible in a log line nobody keeps.
  **Ruling: `TestForceStopSendsNoGuestShutdown` goes in `stop_ctl_linux_test.go`, and that file's ABOUTME widens from "deadlines" to "what ctl traffic doStop sends".** `stop_linux_test.go` is `package jailer_test` and cannot reach `dialCtlFn` or `doStop`; the ctl file already owns `stopOnlyAdapter`, `writeLiveRunnerManifest` and the recorder pattern, so a new file would duplicate the scaffolding to say one thing. The test exists because `TestForceStopSendsKill` cannot say it: its harness powers the guest off with a no-op, so a forced stop that wrongly took the graceful path waits out the poll and falls through to the same SIGKILL — same signals, five seconds later, green. Mutation proof on aibox03, dropping `!forceImmediate &&` from `stop.go:344`: `TestForceStopSendsKill` still passes (0.70s → 5.69s, the poll timing out); the new test fails with `forced stop sent [shutdown_guest finalize]`. Cost if wrong: the file's name is broader than its first sentence was.
  **Ruling: the live gate asserts liveness and reservation totals together, and disk with them.** Each half passes the other's check — a live VMM under a released reservation oversubscribes the host, a dead VMM whose reservation is still held leaks capacity nothing returns — so asserting either alone leaves the other free. Disk is the third pool and moves on a different event: AT-011's graceful stop keeps it, because the workspace is still there to start onto, and AT-018's force-delete releases it. The charge comes from `GET /vms/<id>` and `GET /host/status`, never from the test's own create parameters, so a host with a different per-VM overhead is measured against the number it publishes; a zero charge fails the subtest rather than making the release arithmetic vacuous. Cost if wrong: two subtests read three numbers where they read two.
  **Ruling: the gate parses `/proc` itself instead of calling `privd.PIDAlive`.** That function is what the stop path's own proof gate calls, and asking the checked code to check itself would make the evidence agree by construction. The risk it takes on is a field read at the wrong offset — field 22 of `/proc/<pid>/stat`, after the last `)` — which would call about every process on the host dead, so both subtests run the read against the live VM first as a positive control. Cost if wrong: a second `/proc/<pid>/stat` parser exists in the tree, in a test.
  Live evidence: aibox03, eight subtests, eight passes, zero skips, 102.6s, real Firecracker — `tests/integration/evidence/m1a-gate-aibox03.txt`, three consecutive green runs. AT-011: `vmA vmm pid 3279008 starttime 40835013 gone from /proc=verified; host reserved 1024 MiB 2 vcpu 8320 MiB disk -> 512 MiB 1 vcpu 8320 MiB disk`. AT-018 force-delete: `vmm pid 3280286 starttime 40843740 gone from /proc=verified; host reserved 1024 MiB 2 vcpu 8320 MiB disk -> 512 MiB 1 vcpu 4160 MiB disk`. `env -u GOROOT mise exec -- ./scripts/check` green locally, exit 0; `uv run docs/validation/check.py` 46/46; `docs/VALIDATION.md` revision 19.
  Not done, and the rows say so: AT-018's gap (b) is narrowed, not closed. One delete out of six cycles reads the reservation totals; the five graceful cycles assert nothing about them, the six-counter delta never queries `/host/status`, and no cycle compares against an idle baseline. The two `scripts/check` failures on aibox03 are environmental and pre-date this branch — `golangci-lint` is not installed there, and the checked-in web dist differs from a rebuild on that host. The local gate runs both and passes.
- 2026-09-05 (b33f merged) — `m2-prove-vm-death` fast-forwarded onto `main` at `01a8d5f`, six commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after, all gates passed. The branch is kept, identical to main. "Stopped" is now a claim backed by a `/proc` read of the manifest's VMM identity on every path that writes it: the graceful stop, the forced stop, a terminal runner phase that disagrees with the process table, and the ambiguous startup finding `cmd/vmobsd` used to drop. Compute is released on the process ending and nothing else; a chroot the release could not reclaim is `ErrCleanupPending` and reaches the attention queue instead of stderr. Open katas: `zd43` (P1), `q4b2`, `sd56`, `2jt1` (P2), `3tn6`, `f8f2`, `b2t2`, `4fap` (P3). Next: `zd43` — anchor privileged signals and cleanup to exact owned processes and paths, which is the same question one layer down (b33f proved the VMM died; `zd43` asks whether privd can prove the thing it signals is the thing it owns). Its own text says security-boundary changes are reviewed before implementation, so it starts with a design conversation, not code.
- 2026-09-05 (kata `zd43`, finding 1 landed) — Branch `m2-anchor-privileged-ops`, two commits, fast-forwarded onto `main` at `9029e94`. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after. The kata's own last acceptance line holds its descriptor-anchoring work for review; the inventory that preceded it turned up something that was not hardening.
  **`StagedFile.Name` was documented as a basename and nothing made it one.** `proto.go` named the five legal files in a comment; no code checked. The name is joined onto two different bases — the caller's stage dir when the file is opened and digest-verified, the jail root when it is copied — and those bases sit at different depths while `stage_dir`'s depth is the caller's to pick, so one name with enough `..` reads from inside the stage root and writes wherever a directory already exists. `O_NOFOLLOW` guards the final component against symlinks and says nothing about `..`; the digest gate checks the bytes match what the caller *claimed*, not that the caller may write them; the uid gate serves one uid, and privd is root where that uid is not. SPEC §3.3 makes validating path roots privd's job, and for the destination it was not doing it. Measured on aibox03 under `t.TempDir()`: 60 caller-chosen bytes written outside the jail root, plus an `Lchown` of the escaped path to the VM's uid. Nothing on the host was touched, and no exploit was run against a live privd.
  **Ruling: an allowlist, not a hunt for hostile syntax.** The set of files a launch stages is closed and privd knows all of it, so `ValidStagedName` compares against `StagedFileNames` verbatim. A predicate that cleaned the name first and compared after would accept `./vmlinux` and `vmlinux/../vmlinux` — names no launch sends — and would then rest on `filepath.Clean` agreeing with the kernel about every input, which is the argument worth not having. Cost if wrong: adding a sixth staged file means editing one slice, and `TestStagedFileNamesAreBasenames` refuses a path in it.
  **Ruling: the list lives on privd's side of the wire and the jailer reads it from there.** `computeStagedFiles` had its own literal copy; a drift in either direction fails every launch or reopens the hole. The boundary is the wire, not the package — the server still validates whatever arrives, and an honest client deriving its list from the same constant costs nothing. Refusal happens in `handleStartVM` before the backend creates a tree or opens an fd, and again in `VerifyStagedFile`, which is exported: the gate's absence in some future caller should be a refusal rather than a traversal. Cost if wrong: one predicate is called twice per file.
  Four mutations, four killed — dropping the handler loop (six hostile names accepted, backend reached), cleaning before comparing (five traversals accepted), a path added to `StagedFileNames`, and dropping the `VerifyStagedFile` guard, which fails naming the 60 bytes it wrote outside the jail root. Linux: `internal/privd` and `internal/jailer` ok on aibox03 (jailer 75.1s, real runtime); the live M1a gate ok at 103.7s, so real Firecracker VMs still boot with the allowlist in front of them.
  Still open in `zd43`, held for review: the signal check/use windows (`CheckSignalIdentity` reads `/proc` then `unix.Kill` on a bare pid; `killJailedVMM` has the same shape with a longer gap), `ReleaseVM` and `AbortStartVM` removing by path name, and `CopyFromPinnedFd`'s destination open with no `O_NOFOLLOW` — plus `handleStartVM` resolving `stage_dir` with `EvalSymlinks` and then discarding the result, so the backend re-derives from the original name. Kernel 6.8 on aibox03 and `golang.org/x/sys` v0.47.0 have `Openat2`, `PidfdOpen` and `PidfdSendSignal`.
- 2026-09-05 (kata `zd43`, design for the rest) — `docs/superpowers/plans/2026-09-05-zd43-anchored-privileged-ops.md` at `b18c227` on `main`, docs only. Doctor Biz asked for the door fixed first and a design for the rest; this is the design, and privd source is untouched until it is read. Measurement moved every one of the three findings the previous entry held, so that list is superseded rather than extended.
  **Two shrank.** `os.FindProcess` on Linux already calls `pidfd_open` — measured on aibox03, the fd reads back as `anon_inode:[pidfd]`, `Signal(0)` after exit and reap returns `os: process already finished`, and `FindProcess` on a pid that does not exist returns a done process. No `unix.PidfdOpen` of our own is needed; what the fix buys is the *order* — descriptor first, identity second, signal third, so a recycle before the open is caught by the starttime compare and a recycle after it cannot redirect the signal. And `os.RemoveAll` on Unix already recurses with `openat`/`unlinkat` against fds and unlinks a symlink rather than following it, so `ReleaseVM` and `AbortStartVM` are not the hole the kata comment described; `chownTree` is safe for the same kind of reason (`WalkDir` does not descend symlinks, `Lchown` changes the link). The removal item drops to consistency work and is the first thing to cut.
  **One grew.** `chownTree` hands the jail tree to the VM's uid before the jailer runs, and the jailer pivot_roots into it, so `firecracker.pid` sits at `/firecracker.pid` inside the VMM's own namespace in a directory that uid owns. privd reads it by path during the 10s `v.sock` wait and echoes the whole file — into `fmt.Errorf("privd: parse firecracker.pid %q: %w", ...)` at `vmops.go:272`, which returns to the controller, and into `r.log.Printf("...firecracker.pid holds %q...")` at `:394`. A symlink planted there during that window turns a `%q` into a root file-read oracle across the boundary SPEC §3.3 puts privd there to hold. Read from the path construction and the ownership; not exploited, and the doc says so.
  **Ruling: one `openat` against a retained dirfd, not `os.Root`, for every name `ValidStagedName` covers.** Measured trap: `Root.OpenFile` ignores a caller's `O_NOFOLLOW` for a symlink that stays inside the root, where `os.OpenFile` refuses the same open today — so a naive port of `VerifyStagedFile` to `os.Root` would loosen a live refusal. A bare filename has no intermediate components to protect, which makes the multi-component resolver both weaker and more machinery than the one syscall. `os.Root` earns exactly one use, the stage directory, whose components privd did not choose. Cost if wrong: two mechanisms in one file instead of one.
  **Ruling: recommend deriving the stage dir rather than validating the caller's harder.** `internal/jailer/launch.go:137` builds it as `filepath.Join(StageRoot, vmID)` — one component, an id privd already validates, under a root privd already configures and the M1a gate already reconciles. Deleting a caller-controlled path from the privileged surface is the same move the allowlist made. Left as a call for Doctor Biz alongside what a leftover jail root should do to a start, where the recommendation is a typed refusal, not a recursive delete on the start path. Cost if wrong: a protocol field privd stops trusting stays on the wire unused, which is its own small lie to clean up later.
  `uv run docs/validation/check.py` 46/46. Probes ran in `t.TempDir()` on aibox03 under go1.27.0 and touched nothing on the host; the probe directory was removed after. No `scripts/check` run — this commit adds one markdown file and changes no Go.
- 2026-09-05 (kata `zd43` complete) — Branch `m2-anchor-privd-descriptors`, five commits, merged to `main` at `db42fd9` with `--no-ff`. The design doc at `docs/superpowers/plans/2026-09-05-zd43-anchored-privileged-ops.md` named four findings; all four are fixed. The theme is that privd stops naming things and starts holding them.
  **`ab89f8a` — signals take a descriptor opened before the identity read.** `CheckSignalIdentity` read `/proc/<pid>/stat` and `unix.Kill` then aimed at a bare pid; between the two the pid can be reaped and reissued, and the signal lands on whatever inherited it. `os.FindProcess` on Linux already opens a pidfd (measured), so the fix is the order, not new machinery: open the handle, then read identity through it, then signal it. A recycle before the open fails the starttime compare; a recycle after it cannot redirect a `pidfd_send_signal`. `killJailedVMM` had the same shape with a longer gap and gets the same treatment.
  **`009e4f9` — the `firecracker.pid` read is bounded at 32 bytes.** The file lives in a directory the VM's uid owns, privd reads it during the 10s `v.sock` wait, and both the parse error and the log line echoed the whole contents through `%q`. A file-read oracle across the §3.3 boundary, closed by reading a pid-sized prefix and nothing more.
  **`47c58ec` — the jail root is a retained descriptor.** Every subsequent operation — the five staged copies, the `v.sock` wait, the chmod, the pid read — is an `openat` with `O_NOFOLLOW` against that fd instead of a fresh path walk. **Ruling: the retained fd survives the jailer's exec.** privd stages `fc-config.json` into the root *before* the exec and firecracker reads it from inside the chroot, so a jailer that swapped the directory under us would already break every start today. Cost if wrong: a `v.sock` timeout and an ENOENT on the pid read, both loud. The live gate passed, so it held.
  **`d5700a9` — `O_EXCL` on staged writes.** `O_TRUNC` would boot a VM into a chroot privd did not build and cannot describe. Doctor Biz chose refusal over kill-and-clear, so a jail root already holding a staged file returns `invalid_state` naming the file and telling the operator to release the VM. `O_EXCL` also refuses a pre-existing hardlink, which `O_NOFOLLOW` says nothing about, without leaning on `fs.protected_hardlinks` — a sysctl privd does not own.
  **`f65168c` — privd derives `<StageRoot>/<vm_id>` and stops reading `req.StageDir`.** **Ruling: `stage_dir` stops being load-bearing.** Validating the caller's path harder was the alternative and it has no good form: string equality against the derived path breaks `TestStartVMSymlinkStageRoot`, which legitimately configures `StageRoot` as a symlink, and identity-by-fstat means opening the caller's path to compare it — the very thing being removed. So containment becomes structural: `ValidVMID` already makes `vm_id` one safe component, the directory is opened `O_NOFOLLOW` against the stage root, and it is held open for the whole verify pass so the bytes copied are the bytes read. The `EvalSymlinks` block in `handleStartVM` — which resolved a path and then discarded the result — is deleted. The field stays on the wire for one cross-check it can still fail: whether its last component names this VM, caught before a jail tree exists. Cost if wrong: an install whose adapter and privd got different stage roots now fails at privd's open with a message naming the directory privd looked in, instead of failing later somewhere worse.
  **`os.Root` is not used anywhere in this work, deliberately.** Measured on go1.27: `Root.OpenFile` drops a caller's `O_NOFOLLOW` for a symlink that stays inside the root, where plain `os.OpenFile` refuses the same open. A port to `os.Root` would have loosened a live refusal. Every fix here is a raw `unix.Openat` against a retained dirfd.
  Fifteen mutations, fifteen dead, across all five commits — including dropping each `O_NOFOLLOW` separately, restoring `O_TRUNC`, reading `req.StageDir` again, and removing the server cross-check. Two vacuous tests found and fixed while working: `symlink_rejected` and `TestVerifyStagedFileReturnsNilFdOnMismatch` both used file names outside `StagedFileNames`, so the allowlist refused them before the behaviour under test ran; both now use `vmlinux`, and the mismatch test asserts the `digest_mismatch` cause.
  `env -u GOROOT mise exec -- ./scripts/check` green locally, exit 0. aibox03 go1.27.0: `internal/privd` ok 1.9s, `internal/jailer` ok 75.0s, `internal/runner` ok 8.3s. `uv run docs/validation/check.py` 46/46. Live M1a gate after Doctor Biz reinstalled privd (binary rebuilt, unit restarted, socket present): eight subtests, eight passes, zero skips, 97.4s, real Firecracker.
  Not done, and the kata close says so: the `RunCmd` hook path skips the `v.sock` wait, the chmod and the pid read, so unit coverage of those three is partial by construction and the live gate is what exercises them. Nothing here defends against a bind mount or a `/proc` special file placed by something already root — `os.Root` is not `openat2`, and neither is `openat`. `JailBase` and `StageRoot` come from flags, so a wrong flag is still a wrong root. Removal (`ReleaseVM`, `AbortStartVM`) still works by path: `os.RemoveAll` already recurses with `openat`/`unlinkat` and unlinks symlinks rather than following them, so the design doc rated anchoring it consistency work and it was not taken.
  Open katas: `q4b2`, `sd56`, `2jt1` (P2), `3tn6`, `f8f2`, `b2t2`, `4fap` (P3). No P1 remains.
- 2026-09-05 (kata `sd56` complete) — Branch `m2-harden-artifact-admission`, four commits. The kata asked whether a pinned artifact's digest actually binds the bytes a VM boots. It did not, in three separate ways, and all three are closed.
  **`79ef4bb` — `lock.OpenPinned` returns the descriptor its digest covered.** The leaf open is `O_NOFOLLOW|O_NONBLOCK` (a symlink refused at the moment that matters; a planted fifo cannot park the open) and the file must be regular. Above it, a per-component `Lstat` walk refuses an escaping or unclean relative path, a symlinked component, and a directory any user on the host may write to. Refusals stay four distinct operator answers — a digest, `absent`, `unsafe_path`, `unreadable` — with the offending detail carried in a typed `*DigestError` rather than parsed back out of an error string, and `Mismatch.Describe()` becomes the one formatter that `main` and `preflight` both call.
  **Ruling: `refuseWritable` tests the other-write bit, not `0o022`.** The strict version refused ten pre-existing `internal/jailer` tests on aibox03 before it refused anything real. Measured on the host: umask 002, `/srv/vmobs` is `harper:vmobs-fixture` 0775 so the fixture group can stage into it, `~/vmobs-build/images/dist` is 0775, and Go creates `t.TempDir()`'s subdirectory with `os.Mkdir(dir, 0777)` — so a group-writable refusal rejects both the live artifact directory and every temp-dir fixture on a umask-002 host. Cost if wrong: a member of a deliberately shared group can swap an artifact without the placement check noticing; the digest still catches it. `TestOpenPinnedAcceptsAGroupWritableDirectory` pins the ruling so a later tightening has to argue with it.
  **Ruling: the trusted-path walk is not applied to `install_path`.** `VerifyBinaries` checks its descriptor but gets no component walk: `install_path` names a file on the operator's filesystem with no root vmobs could assert a walk from, and rooting at `/` would refuse every install under a world-writable ancestor. Cost if wrong: a symlinked `/usr/local/bin/firecracker` under a writable ancestor is caught by its digest and not by placement.
  **Ruling: preflight still does not hash artifacts.** The line above at 2026-09-04 already settled it — `VerifyArtifacts` on a read path means hashing a 1 GiB root image on every `GET /host/status`. Artifacts are verified at stage time, the moment that decides a boot. Cost if wrong: a green `/host/status` says nothing about whether the pinned images still match; the operator finds out at launch.
  **`c729c6f` — `doStage` stages from the verified descriptor, and the digest chain gets an anchor.** It used to hash the artifact through `VerifyArtifacts`, close it, and reopen the same *name* to copy, so what got staged was whatever the name resolved to the second time. Both artifacts are now opened before `MkdirAll`, which also means a refused launch leaves no stage directory behind and neither artifact is copied when the other will be refused. `copyFile` becomes `copyVerified` taking an `*os.File` rather than a path — that signature is what stops the gap being reintroduced. Separately, `computeStagedFiles` hashed the *staged copy* and sent that digest to privd, which verified the staged bytes against a digest derived from those same bytes; it now cross-checks the two lock-backed staged files against the lock's pinned digests. `config.ext4`, `workspace.ext4` and `fc-config.json` are generated per boot and have no pin, and the comment says so rather than implying otherwise.
  **`43bc228` — a real-host rejection gate.** `tests/integration/artifact_admission_test.go` runs a real `vmobsd` against the installed privd, pointed at a disposable artifact root: a copy of `runtime.lock.json` beside hard links to the promoted artifacts, so the 548 MiB kernel and 1 GiB root image are not duplicated on a host that already needs ~21 GB free. The byte-flip case takes a private copy first and inverts one byte at offset 287448208 — one byte in the middle of 548 MiB is the tamper a whole-file digest exists to catch, and a truncated stand-in would prove less. The symlink case points at the pinned bytes under an untrusted name, so only the placement check can refuse it. Each case reads the AT-018 observables before the create and after the VM reaches `failed`: netns, veth, jail dirs, firecracker processes, stage dirs. State-directory entries are deliberately excluded — `POST /vms` writes the VM's record before any launch runs, and that record is what lets an operator read the failure back.
  **Ruling: `OpenPinned`'s placement walk is documented as not race-proof rather than made race-proof.** Its doc comment says the `Lstat` walk is a placement check and cannot be made into a race defence by adding more `Lstat`s. `os.Root` is not the fix — it drops a caller's `O_NOFOLLOW` for an in-root symlink, measured on go1.27 during `zd43`. The descriptor the caller receives is race-proof, which is the property the kata asked for.
  Eight mutations, seven dead — dropping `O_NOFOLLOW`, the component symlink check, the parent and root `refuseWritable` calls, `IsLocal`, `IsRegular`, and the rewind before returning. **M8 survived and is recorded rather than papered over**: replacing `return f, nil` with `f.Close(); return os.Open(path)` is not killed, because a test's substitution lands after `OpenPinned` returns and an internal reopen still finds the original inode. Killing it needs a substitution between the hash and the return inside one call, which a test can only win by racing, and a flaky security test is worse than a documented gap.
  `env -u GOROOT mise exec -- ./scripts/check` green locally, all gates. aibox03 go1.27.0 with `VMOBS_FIXTURE=1`, full `go test ./...`: every package green, `internal/jailer` 76.0s, `tests/integration` 385.3s including the whole M1a gate, and the new rejection gate at 3.2s.
  Not done, deliberately: nothing defends against a bind mount over the artifact path or a `/proc`-backed special file that passes `IsRegular`. Both need the mount namespace, which is privd's side of the §3.3 boundary — `q4b2`'s territory, and `q4b2` says to review that boundary with Doctor Biz before implementing.
- 2026-09-05 (sd56 merged) — `m2-harden-artifact-admission` fast-forwarded onto `main` at `a0b4575`, five commits, no merge commit. `env -u GOROOT mise exec -- ./scripts/check` green on `main` after, all gates passed. A pinned digest now binds bytes rather than a name: the launch stages from the descriptor it hashed, and the staged copy is checked against the lock instead of against itself. Open katas: `q4b2`, `2jt1` (P2), `3tn6`, `f8f2`, `b2t2`, `4fap` (P3). Next: `f8f2` — current-vs-startup lock semantics, which `sd56` was told not to disturb and which the gotchas entry on the lock's two lifetimes has been describing as an open inconsistency since `n6wh`.
- 2026-09-05 (kata `f8f2` complete) — Branch `m2-live-lock-facts`, three commits, fast-forwarded onto `main` at `fd1e667`. `scripts/check` green on `main` after the merge. The kata asked whether a daemon should act on a lock nobody re-verified. It should not, and the answer was worth more than the kata thought.
  **`25c5ef2` — preflight reads `runtime.lock.json` at check time.** `preflight.Config.Lock`/`.LockErr` become `LockPath string`; `Runner.loadLock` reads the file once per report and hands the result to both checks that consult it (`fc_binaries`, `kernel_tuple`), so the two describe the same file at the same moment. `cmd/vmobsd/main.go`'s second `lock.Load` block is deleted and `preflightConfig(cfg)` carries `cfg.Runtime.LockFile` — the same path the jailer stages from. `verifyRuntimeLock` still parses once at startup and stays that way: boot-time tamper evidence (L0-R9 / AT-002) is a different question from a live verdict.
  **Ruling: re-load per run; reject the other two shapes the kata offered.** Refusing to serve a changed lock is wrong because `scripts/aibox03/setup.sh` re-pins as routine host maintenance, and a daemon that stops serving on a legitimate re-pin is worse than the bug it prevents. Naming the parse time in the report is wrong because it makes a wrong answer legible instead of correct. Cost if wrong: one small JSON read per `/host/status`, next to the two binary hashes the check already does.
  **The kata's "not urgent" was too kind, and the correction is the finding.** `Adapter.Availability` (`internal/jailer/adapter.go:88`) turns the first failing preflight check into a `runtime.UnavailableError`, so a stale pin does not merely misreport — it publishes `runtime.available: false` and refuses **every** `POST /vms` until someone restarts the daemon. The operator re-pins to fix the host and the host keeps saying no. A doctor line with the blast radius of the launch path.
  **A configured path with no file behind it now reports `lock_absent`, not `lock_load_error`.** The startup double-load turned a missing file into a load error, so an operator who had never run `setup.sh` was told to re-pin a lock that did not exist. Pass evidence also names the file it read (`pinned by: <path>`).
  **`29c9117` — the served path gets its own gate.** `TestDoctorFollowsTheLockWhileRunning` drives one real daemon on aibox03 through three `/host/status` calls with no restart and no VM: the installed host passes; a re-pin to a digest the binary does not have fails and takes `runtime.available` with it; re-pinning to the truth restores both. It exists because `main.go`'s hook re-runs preflight per request *today* and the comment beside it says caching is future work — a cache added there would leave every unit test green and put the stale-pin behaviour straight back. The lock copy is re-pinned by write-and-rename so a concurrent report cannot read a half-written file.
  Five mutations, five dead. Unit: holding the lock after the first load kills the re-pin test; swallowing the load error kills both corrupted-lock tests; dropping the `ErrNotExist` branch kills the missing-file test; dropping the evidence line kills the pass-evidence assertion. Integration: the held-lock mutation fails on the real host with "fc_binaries after a re-pin the binary does not match = pass, want fail" and `runtime.available` stuck true.
  `env -u GOROOT mise exec -- ./scripts/check` green locally, all gates. aibox03 go1.27.0 with `VMOBS_FIXTURE=1`, full `go test ./...`: every package green, `internal/jailer` 76.0s, `tests/integration` 386.6s including the whole M1a gate; the new freshness gate 1.03s.
  Not done: preflight still does not hash the pinned artifacts (sd56's ruling — a 1 GiB read on a status call), so `fc_binaries` verifies binaries live while the `images` block reports pins without checking the bytes behind them. The launch path checks those at stage time.
  Open katas: `q4b2`, `2jt1` (P2), `3tn6`, `b2t2`, `4fap` (P3). No P1 remains.
- 2026-09-06 (kata `2jt1` complete) — Branch `m1a-real-runtime`, four commits, fast-forwarded onto `main` at `2a1c974`. `scripts/check` green on `main` after the merge. The kata asked for acceptance evidence that a reader can check instead of trust. Evidence is now a record bound by digest, and a prose claim in the docs that nothing backed has been withdrawn.
  **`7d845b9` — `internal/evidence` publishes immutable, digest-bound executions.** An `Execution` names the acceptance row, the outcome (`Executed`/`Specified` × `Pass`/`Fail`/`Blocked`/`Inconclusive`), and a `Manifest` binding the source revision and dirty flag, the sha256 of every binary that ran, the runtime-lock digest with the four artifact digests the lock pins, host facts, and the transcript's own digest. `Publish` writes a fully synced temporary and `os.Link`s it into place: `link(2)` fails on an existing name and does not follow a symlink planted there, so a record cannot be rewritten and a rerun that disagrees has to stand beside the first as a new id. `Validate` is the honesty boundary and runs before anything reaches the disk. `Load`/`Verify`/`Status` read back — `Verify` re-hashes each artifact from the bytes on disk, and a record whose artifact moved stops being evidence rather than becoming a weaker claim.
  **`87cdc59` — the M1a gate publishes one record per row it covers.** `gateRecorder` wraps seven `t.Run` calls (AT-001, AT-004, AT-011, AT-006, AT-009, AT-018, AT-007); each subtest's appended transcript slice becomes that record's artifact. Rows that claim cleanup carry a before/after `Inventory` — netns, veth, jail entries, firecracker procs, state and stage dir entries — and a `Terminal` observation labelled `host_observed`, so "it cleaned up" is a pair of counts a reader can subtract instead of a sentence.
  **Ruling: the remote gate gets its revision from `scripts/linux`, not from git.** The rsync excludes `.git`, so a gate on aibox03 cannot ask what it is testing. The wrapper now carries `VMOBS_SOURCE_REVISION` and `VMOBS_SOURCE_DIRTY` across. Without a revision the gate publishes nothing and says why — a record with no provenance is worse than no record. Cost if wrong: the wrapper prefixes `env VAR=… <command>`, so a remote `a; b` gives the vars to `a` only; put the gate first.
  **Ruling: `doctor_pass` is a precondition, not an acceptance row.** It publishes no record and instead sets `preflight_overall` and `runtime_available` as host facts in every record the run produces. Cost if wrong: its transcript lives only in the committed `.txt`.
  **Ruling: `Publish` does not scan for secrets; the gate does.** §15.3's `assertNoToken` flags any 64-char hex run, which a record's digest fields are by construction. The scan runs in the gate over the authored strings only — summary, expected, actual, unmeasured notes, terminal subject and state — where the run's minted credentials are known.
  **Ruling: `dirty` counts untracked files too.** Permanently true in this repo because of an untracked tool file, but honest: an untracked `.go` file compiles into the binary, and the binary digests carry the real weight either way.
  **Ruling: records are committed** at `tests/integration/evidence/executions/`. A record living only in `/tmp` on one host is not evidence anyone else can check. `ls` is not the reader — the README beside them says so and points at `Load`.
  **`080467a` — a stale claim withdrawn, and the hand-copied digests removed.** `docs/ACCEPTANCE.md` quoted a rootfs digest that `3ec51bf` had superseded, and `docs/VALIDATION.md` revision 19 claimed the 2026-09-05 runs used the same runtime lock as 2026-09-02 — but the re-pin landed at 12:12 that day, three hours before the evidence commit at 15:44 (`git merge-base --is-ancestor` settles it). Revision 20 withdraws the claim by name and the page no longer carries a lock digest anyone has to retype. `docs/ACCEPTANCE.md` gained an **Execution records** section stating that the rules are enforced rather than described, and that the committed `.txt` transcripts are readable history, not provenance.
  **`2a1c974` — `evidence.Names` deleted.** It never earned a caller; `Load` is the reader.
  Live run on aibox03 2026-09-06, go1.27.0: `TestM1aGate` 8 subtests, 8 passes, 0 skips, 102.11s. Seven records published, pulled back with `tar | base64` over plain ssh (the next `scripts/linux` rsync `--delete` would have eaten anything left inside the tree), and round-tripped: `Load` validated all seven, `Verify` re-hashed every artifact, `Status` returned pass for each and "no executed real-host record" for AT-005. The transcript is byte-identical to the committed 2026-09-05 one once uuids, timestamps, pids and temp-dir names are normalized, apart from the appended record list. `env -u GOROOT mise exec -- ./scripts/check` green, all gates; `uv run docs/validation/check.py` 47/47.
  Not done, deliberately: no `cmd/vmobs-evidence` — reading records back is covered by 16 package tests and the one-off verifier was declared throwaway and deleted. The records name `87cdc59`, the commit that produced them, not `main`'s tip; re-running to make the id prettier would have replaced real evidence with tidier evidence.
  Open katas: `q4b2` (P2), `3tn6` (P3), `b2t2`, `4fap` (P4). **`q4b2` is blocked on Doctor Biz** — its own text requires reviewing the device/cgroup/security boundary with him before implementation, and `sd56` and `f8f2` both stopped at the same line.
- 2026-09-06 (kata `b2t2` complete) — Branch `fix-launch-postmortem`, three commits, fast-forwarded onto `main`. The kata: every launch that fails at `runner_spawned` or `attached` reports the stage and then deletes the only record of why, because `doRollback` removes the state directory `runner.log` lives in. It was found while diagnosing an M2a gate failure whose fatal step was never isolated.
  **`internal/jailer/postmortem.go` — the rollback keeps the log.** `rescueFailedLaunch` copies `runner.log` and `runner-state.json` out of `<StateDir>/vms/<id>` into `<StateDir>/failed/<UTC timestamp>-<vm>-<boot>/` before the removal, and the launch error gains `; runner output rescued to <path>`. `runLaunch` writes that error into the VM's `FailureReason` and the operation's `ErrorMessage`, so the path reaches the operator through the API rather than only the daemon log.
  **Ruling: the archive lives at `<StateDir>/failed/`, beside `vms/`, not inside it.** `allocateSlot`, `Reconcile`, `stop.go` and the M1a gate's `stateDirEntries` all read every entry of `<StateDir>/vms` as a VM; an archive in there would read as a leak on every cleanup assertion in the suite. Cost if wrong: one more directory under the state dir.
  **Ruling: the bytes stay on disk and the error names the path, rather than the log tail going into the error.** The archive inherits the state dir's 0700/0600 regime, so nothing widens; a path is remediation, which is what SPEC asks a typed error to carry. Cost if wrong: an operator has to open a file instead of reading the message.
  **Ruling: the rescue happens inside `doRollback`, not in the `rollback` closure.** `doRollback` is the thing that deletes the directory, so a second call site cannot forget it, and by then the runner has been killed — rescuing any earlier would archive a half-written log.
  **Ruling: truncation is announced in the file.** Over 256 KiB, the archived copy opens with `[rescued tail: last N of M bytes; earlier output dropped]`. A truncated log that looks whole is a lie about what the runner printed. One rule for both artifacts; a 256 KiB `runner-state.json` is pathological and its tail still beats nothing.
  **Ruling: a rescue that lands nothing returns "" and removes its own directory.** The first draft's error said "rescued" whenever the directory was created, which would point the reader at an empty archive on a full disk. The counter is the honesty fix, and the message became "runner output rescued" because it is now the only claim both artifacts can support.
  **Ruling: the kata's suggested target — the gate harness's postmortem directory — is rejected.** That is test code writing to `/tmp/m1a-gate-postmortem-<pid>/` (`tests/integration/postmortem_test.go`); a daemon does not write into a test's scratch directory. The two converged independently on the same two files and the same §15.3 token exclusion, and they cover opposite halves: the harness rescues VMs it can still see, the rollback rescues the ones it deleted before the test could look. `postmortem.go` says so in a comment.
  **Ruling: `archiveSegment` sanitizes both ids even though both are server-minted.** The archive name is built by concatenation, and a separator reaching it would put a rollback's writes outside the state directory. Six lines against a directory traversal on the failure path.
  **A pre-existing docs defect, found and fixed in passing: AT-005's nine `launch.go` line citations were all wrong at HEAD**, by 4, 9 and 10 lines in different places, because separate commits inserted lines above them. `git show HEAD:internal/jailer/launch.go` settles each one — the doc's `:107` "state dir mkdir" is `CIDR: cidr,`. All nine re-derived and corrected; `check.py` cannot catch this class, so the remedy recorded in `gotchas.md` is `git grep -n 'launch.go:' docs/` after moving code. A line citation into code rots exactly like a digest quoted into prose.
  **AT-075's comment block is corrected too**: it said recovering the lost log "needs a product change outside M2a's scope". That change now exists, and the block says the run it describes predates it, so that run's fatal step stays unknown.
  Eight mutations, eight dead: rescue the token; drop the truncation marker; head instead of tail; archive when there is nothing to rescue; prune the newest instead of the oldest; skip the prune; drop `archiveSegment`; and — on aibox03 — replace the rescue call with `"", nil`, which reproduced the kata's original message exactly and was caught by the attach subtest.
  `env -u GOROOT mise exec -- ./scripts/check` green, all gates; `uv run docs/validation/check.py` 47/47. aibox03 go1.27.0, full `go test ./...` green, `tests/integration` 388.5s; then `internal/jailer` re-run on the committed bytes: 6 rescue tests, `TestInject` 9/9, `TestRollbackRetriesReleaseVM`, 24.6s. The observed error, verbatim: `launch vm-inject-6 failed at stage attached: timeout waiting for runner to attach: context deadline exceeded; runner output rescued to /tmp/…/state/failed/20260906T053820Z-vm-inject-6-boot-vm-inject-6`.
  Not done, deliberately: nothing sweeps `<StateDir>/failed/` on daemon start, and no API surfaces the archives. Sixteen archives of at most 512 KiB is 8 MiB worst case, and an operator reading a `FailureReason` already has the path. A cleanup backlog is M1b's.
  Open katas: `q4b2` (P2), `3tn6` (P3), `4fap` (P4). **Both `q4b2` and `3tn6` are blocked on Doctor Biz** — `q4b2` requires reviewing the device/cgroup/security boundary with him before implementation; `3tn6` authorizes backlog and design work but not the API or security-boundary change itself. `4fap` (two daemons on one host collide, because `allocateSlot` scans only its own state dir) is unblocked and had `b2t2` as its precondition.
- 2026-09-06 (kata `4fap` complete) — Branch `privd-refuses-held-identities`, two commits, fast-forwarded onto `main`. The kata: `allocateSlot` scans one state dir, so two vmobsd processes on one host both hand out slot 0 and both claim jail uid 20000, guest CID 3 and the first subnet. Nothing detected it; the second VM failed four stages later at `attached` with "runner exited before attaching".
  **`internal/privd/server.go`, `internal/privd/ledger.go` — the ledger becomes a registry.** `ledger.all()` reads every `<valid vm_id>.json` in the ledger directory. `allocate_network` refuses a subnet another entry holds; `start_vm` refuses a jail uid or a guest CID another entry holds. Both are `invalid_state` and both name the holder's vm_id, so the collision is reported where it happens instead of surfacing as a VM that would not boot.
  **Ruling: privd is the registry, and the protocol does not change.** The kata offered two directions — a host-scoped registry, or identities derived from something host-unique. privd is already the single process every claim passes through, and its ledger already records the uid, the CID and the subnet per VM, so the fix is a scan and two refusals: no new verb, no new field, no new file. Cost if wrong: the refusal is only as strong as one-privd-per-host, which the systemd unit gives us and the binary does not (kata `c3f2`); host-unique derivation would have needed no coordination at all.
  **Ruling: a non-empty field is a live lease.** `release_vm` zeroes uid, gid, CID and pid; `release_network` clears the CIDR; whichever empties the last half deletes the file. So an identity still written down is one still held, and the slot allocator is right to hand out the rest. Cost if wrong: a privd killed between the VM dying and `release_vm` holds a lease nobody owns — bounded, because the ledger directory is tmpfs and a reboot clears every lease.
  **Ruling: an entry that will not parse refuses the claim.** It held a subnet, a uid and a CID, and which ones is now unknown. Unknown is not free — the same ruling `allocateSlot` makes about an unreadable manifest.
  **Ruling: the scan reads every entry, including the requesting VM's own.** The first draft excluded it and shipped a test claiming to prove the exclusion; the mutation round showed the test passed with the exclusion removed. It was dead: `subnetHolder` is reached only after `ledger.get` returned `ErrNotExist`, and `handleStartVM` writes the ledger only after `StartVM` returns a pid, so a VM's own entry always reads zero. The parameter is gone and the test now pins the invariant that makes `all()` safe — a start that fails records nothing, so a retry is not refused by its own dead attempt. Cost if wrong: move that ledger write above `StartVM` and a VM refuses its own retry; that is now a failing test.
  **A defect the fix could have shipped: `cid_base: 0`.** `identityHolder` reads a zero CID as released. Config accepts `cid_base: 0` and validates nothing, which would give slot 0 a live VM whose CID reads as given back — the refusal silently not firing on the one case it exists for. `start_vm` now refuses a CID below 3, which the vsock spec reserves and firecracker rejects anyway, and the two `!= 0` guards came out: with both ranges checked, no valid request can ask for the value a released entry holds. A guard that produces a false negative on the path it guards is worse than none.
  **Ruling: the name filter is `ValidVMID`, not a dot-prefix skip.** The first draft skipped dot-prefixed names under a comment saying it excluded `durable.WriteFile`'s `.publish-*.tmp` temporaries. False — those end in `.tmp`, and the `.json` suffix check already dropped them. A comment naming the wrong reason is worse than no comment; the filter now says what it does and a planted temp file is a test.
  **Ruling: no `docs/ACCEPTANCE.md` row.** No acceptance criterion covers host-global identity claims, and minting an AT id for a hardening kata pollutes a stable-ID space that the gate reads.
  **Ruling: one privd per host is an assumption this fix inherits and does not fix.** `cmd/vmobs-privd/main_linux.go` unlinks the socket path before listening, so a second privd takes the address from a live first one, which keeps running and serving nobody. Two privds is two ledgers and every refusal above goes quiet. The systemd unit gives us one instance; the binary does not. Filed as kata `c3f2` rather than folded in here — a socket-locking change touches startup and deployment, and mixing it into a claim-path change would have made both harder to review. Cost if wrong: the refusals are sound only under the deployment we actually run.
  Eight mutations, eight dead: drop the subnet check; drop the identity check; write the ledger before `StartVM` returns a pid; match on uid only; swallow an unreadable entry; drop the `ValidVMID` name filter; drop the CID range check; and — on aibox03, over real sockets — neuter both scans with `entries[:0]`, which fails `TestTwoDaemonsCollideLoudly` and passes again when reverted.
  `env -u GOROOT mise exec -- ./scripts/check` green locally, all gates. aibox03 go1.27.0: full `go test ./...` green (`internal/jailer` 75.5s, `internal/privd` 2.3s); then on the committed bytes `internal/privd` 1.94s with `TestTwoDaemonsCollideLoudly` PASS in 0.03s — one privd on a real unix socket, a fresh connection per verb, three daemons: the subnet refused, the uid refused, and a third daemon carrying its own `jail_uid_base` but the default `cid_base` refused on the CID alone. The full live gate with `VMOBS_FIXTURE=1` ran green at 386.5s, next to `b2t2`'s 388.5s and `f8f2`'s 386.6s.
  **The live gate cannot have exercised any of this, and says so.** `tests/integration` dials the *installed* privd at `/run/vmobs/privd.sock` (`m1a_gate_test.go:35`). `/usr/local/sbin/vmobs-privd` on aibox03 was built 2026-09-06 03:37:37 UTC; the fix committed at 06:00:04 UTC. So the gate ran against the pre-fix server, and its green run is a no-regression result, not evidence for the refusals. Those rest on `TestTwoDaemonsCollideLoudly` over real sockets plus seven portable tests at the `dispatch` seam. Installing the new privd needs Doctor Biz personally: `ssh -t harper@100.64.0.100 'cd vmobs-build && sudo sh scripts/aibox03/setup.sh'`.
  Not done, deliberately: `internal/config` still accepts `cid_base: 0` and validates nothing. The refusal at `start_vm` is the load-bearing guard because it stands at the claim regardless of which daemon's config produced the request; a config validator would be a second, weaker copy of the same rule. Nothing sweeps a lease whose VM died while privd was down — the ledger directory is tmpfs, so a reboot clears every one.
  Open katas: `q4b2` (P2), `3tn6` (P3), `c3f2` (P4). **`q4b2` and `3tn6` are both blocked on Doctor Biz** by their own text — `q4b2` requires reviewing the device/cgroup/security boundary with him, `3tn6` authorizes backlog and design work but not the API or security-boundary change. `c3f2` (a second privd unlinks a live one's socket) is unblocked and is `4fap`'s own honest gap.
- 2026-09-06 (kata `c3f2` complete) — Branch `privd-single-instance`, two commits (`8a1f90a` the fix, `6501aca` the docs), fast-forwarded onto `main`. The kata: `main_linux.go` removed the socket path before listening, so a second privd unlinked a live first one's socket and bound its own. The first kept running on a name nothing could reach; every client silently moved to the second. Found by reading the listen path while closing `4fap`, not in a run.
  **`internal/privd/instancelock.go`, `cmd/vmobs-privd/singleton.go` — two flocks, taken before anything is removed or bound.** `acquireSingleton` holds `<socket>.lock` and `<ledger-dir>/.privd.lock`, then clears the stale socket. The order is the whole fix: the hold is what makes that socket provably stale. A second privd now dies at startup naming the socket and the holder's pid.
  **Ruling: flock, not the kata's first direction (bind, then connect-probe on EADDRINUSE).** The probe is racy — a privd that has unlinked but not yet bound answers ECONNREFUSED and reads as stale — and it covers only the address. flock's hold belongs to the open file description, so the kernel drops it when the process exits, is killed or panics; there is no stale-lock state to reason about, which is the failure class a pidfile would have added. Cost if wrong: a lock file per socket and per ledger directory, and an operator who deletes one by hand defeats it.
  **Ruling: two locks, because there are two invariants.** The `4fap` refusals are read out of the ledger, and two privds given different socket paths but one `--ledger-dir` would serialize their claims behind separate mutexes and both read an identity as free. The socket lock alone does not catch that; the ledger lock does. Cost if wrong: one extra file. `TestSecondPrivdCannotTakeTheSocketWithItsOwnLedger` and `TestSecondPrivdCannotShareTheLedger` are the two halves, and neither passes with the other lock alone.
  **Ruling: the ledger lock lives inside the directory, not beside it.** Two names for one directory then take one lock, because the kernel resolves both to one inode; a sibling `<dir>.privd.lock` would be two files when the directory is reached through a symlink. The scan ignores it — an entry is `<valid vm_id>.json`, which `.privd.lock` is not.
  **Ruling: the lock files are never removed on shutdown.** Unlinking one while another privd sits between its `open` and its `flock` leaves the two holding different inodes and both believing they own the host. The socket goes; the locks stay.
  **A bug the mutation round found in my own test.** `TestSecondPrivdIsRefusedByASpelledDifferentlyPath` passed for a reason unrelated to the code it named: `filepath.Join` cleans its own arguments, so both spellings were already identical before `acquireSingleton` saw them. The mutation that removed `filepath.Abs` survived. Chasing it showed `Abs` was not merely untested but wrong: it resolves `..` lexically while the kernel resolves it after following symlinks, so a cleaned socket path can name a different file than the one `net.Listen` binds — privd would lock one directory and serve a socket in another, on top of a live privd it never noticed. `Abs` is gone, the paths are used as given, and `TestTheLockAndTheSocketResolveTogether` builds the shape where the two resolvers disagree and asserts they stay together.
  **A second harness defect, same round.** Two mutations scored DEAD on a build failure, not a test — each had removed the only remaining use of the `filepath` import. A mutation that does not compile proves nothing, so the harness now reports `DID NOT BUILD` as its own verdict instead of counting it as a kill. Both were rewritten to compile and both then died to named tests.
  **The lock file's name is one constant, owned by the scan that has to ignore it.** `privd.LedgerLockName` lives in `internal/privd/ledger.go` beside `all()`, and `cmd/vmobs-privd` uses it — two spellings of a name that must agree is exactly the drift this avoids. Planting it in `TestAllocateNetworkIgnoresDebrisInTheLedgerDirectory` then surfaced a third thing: a mutation removing the scan's `.json` suffix check survived, because `ValidVMID` is what rejects a dot-prefixed name and the suffix check was doing nothing the debris test could see. It is not redundant — it stands between a valid VM id with no extension and a `get` for a file that is not there, which fails the scan and refuses every claim on the host. `vm-no-extension` joined the planted debris and the mutation now dies. My comment claiming the `.json` suffix caught the lock file was wrong and is corrected.
  Twelve mutations, twelve dead, each by a named test: no socket lock; no ledger lock; remove the socket before the locks (the original bug); clean the paths lexically; `LOCK_SH` for `LOCK_EX`; drop `O_CLOEXEC`; hold the fd in an `*os.File` the caller does not keep; keep the socket lock when the ledger claim fails; record a pid that is not this process's; drop `Release`'s double-close guard; put the ledger lock beside the directory instead of inside it; and drop the ledger scan's filename filter.
  **Two claims in the comments were measured rather than asserted.** An flock held through an `*os.File` really is released by the collector on go1.26.6 — a throwaway probe printed it, and `TestInstanceLockSurvivesCollection` fails against an `*os.File` version, which is why the type keeps a bare int fd. And `O_CLOEXEC` really is load-bearing: `vmops.go:457` uses `exec.CommandContext`, and `os/exec` relies on `O_CLOEXEC` rather than sweeping descriptors, so an inherited lock fd would leave a jailed guest holding privd's hold — `TestInstanceLockDoesNotSurviveIntoAChild` spawns a real child and proves it does not.
  **The shipped binary was run, not just the packages.** On aibox03, with `flock(1)` holding `/tmp/c3f2/privd.sock.lock`, `vmobs-privd` refused at startup: `vmobs-privd: another vmobs-privd owns the socket /tmp/c3f2/privd.sock: lock /tmp/c3f2/privd.sock.lock is already held by another process: resource temporarily unavailable`. It says "another process" rather than a pid because `flock(1)` writes nothing into the file — the fallback path, working; a privd holder records its pid and is named by it. Once the hold dropped, the same command got past the singleton and died at `chown socket: operation not permitted`, which is what an unprivileged privd should fail on and nothing earlier. It left `privd.sock`, `privd.sock.lock` and `ledger/.privd.lock` behind: `log.Fatalf` calls `os.Exit`, so `defer single.release()` never ran and the kernel dropped both holds anyway. That is the argument for flock in one observation.
  `env -u GOROOT mise exec -- ./scripts/check` green locally, all ten gates; `uv run docs/validation/check.py` 47/47. On aibox03 (go1.27.0), on the committed bytes: all eight `InstanceLock` tests and all nine `cmd/vmobs-privd` singleton tests PASS, `go test ./...` green across the tree (`internal/jailer` 75.0s, `internal/privd` 1.96s), and the full live gate with `VMOBS_FIXTURE=1` green at 385.4s, next to `4fap`'s 386.5s and `b2t2`'s 388.5s. **What the live gate does and does not say here.** It dials the *installed* privd at `/run/vmobs/privd.sock` (`m1a_gate_test.go:35`), and `/usr/local/sbin/vmobs-privd` on aibox03 is still the binary built 2026-09-06 03:37:37 UTC — before `4fap` and before this. So the gate is a no-regression result for the launch path, not evidence for the singleton; the singleton's evidence is the shipped-binary run above plus sixteen tests. Both fixes reach the host only when Doctor Biz runs `ssh -t harper@100.64.0.100 'cd vmobs-build && sudo sh scripts/aibox03/setup.sh'`.
- 2026-09-06 (kata `3tn6`, authorized half done — the gap map, pending review) — Commit `e81c18a` on `main`: `docs/design/agent-control-contract.md`, plus its README entry, a VALIDATION revision, and one `check.py` change. The kata says creating it authorizes backlog and design work but not an unreviewed API or security-boundary change, so nothing here ships a route; the document is what Doctor Biz reviews before any of it is built.
  **Ruling: a standalone design doc, not an edit to SPEC.md.** SPEC.md is the binding contract and changing it is his call. A separate page can carry a proposal and be wrong without anything downstream believing it. The doc says so in its own first section and marks every proposed route unimplemented. Cost if wrong: one more file in `docs/` that a future SPEC revision has to absorb or delete.
  **Ruling: every "v2 has this" row is read against the implementation, not the spec.** The map is worthless if a row claims a capability the code does not have, and the kata makes that its acceptance condition. It caught two: `/events/stream` is specified and is registered in `api.New` as a nil handler — a 501 stub — so `watch` is polling today; and `GET /api/v1` (v1's bootstrap path) is not a route at all. Cost if wrong: nothing, this is the cheap direction to be careful in.
  **Ruling: citations name a file and a symbol, never a line.** `check.py` validates the docs package's structure and never opens a Go file, so a line citation rots in silence — the lesson `b2t2` already paid for in AT-005.
  **`GET /api/v1` was measured, not reasoned.** A throwaway test against a live `httptest` server, then deleted: status 404, `cause: route_unknown`, and the body carries the `/meta` remediation. So an agent arriving with v1's bootstrap habit is redirected rather than stopped — errors-teach doing the discovery work, and the one place where v2's principles cover a v1 incompatibility for free.
  **Two findings are against v2's own rules, not just v1's.** `handleMeta` derives `features` from the route table and then builds `links` as a literal map of ten paths under a hand-held rule; `features` reports `vm_batches: true` and no key names a batch path, so the feature is advertised with nowhere to send it. `docs/README.md`'s own builder directive forbids the second hand-maintained copy, and P-07 says the same. And `/situation` sheds `attention_head` then `changed_vms` by halving, in silence, while `situation.Engine.Snapshot` computes `AttentionOpen` on every request into a struct field the API never reads. The shed loop's comment says a truncated watch scope would violate P-03 and it never sheds one — then truncates the two lists beside it without a count. A short `attention_head` reads as a calm host for exactly the reason P-03 names.
  **A trap in the kata's own reference, recorded so nobody walks into it.** It points at v1's AT-089..095. Those seven IDs are already taken in v2 by seven different tests — v1's AT-089 is cold-agent discovery, v2's AT-089 is `/situation` under a mixed fleet. `check.py` asserts exactly 102 sequential rows and `internal/evidence`'s `acceptanceRows` mirrors the count, so copying a v1 id either fails the package check or silently rebinds a live row. The doc's proposed scenarios are AT-103..106 and are marked unimplemented.
  **One check changed, and it bites.** The README file inventory regex matched only `schemas/`, `examples/` and `validation/check.py`, so a README line naming any other path was never verified to exist. It now covers `design/` too — renaming the new document away drops the run to 46/47 on "Every file listed in README exists", measured both ways.
  `env -u GOROOT mise exec -- ./scripts/check` green locally, all ten gates; `uv run docs/validation/check.py` 47/47. No aibox03 run: this commit changes no Go behaviour — the only `.py` change is a docs-inventory regex and the rest is markdown.
  Ranked recommendation in the doc: generate `/meta`'s links, then situation omission counts, then checkpoints (the one missing v1 object v2's own P-04 already promises), then a watch cursor on mutation responses, then `retry_strategy`; plan/apply and budgets wait until there is an intent that needs a preview. The first five are additive response fields and one new table — none touches privd, the guest protocol, or the privileged boundary.
  Open katas: `q4b2` (P2, blocked on reviewing the device/cgroup/security boundary with Doctor Biz), `3tn6` (P3, open pending his review of the map).
- 2026-09-06 (kata `q4b2`, authorized half done — the container boundary, measured) — `docs/design/container-boundary.md`, plus its README entry and VALIDATION Revision 24. The kata blocks implementation on a device/cgroup/security review with Doctor Biz and permits packaging investigation alongside; this produces what that review needs and says so in its own §0. No Dockerfile, no compose file, no shipping profile — the kata forbids promoting diagnostic permissions into shipping configuration without review, and a document is the thing that gets reviewed.
  **Ruling: v1's container measurements are re-derived, not copied.** v1's plan doc has real numbers from v1's stack, and v1's stack is not v2's: v1 ran Firecracker directly, v2 runs it under the jailer inside a per-VM netns behind a privileged daemon. v1's own doc says its passing test "did not validate guest authentication, jailer isolation, TAP/network namespaces, the helper, restart recovery, or AT-002" and that "neither `NET_ADMIN` nor `/dev/net/tun` was added here" — exactly the halves v2 depends on. Cost if wrong: a few disposable container runs. Cost if the other way and wrong: a security review reading v1's numbers as v2's, which is theatre.
  **The re-derivation killed two inherited assumptions and found one v1 could not have seen.** `/sys/fs/cgroup` is read-only in every profile including the one that works, and v2's jailer exits 0 anyway creating no cgroup directory — with `--cgroup-version 2` and no `--cgroup`/`--parent-cgroup` (`vmops.go:319`) no cgroup write is attempted, so v1's cgroup wall (systemd needing `/init.scope`) is not v2's. `/dev/vhost-vsock` is never opened: the guest channel is a unix socket at `<JailBase>/firecracker/<id>/root/v.sock` (`runner/runner.go:52,226`), so passing that device would grant it for nothing. And the new one: **Docker's default seccomp profile blocks `pivot_root`**, so the jailer fails at no capability level until seccomp is lifted. v1 never ran the jailer, so v1 never met this gate.
  **Three gates, isolated because the jailer fails at a different step in each profile.** Default caps die at `SetNetNs` (EPERM) — namespace joining needs `CAP_SYS_ADMIN`. With both caps and seccomp fully unconfined it still dies at `mount --make-slave` (EACCES) — `docker-default`'s blanket mount denial, which no capability defeats. With AppArmor unconfined and seccomp loaded it dies at `pivot_root` (EPERM). With both lifted: exit 0, `Firecracker v1.16.1` printed from inside the jail, identical to `--privileged` on every cell. So v1's profile needed one exception; v2's needs two, and the second is invisible until the jailer runs.
  **Measured on aibox03 2026-09-06** (Docker 27.2.1, kernel 6.8.0-138-generic, Ubuntu 24.04.4, jailer/Firecracker v1.16.1). Default container capability mask `0x00000000a80425fb` = 14 caps, granting `SETUID`/`SETGID`/`KILL` (which the privilege drop and `signal_vm` need) and withholding `NET_ADMIN` and `SYS_ADMIN`. Network verbs measured with the helper's exact ruleset: `CAP_NET_ADMIN` + `/dev/net/tun` buys tap, veth and nft and buys nothing toward `ip netns add`, which fails on its bind-mount. Every probe ran `--rm --network none` from a throwaway image, both removed; each run reported no leftover containers and no host namespace, cgroup or device was touched.
  **What is not measured is named in §7, and the first item is the one that breaks next.** The jailer setuids to the jail uid before exec'ing firecracker, and in-container `/dev/kvm` is `crw-rw---- root:108` — container root's `DAC_OVERRIDE` does not help a process that already dropped to 36000. A profile that runs `jailer --version` is not a profile that boots. Also unmeasured: a real boot, restart recovery under a changed PID namespace, AT-002 in a container, and the narrow seccomp profile (default + `pivot_root`) that option 4 would need. **Wrong, corrected 2026-09-06 by measurement** (§10): the jailer `mknod`s its own `dev/kvm` inside the chroot as root and chowns it to the jail uid, so firecracker opens a node it owns with no supplementary groups at all and `--group-add` answers nothing. The gid matters one layer out, for vmobsd's `arch_kvm` preflight, and `--group-add` cannot deliver it there either — `setpriv --init-groups` rebuilds the set from `/etc/group`. Of the rest, only AT-002 in a container is still unmeasured; the boot, the restart behaviour and the narrow seccomp profile all landed.
  `uv run docs/validation/check.py` 47/47; `env -u GOROOT mise exec -- ./scripts/check` green, all ten gates. No aibox03 gate run: markdown only, no Go behaviour changed.
  The decision the kata is waiting on, stated in §9: is a container holding `CAP_SYS_ADMIN` with no AppArmor mount confinement and no seccomp filter a boundary worth having? If yes, the work is the narrow per-container profiles; if no, `q4b2`'s implementation half should close as declined rather than sit open.
- 2026-09-06 (kata `q4b2`, implementation half — option 4 built, and a VM booted inside it) — Authorized by Doctor Biz on the §9 question. New: `deploy/Dockerfile`, `deploy/entrypoint.sh`, `deploy/config.yaml`, `deploy/apparmor/vmobs-jailer`, `deploy/seccomp/vmobs-jailer.json`, `deploy/README.md`, `scripts/vmobs-container`, `.dockerignore`. Commits `c494189` (privd jail-base fix) and `e57e3b7` (image provenance, kvm gid, build context). Evidence in `docs/design/container-boundary.md` §10.
  **Two VMs at the m1a gate's shape reached `running` in 7s each** inside the container, on jail uids 20000/20001, CIDs 3/4, subnets `10.190.0.0/30` and `10.190.0.4/30`; both `telemetry_health: healthy` with `guest.channel_established` and clean rings at 45s; both stopped and deleted in 1s each, leaving no firecracker, runner, netns, jail dir, stage dir or ledger file, and every reservation at zero. The host's `vmobs-privd` stayed `active` and the host's `/srv/vmobs` was never touched. Image `vmobs:fc56f1f-dirty`; `apparmor=unconfined` throughout, so the AppArmor half is still owed.
  **The fresh install found a real bug, and the host had been hiding it.** privd created `<jail-base>/firecracker` with `MkdirAll(…, 0o750)` under its pinned `0002` umask — a `root:root` directory the unprivileged daemon cannot traverse, which it must, to dial `v.sock` beneath it and to stat the chroot on teardown. Every launch timed out at stage `attached` naming nothing; every delete returned 500. aibox03's host install had the directory at 0755 already from the M0 root helper, and `MkdirAll` leaves an existing mode alone, so the defect had no surface until a clean slate. Fixed as `privd.EnsureJailBase`, called at startup *and* on the launch path: teardown creates nothing, so a launch-only repair leaves an install unable to delete inherited VMs and unable to launch out of the capacity they hold — measured, three rows parked in `deleting` with `POST /vms` refused for their 12480 MiB.
  **Ruling: file the restart-orphan defect as its own kata (`7p8m`) rather than fix it inside `q4b2`.** A container restart kills every VMM (own PID namespace) and empties privd's ledger (`--tmpfs /run`); Reconcile correctly marks the VM `failed`, but the 1.7 GB chroot survives and `RealOps.ReleaseVM` — the only code that removes it — is reachable only through a live ledger entry, so `DELETE` returns 500 `runtime_operation_failed` ("privd: not_found: vm not in ledger") and the row parks in `deleting` forever holding its reservation. This is a host bug too, across a privd restart or reboot; the container just reaches it in one command. Fixing it changes privd's release contract, which `internal/jailer/release_orphan_linux_test.go` pins with nine tests — including that `not_found` is the ordinary answer when nothing survives, the exact rule that misfires when something does. Cost if wrong: q4b2's demo carried one hand-reclaimed directory.
  **Ruling: `--group-add` is not in the run line, and the entrypoint names the kvm gid instead.** `setpriv --init-groups` rebuilds the supplementary set from `/etc/group` and discards what docker granted, so the flag cannot reach vmobsd's `arch_kvm` preflight; the entrypoint reads the gid off the device node it was given. Firecracker never needed it — the jailer makes its own `dev/kvm` in the chroot. Cost if wrong: a host whose `/dev/kvm` is not group-readable fails the preflight loudly at startup rather than silently four stages into a launch.
  **Still owed, and named in §7 rather than glossed:** the AppArmor profile has never been loaded — every run used `apparmor=unconfined`, and `scripts/vmobs-container start` prints a warning when it sees that value. Loading it is one root command on aibox03 (`sudo apparmor_parser -r -W deploy/apparmor/vmobs-jailer`) and it is a Doctor Biz handoff. AT-002 in a container is unmeasured, and no console PTY has been opened in one.
  `env -u GOROOT mise exec -- ./scripts/check` green, all ten gates. On aibox03: `go test ./internal/... ./cmd/... ./tests/deploy/...` all ok.
  Open katas: `q4b2` (P2, implementation half built and measured; open on the AppArmor profile, which needs one root command from Doctor Biz), `7p8m` (P2, new — privd restart strands every jail chroot), `3tn6` (P3, open pending his review of the map).
